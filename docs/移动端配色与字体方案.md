# 移动端配色与字体方案

> 适用范围：`frontend/`（uni-app Vue3，目标端 H5，后续可复用到小程序 / App）
> 设计标准：**WCAG 2.2 Level AA**
> 文档中的对比度、灰度差、色盲可区分度**全部为脚本实测值**，非目测。
> 测量方法见附录 A，一键复现：`node scripts/check-contrast.mjs`（或 `npm run check:contrast`）。

---

## 0. 结论先行

| 项 | 结论 |
|---|---|
| 主色 | 品牌橙 `#FF6B35` **降级为装饰色**（不得承载文字）；实心按钮/选中态用 `#CF4A12`，橙色文字用 `#C7430F` |
| 辅助色 | 信息蓝 `#1D63B8`、成功 `#0B8038`、警告 `#B45309`、危险 `#D92D20` |
| 背景色 | 页面 `#F5F6F8`（保留）、卡片 `#FFFFFF`、浅底 `#EEF1F5` |
| 文字色 | 主 `#1F2329` / 次 `#5A6472` / 辅助 `#6E7787` / 禁用 `#A8B0BD` |
| 字体族 | 系统字体栈，**不引入任何中文 Web 字体**（含数字 `tabular-nums`） |
| 最小字号 | **12px 为中文硬下限**，现有 11px 全部抬升 |
| 字重 | 只用 **400 / 500 / 600**，禁用 700（CJK 伪粗体） |
| 行高 | 正文 1.6、标题 1.4、大数字 1.2 |
| 最关键的修复 | 迁移前有 **11 处对比度不达标**（§1.2 表 ❌ 行数），其中「白字压主色按钮」只有 2.84:1 → **已在阶段 1–4 全部修复**（见 §7.1） |

---

## 1. 现状体检（实测）

> **本节记录的是「迁移前基线」**，用于说明方案要解决什么问题，**不是当前代码的状态**。
> 三个结构性问题已由阶段 1–4 消解（token 层已建立并接入、裸色值已归一、组件改色不再逐个动文件）；
> §1.2 的 11 处对比度不达标已全部修复；**§1.3 的字号问题仍待阶段 5 处理**。
> 当前状态见 §7。

### 1.1 三个结构性问题（迁移前）

**① 没有 token 层。** 全 `src/` 扫描出 **40+ 个裸色值**，其中"同义不同值"的灰色多达 5 个：

| 语义 | 现网同时存在的色值 |
|---|---|
| 页面底 | `#f5f6f8`、`#f7f8fa`、`#f7f7f8`、`#f8f8f8`、`#fafafa` |
| 浅分隔底 | `#f5f5f5`、`#f2f3f5`、`#f0f0f0`、`#f1f1f1`、`#eeeeee` |
| 最弱文字 | `#bbb`、`#ccc`、`#c0c0c0`、`#c8c7cc` |

**② `uni.scss` 里的变量引用次数为 0。** 文件里定义了 `$uni-color-primary` 等一整套 token，但**没有任何组件引用过**——它是一个没人用的设计系统。这是最讽刺的一条：项目**有** token 文件，只是它名存实亡。

**③ 颜色直接写在组件里。** 改一次品牌色要动 15 个 vue 文件。

> **现状（阶段 1–4 后）**：① 裸色值已收敛到 `tokens.scss` 单一真源并完成归一；② `uni.scss` 现在 `@use` 了 token，全部组件都能取到；③ 改品牌色只需改 `tokens.scss` 一处。详见 §7。

### 1.2 对比度实测（WCAG 2.2 AA）

判定标准：正文 **≥4.5:1**，大字（≥24px 或 ≥18.66px 粗体）**≥3:1**。

| 场景 | 色值 | 实测 | 判定 |
|---|---|---|---|
| 正文 | `#333` on `#fff` | 12.63:1 | ✅ |
| 次要文字 | `#666` on `#fff` | 5.74:1 | ✅ |
| **辅助文字** | `#999` on `#fff` | **2.85:1** | ❌ 需 4.5 |
| **最弱文字** | `#bbb` on `#fff` | **1.92:1** | ❌ |
| **占位符** | `#ccc` on `#fff` | **1.61:1** | ❌ |
| **辅助文字（页面底）** | `#999` on `#f5f6f8` | **2.63:1** | ❌ |
| **白字压主色按钮** | `#fff` on `#ff6b35` | **2.84:1** | ❌ 连大字号的 3:1 都不到 |
| **渐变 banner 未端** | `#fff` on `#ffb347` | **1.78:1** | ❌ 严重 |
| **收入金额** | `#52c41a` on `#fff` | **2.27:1** | ❌ 需 4.5 |
| **支出金额** | `#ff4d4f` on `#fff` | **3.27:1** | ❌ 需 4.5 |
| **Tab 选中** | `#ff6b35` on `#fff` | **2.84:1** | ❌ |
| **Tab 未选中** | `#999999` on `#fff` | **2.85:1** | ❌ |
| 链接色 | `#4a90d9` on `#fff` | 3.34:1 | ❌ 需 4.5 |

**11 处不达标**（本节表中 ❌ 的行数）。其中"白字压橙色按钮"是最要命的一处：它是主操作按钮，用户在阳光下、在低端机上最先看不清的就是它。

**这 11 处已在阶段 1–4 全部修复**（改后的实测值）：

| 场景 | 旧 | 新 | 修复后 |
|---|---|---|---|
| 辅助文字 on 白 | `#999` 2.85:1 | `text-tertiary` `#6E7787` | 4.51:1 ✅ |
| 最弱文字 on 白 | `#bbb` 1.92:1 | `text-tertiary` `#6E7787` | 4.51:1 ✅ |
| 占位符 on 白 | `#ccc` 1.61:1 | `text-tertiary` `#6E7787` | 4.51:1 ✅ |
| 辅助文字 on 页面底 | `#999` 2.63:1 | `text-secondary` `#5A6472` | 5.55:1 ✅ |
| 白字压主色按钮 | `#fff`/`#FF6B35` 2.84:1 | `brand-600` `#CF4A12` | 4.52:1 ✅ |
| 渐变 banner 末端 | `#fff`/`#FFB347` 1.78:1 | `$gradient-banner` = `#CF4A12 → #A8380A` | 4.52:1（最浅端）✅ |
| 收入金额 | `#52c41a` 2.27:1 | `income` `#0B8038` | 5.05:1 ✅ |
| 支出金额 | `#ff4d4f` 3.27:1 | `expense` `#D92D20` | 4.83:1 ✅ |
| Tab 选中 | `#ff6b35` 2.84:1 | `#C7430F` | 4.95:1 ✅ |
| Tab 未选中 | `#999999` 2.85:1 | `#6E7787` | 4.51:1 ✅ |
| 链接色 | `#4a90d9` 3.34:1 | `info` `#1D63B8` | 5.95:1 ✅ |

> 上表 **11 行 = 上方表格里 ❌ 的行数**（此数由 `scripts/check-contrast.mjs` 第 12 节自动比对，别再手写）。

> 一个必须记住的边界：`expense` `#D92D20` 在**白卡片**上 4.83:1 达标，但压到**页面底** `#F5F6F8` 上只剩 **4.47:1**（差 0.03）。
> 故 §2.6 规定支出金额只出现在白卡片 / 白列表项上，不得直接落在 `bg-page` 上。同理 `text-tertiary` 在页面底也只有 4.17:1（见 §2.5）。

> 注意一个反直觉点：**颜色越"好看"越容易不达标**。橙、绿、青这类高明度色相天生对比度低——这不是审美问题，是光学问题。解决办法不是放弃品牌色，而是**给它分派正确的职责**（见 §2.2）。

### 1.3 字号现状（**已修复**，阶段 5）

迁移前基线（阶段 1–4 完成后的实测：扫描全部 16 个 `.vue` 的 `<style>` 块）：

| 档位 | 11 | 12 | 13 | 14 | 15 | 16 | 17 | 18 | 20 | 22 | 24 | 26 | 28 | 36 | 48 | 64 | `$font-body` |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 处数 | 6 | 17 | 18 | 18 | 19 | 9 | 1 | 5 | 8 | 3 | 2 | 1 | 2 | 2 | 1 | 1 | 1 |

**合计 17 档 / 114 处。** 11px 出现 **6 处**（中文在手机上笔画糊成一团，属于"设计师在 27 寸屏上做的决定"）；
13px 出现 **18 处**，与 12px / 14px 的语义完全重叠，其中 16 处并入 14px（另 2 处所在选择器在本轮被改名/删除）。

> ⚠️ **本节旧版本的数字是错的，已订正。** 原文写"现网实际使用 `11/12/13/14/15/16/26/36`，共 **8 档**，其中 **11px 出现 3 处**"。
> 两处错误：① 只数了"文字字号"，把 17/18/20/22/24/28/48/64 这 8 个档位整档漏掉了（其中多数是图标/图形尺寸）；
> ② 11px 实际是 **6 处**，不是 3 处。
> **这是"计数没被校验器覆盖"的第二个实例**——第一个是 §1.2 那个从初版起就错的"9 处不达标"。
> 两次犯的是同一个错：校验器只断言**数值**，而没人检查**计数**。

迁移后：**114 处全部走 token，`.vue` 中不再有任何字面量 `font-size`** ——
文字 91 处（`$font-*` 8 档）+ 图标 23 处（`$icon-*` 8 档）。
其中 **31 处渲染值真的变了**（其余 78 处仅换 token），逐条见 §7.1 与 §8。

> **2026-09-12 订正（一）：`$icon-*` 从 23 处降到 2 处。**
> 原因：FL-1 落地时把 emoji 全部换成了 `SvgIcon` 组件，
> **图标改由组件的 `size` prop 控制尺寸，不再走 `font-size`** ——
> 这正是方案 §5.1 第 4 条要根治的问题（emoji 是"字"，会随字号一起放大）。
>
> **2026-09-12 订正（二）：文字字号 91 → 95 处，总数 93 → 97。**
> 原因：明细页按日分组改造（日期头 + 当日小计）新增 4 处 `$font-*`。
>
> **2026-09-12 订正（三）：文字字号 95 → 96 处，总数 97 → 98。**
> 原因：统计页「结余独占一行」改造新增 1 处 `$font-*`。
>
> **2026-09-12 订正（四）：文字字号 96 → 97 处，总数 98 → 99。**
> 原因：我的页新增分组标题「账本与分类」1 处 `$font-*`。
>
> **2026-09-12 订正（五）：文字字号 97 → 105 处，总数 99 → 107。**
> 原因：新增「账本选择」启动页（账本列表 / 兜底态 / 自绘底栏）共 8 处 `$font-*`。
>
> **2026-09-13 订正（六）：文字字号 105 → 104 处，总数 107 → 106。**
> 原因：「记一笔」由悬浮按钮改为**底栏内的凸起项**，`RecordFab.vue` 整份删除
> （它的 `.fab-label` 是 1 处 `$font-*`）。凸起项的文字复用 `TabBar` 原有的
> `.tab-text`，**没有新增字号声明** —— 所以是净减 1 处。
>
> **2026-09-13 订正（七）：文字字号 104 → 110 处，总数 106 → 112。**
> 原因：新增「新建二级分类」与「图标选择」两个页面，共 6 处 `$font-*`。
>
> **2026-09-13 订正（八）：文字字号 110 → 113 处，总数 112 → 115。**
> 原因：分类管理页按参考图重构（9 → 11 处：新增顶栏标题/操作、搜索框、
> 底栏「批量操作」的双行文案与三个操作标签；原支出/收入开关、输入框、提示行随之下线），
> 加上「新建分类」页新增的「层级」说明行（+1），净增 3 处。
>
> 校验脚本 §13④ 的当前期望值为 **115 / 113 / 2 / 0**。
>
> **2026-09-13 订正（九）：文字字号 113 → 102 处，总数 115 → 104。**
> 原因：「明细」下线，删除 `frontend/src/components/views/DetailView.vue`
> （该文件有 11 处 `$font-*`）。**这次是「减少」造成的计数变化** ——
> 删掉一个真正承载内容的视图，字号用量理应跟着降，不是「数字对不上」。
> 当前档位分布（实测）：文字 8 档 —— `$font-body-sm` 34 / `$font-caption` 25 / `$font-body` 23 /
> `$font-body-lg` 9 / `$font-h1` 4 / `$font-h2` 3 / `$font-display` 2 / `$font-display-lg` 2；
> 图标 2 档 —— `$icon-xs` 1 / `$icon-xl` 1。
>
> 校验脚本 §13④ 的当前期望值为 **104 / 102 / 2 / 0**。

---

## 2. 配色方案

### 2.1 三条设计原则

1. **品牌色只做"识别"，不做"承载"**——`#FF6B35` 用于图形、渐变、图标底、图表首色；凡是要在上面写字的地方，一律换深阶。
2. **每个语义只允许一个值**——灰就是灰，不再有 5 个"差不多"的灰。
3. **颜色永不单独传达信息**——收支必须同时有 `+`/`-` 号或文字标签（WCAG 1.4.1）。

### 2.2 品牌主色阶

| Token | 色值 | 白字对比度 | 使用场景 |
|---|---|---|---|
| `brand-50` | `#FFF3EE` | — | 选中行背景、极浅提示底 |
| `brand-100` | `#FFE3D6` | — | 标签底、进度条槽、次级按钮底 |
| `brand-200` | `#FFC7AE` | — | 边框、分割强调 |
| `brand-300` | `#FFA07A` | — | 图形、插画 |
| `brand-400` | `#FF8552` | 2.41 ❌ | 渐变亮端、装饰（**禁放文字**） |
| **`brand-500`** | **`#FF6B35`** | **2.84 ❌** | **品牌基色**：Logo、图标底、渐变、图表首色、装饰条（**禁放文字**） |
| **`brand-600`** | **`#CF4A12`** | **4.52 ✅** | **实心按钮底**（白字）、开关选中底、徽标底 |
| **`brand-700`** | **`#C7430F`** | **4.95 ✅** | **橙色文字**：链接、Tab 选中、焦点环、金额强调 |
| `brand-800` | `#B33A0C` | 5.95 ✅ | 按下态、浅底徽标上的深字 |
| `brand-900` | `#8F2E08` | 8.20 ✅ | 深色渐变暗端 |

**理由**：品牌识别靠的是**色相**而不是明度，所以把 `#FF6B35` 原封不动留在 Logo、图标、图表里，视觉记忆一点没丢；只在"白字压橙底"这类需要对比度的场合换成 `#CF4A12`，人眼在正常浏览下几乎察觉不到这是两个橙。

**另一条路**（如果想 100% 保留 `#FF6B35` 做按钮底）：改用深墨字 `#3D1200` 压在 `#FF6B35` 上，实测 **5.76:1** ✅。适合做「浅橙底 + 深字」的次级按钮，比白字版本更透气。

**首页渐变 banner**：
```scss
// ❌ 现状：#FF6B35 → #FF9563 → #FFB347（白字最差 1.78:1）
// ✅ 改为：
background: linear-gradient(135deg, #CF4A12 0%, #A8380A 100%);  // 白字最差端 4.52:1 ✅
```

### 2.3 辅助色（信息 / 成功 / 警告 / 危险）

| Token | 色值 | on `#fff` | 使用场景 |
|---|---|---|---|
| `info` | `#1D63B8` | 5.95 ✅ | 信息提示、链接型文案、帮助说明 |
| `success` | `#0B8038` | 5.05 ✅ | 保存成功 Toast、正向提示 |
| `warning` | `#B45309` | 5.02 ✅ | 删除前警示、额度提醒 |
| `danger` | `#D92D20` | 4.83 ✅ | 危险操作按钮、错误文案、**支出金额** |

> ⚠️ **不要在同一个列表里同时用 `warning` 和 `brand`**：`#B45309` 与 `#C2410C` 在红绿色盲模拟下距离仅 **11.7**，等于同一个颜色（实测数据）。如果某处两者必须共存，warning 改用 `#9A6700`（偏黄）并加图标区分。

**浅底徽标组合**（深字压浅底，非白底场景，已单独实测）：

| 用途 | 文字色 | 底色 | 对比度 |
|---|---|---|---|
| 收入徽标 | `#0B6B33` | `#E7F6ED` | 5.94:1 ✅ |
| 支出徽标 | `#B42318` | `#FDECEA` | 5.75:1 ✅ |
| 品牌徽标 | `#B33A0C` | `#FFF1EB` | 5.40:1 ✅ |
| 中性徽标 | `#4A5563` | `#EEF1F5` | 6.69:1 ✅ |

> 注意徽标**不能直接复用 `$income` / `$expense` 的色值**——同一个绿压到浅绿底上就掉下去了（实测）：

| 若直接复用主语义色 | 实测 | 判定 |
|---|---|---|
| `$income` `#0B8038` on `#E7F6ED` | 4.52:1 | ✅ 勉强过（余量 0.02） |
| `$expense` `#D92D20` on `#FDECEA` | **4.22:1** | ❌ 差 0.28 |
| `$brand-700` `#C7430F` on `#FFF1EB` | **4.49:1** | ❌ 差 **0.01** |

**结论**：浅底场景必须用专门的深一档文字色（`#0B6B33` / `#B42318` / `#B33A0C`）。这是"同一语义在不同背景上需要不同色值"的典型例子——`#C7430F` 在白底上 4.95:1 达标，压到浅橙底上就只剩 4.49:1。**背景变了，前景必须重算。**

### 2.4 背景与边界

| Token | 色值 | 使用场景 |
|---|---|---|
| `bg-page` | `#F5F6F8` | 页面底色（保留现状，无需改动） |
| `bg-card` | `#FFFFFF` | 卡片、列表项、弹窗 |
| `bg-subtle` | `#EEF1F5` | 输入框底、代码块、次级区块 |
| `bg-mask` | `rgba(0,0,0,0.45)` | 弹窗遮罩 |
| `divider` | `#EDEFF3` | 卡片内分隔线（**纯装饰，豁免对比度要求**） |
| `border-card` | `#E3E7EE` | 卡片描边（装饰） |
| **`border-input`** | **`#8A94A6`** | **表单控件边界**（实测 3.06:1，满足 WCAG 1.4.11 的 3:1） |

**关键区分**：分隔线是"装饰"，可以很浅；**输入框边界是"控件"**，必须 ≥3:1，否则弱视用户找不到这个输入框在哪。现状用 `#c8c7cc` 做边界（≈1.7:1）不达标——这正是很多 App 的表单在弱视用户眼中"消失"的原因。

**焦点环**：`2px solid #C7430F` + `outline-offset: 2px`（4.95:1 ✅）。键盘用户全靠它，别用 `outline: none` 一刀切。

**品牌色投影**：`$shadow-brand-soft` `rgba(207,74,18,0.35)`、`$shadow-brand-glow` `rgba(207,74,18,0.4)`。
品牌色的"淡化"也算品牌色——按钮底改成 `#CF4A12` 之后，阴影仍用 `rgba(255,107,53,…)` 就会出现"按钮比自己的光晕还深"的怪相。
（Dart Sass 不接受 `rgba($list, α)`，所以只能显式写死 rgba，不能用变量拼。）

### 2.5 文字色阶

| Token | 色值 | on 卡片 | on 页面底 | 使用场景 |
|---|---|---|---|---|
| `text-primary` | `#1F2329` | 15.78 ✅ | 14.59 ✅ | 金额、标题、列表主文案 |
| `text-secondary` | `#5A6472` | 6.00 ✅ | 5.55 ✅ | 次级说明、表单标签、备注；**也是非白背景上的安全下限** |
| **`text-tertiary`** | **`#6E7787`** | **4.51 ✅** | **4.17 ❌** | 仅白卡上的时间戳、计数、辅助说明（此前用 `#999`，仅 2.85） |
| `text-disabled` | `#A8B0BD` | 2.19 | 2.02 | 禁用态 / 纯装饰分隔符（WCAG 豁免） |
| `text-inverse` | `#FFFFFF` | — | — | 深底上的文字 |

> `text-tertiary` 的 4.51:1 是**卡着 4.5 线过的**（差 0.01）。这是刻意的：既要满足 AA，又要保持"辅助信息"该有的轻。如果后续要改，只能往深改，不能往浅。

> ⚠️ **`text-tertiary` 只适用于白卡背景。** 换到页面底 `#F5F6F8` 上只剩 **4.17:1**、换到浅底 `#EEF1F5` 上只有 **3.98:1**，都不达标。
> 凡是要落在页面底 / 浅底上的说明文字（如明细页 `.tip`、登录页 `.subtitle` 与 `.tip-text`、`EmptyState` 的文案），一律用 `text-secondary`。
> 这是「**背景一变，前景必须重算**」的第二个实例——第一个是 §5 深色模式。初版表格把 4.17 标成了 ✅，是错的，已订正。

### 2.6 语义色：收入与支出

| 语义 | 色值 | on `#fff` | 场景 |
|---|---|---|---|
| 收入 | `#0B8038` | 5.05 ✅ | 收入金额、结余为正、正向变化 |
| 支出 | `#D92D20` | 4.83 ✅ | 支出金额、负向变化 |
| 中性/结余 | `#1F2329` | 15.78 ✅ | 结余、不区分方向的数字 |

**这里有个必须说清的约定冲突**：

- **记账场景（本项目）**：支出红、收入绿。这与随手记、支付宝账单等国内记账产品的主流习惯一致，且现状代码已经是这个方向，改了反而制造认知断层。
- **投资/股市场景**：**红涨绿跌**（中国内地惯例，与欧美相反）。

**结论：本项目沿用"支出红 / 收入绿"，但这两组语义色 token 禁止被投资类组件复用**——将来若加「理财持仓 / 基金收益」模块，必须另建一组 `market-up`（红 `#D92D20`）/ `market-down`（绿 `#0B8038`）token。把两套约定塞进同一个 token，是未来 bug 的种子。

**并且**：所有收支金额必须带符号。现状明细列表已正确渲染 `+`/`-`，但首页 `formatMoney()` **不输出正负号**（只输出负数号），五区间卡片靠"总收入/总支出"文字标签兜底——这个兜底成立，所以不改也合规；但**新增页面时不能用"只有颜色"来区分收支**。

### 2.7 图表（分类占比环形图）

环形图**最多 7 段**（Top 6 + 「其他」）。不是审美洁癖：8 段以上的环形图，每段都窄到看不清，图例比图还长。

```scss
$chart-series: (
  #c2410c,  // 1 品牌橙红
  #1d63b8,  // 2 蓝
  #c2185b,  // 3 品红
  #0e7c42,  // 4 翠绿
  #7c3aed,  // 5 紫
  #0e7490,  // 6 青
  #8a94a6   // 7 中性灰（「其他」专用）
);
```

> ⚠️ **这一组色值有唯一的双写点。** 图表配色既要在 SCSS 里用（`tokens.scss`），又要在 JS 里用
> （图例圆点、首页区间图标底色都是 JS 内联样式）——SCSS 变量传不进 JS，所以只能存两份。
> TS 侧的真源是 **`frontend/src/constants/chart.ts`**，导出 `CHART_SERIES`（7 色）与
> `SOLID_SERIES`（前 6 色，可承载白字）。**改色必须两处同步**，
> `npm run check:contrast` 的第 10 节会比对两者并直接报不一致。

**白字承载能力**（这条决定了首页区间图标能不能用这套色）：

| 色位 | 白字压其上的对比度 | 能否承载白字 |
|---|---|---|
| 前 6 色 | 5.18 / 5.95 / 5.87 / 5.28 / 5.70 / 5.36 | ✅ 可（≥4.5） |
| 第 7 色中性灰 `#8A94A6` | 3.06 | ❌ 仅图表填充 |

**实测验收**：

| 检查项 | 结果 |
|---|---|
| 每色对白底对比度 | 3.06 ~ 5.95 ✅（作为填充色可见） |
| **相邻两段**在红/绿色盲下的可区分度 | **最低 75.0**（阈值 ≥70）✅ |
| 全组合最小可区分度 | 15.6（品牌橙红 ↔ 品红）⚠️ 二者不相邻，靠图例兜底 |

**配套硬要求（缺一不可）**：
1. 相邻扇区之间加 **2px 白色描边**（`stroke="#FFFFFF" stroke-width="2"`）——让相似色也被物理隔开；
2. 图例必须显示**分类名 + 金额 + 占比**——这才是满足 WCAG 1.4.1 的真正手段，颜色只是辅助；
3. **旧调色板要整体替换——但理由必须说准**（下表全部实测，`node scripts/check-contrast.mjs` 可复现）：

| 旧调色板中真正混淆的色对（`RingChart.vue` 的 8 色） | 色盲距离（阈值 40） |
|---|---|
| `#FF6B35` 品牌橙 ↔ `#F5A623` 琥珀 | **17.1** |
| `#F5A623` 琥珀 ↔ `#FF4081` 品红 | **23.0** |
| `#FF6B35` 品牌橙 ↔ `#FF4081` 品红 | **28.1** |
| `#BD10E0` 紫 ↔ `#50E3C2` 薄荷 | **28.6** |
| `#7ED321` 青柠 ↔ `#9B9B9B` 中性灰 | **33.4** |

另有 3 对**灰度下不可辨**（ΔL < 0.05，灰度截图 / 黑白打印时会糊成一片）：
`#4A90D9` ↔ `#FF4081`（ΔL **0.001**）、`#FF6B35` ↔ `#9B9B9B`（0.007）、`#F5A623` ↔ `#7ED321`（0.043）。

> 📌 **勘误**：本方案初版称"`#7ED321` 青柠与 `#50E3C2` 薄荷色相太近，色盲下几乎同色"——**该结论经复核为错**。
> 实测二者色盲距离 **103.9**，是旧调色板中区分度**较高**的一对。
> 旧调色板的真实毛病是**暖色三兄弟互相打架**：橙 / 琥珀 / 品红两两距离只有 17～28。
> 替换的**结论不变**，但依据已按实测订正。详见 §8 变更记录。

### 2.8 使用场景速查

| 界面元素 | 用色 |
|---|---|
| 主按钮（保存/确定） | 底 `#CF4A12`，字 `#FFFFFF` |
| 主按钮按下态 | 底 `#B33A0C` |
| 次级按钮 | 底 `#FFE3D6`，字 `#B33A0C`（5.40:1 ✅） |
| 记一笔悬浮按钮（FAB） | 底 `#CF4A12`，图标 `#FFFFFF` |
| 底部 Tab 选中 | 图标+文字 `#C7430F` |
| 底部 Tab 未选中 | `#6E7787` |
| 金额输入 | `#1F2329`，36px/600，`tabular-nums` |
| 收入金额 | `#0B8038`，带 `+` |
| 支出金额 | `#D92D20`，带 `-` |
| 分类图标底 | `brand-100` 或图表序列色，白字/深字均可（图标内是 emoji，无需对比度） |
| 空状态插画 | `brand-200` / `text-disabled` |
| 危险操作（删除） | 字/底 `#D92D20` |

---

## 3. 字体方案

### 3.1 字体族

```scss
$font-family-base:
  -apple-system, BlinkMacSystemFont,        // iOS / macOS：SF Pro + 苹方
  'PingFang SC',                            // iOS 中文
  'HarmonyOS Sans SC', 'MiSans',            // 鸿蒙 / 小米（国产 ROM 主力）
  'Source Han Sans SC', 'Noto Sans CJK SC', // Android 中文兜底
  'Microsoft YaHei',                        // Windows（H5 调试）
  'Helvetica Neue', Arial, sans-serif;
```

**为什么坚决不引入中文 Web 字体**：

1. 中文全字库动辄数 MB，`font-display: swap` 会造成首屏字体跳动（FOUT），对于"打开就要记账"的工具类 App，这是拿 3 秒换一个用户永远注意不到的"字体质感"。
2. 移动端系统字体**已经是最适合该设备的字体**：iOS 上苹方的字形是为小字号屏显优化过的，Android 上各 ROM 自带字体的字重也比 Web 字体更齐全。
3. 已核实：`frontend/` 目录下**没有任何字体文件**（唯一的 `.ttf` 是 `uni-ui` 的图标字体 `uniicons.ttf`）。这个现状是对的，别动。

**数字用等宽数字（这是记账 App 的关键细节）**：

```scss
.amount {
  font-variant-numeric: tabular-nums;   // 数字等宽，纵向对齐
  font-feature-settings: 'tnum' 1;      // 小程序/旧 WebView 兜底写法
  text-align: right;                    // 列表金额右对齐
}
```

**理由**：默认的比例数字（proportional）里「1」比「8」窄，导致金额列表的个位、十位、小数点**竖向错位**（形如"锯齿"）。记账 App 的数字列表是要被"竖着扫"的，等宽数字让小数点排成一条直线，扫读速度显著提升。这条比任何配色微调都更影响实际体验。

### 3.2 字号层级 / 字重 / 行高

单位用 **px**（不用 rpx）：rpx 会随屏宽放大，在 6.7 寸大屏上会让 36px 的金额变成 41px，反而撑破布局。App 内文字应当"物理尺寸稳定"，不跟着屏幕等比放大。

| Token | 字号 | 行高 | 字重 | 使用场景 |
|---|---|---|---|---|
| `font-display-lg` | 36px | 44 (1.22) | 600 | 记一笔页金额输入 |
| `font-display` | 28px | 36 (1.29) | 600 | 首页资产总额 |
| `font-h1` | 20px | 28 (1.40) | 600 | 页面标题、卡片主标题 |
| `font-h2` | 17px | 24 (1.41) | 600 | 区块标题（如"本月各分类支出排行"） |
| `font-body-lg` | 16px | 24 (1.50) | 500 | 列表金额、主要动作文字 |
| `font-body` | 15px | 24 (1.60) | 400 | 正文、表单值、备注（**基准字号**） |
| `font-body-sm` | 14px | 22 (1.57) | 400 | 次要正文、列表副标题 |
| `font-caption` | 12px | 18 (1.50) | 400 / 500 | 辅助说明、时间戳、标签（**中文硬下限**） |

**与现状的差异**：

| 现状 | 处理 | 原因 |
|---|---|---|
| 11px（3 处） | **抬到 12px** | 11px 中文笔画粘连，且低于任何移动端可读性经验下限 |
| 13px（多处） | 并入 12 或 14 | 13 与 12/14 差异肉眼难辨，只增加维护成本 |
| 26px | 调为 28px | 与 36px 拉开过近，层级不清 |
| 行高未声明 | 全部显式声明 | 各端默认行高不一致（浏览器 1.2 / 小程序继承行为有差异），必须显式声明 |

**字重只用 400 / 500 / 600，禁用 700**：
CJK 字体在字重缺失时靠**算法加粗（伪粗体）**补齐，结果是笔画粘连发虚——同一份代码在 iOS 上正常、在某些 Android WebView 上"糊"。600（Semibold）是拿得到真字重的最重档位。
> 若目标端字体确实缺 500，会自动降级到 400，视觉层级会塌一档。**上线前需在 Android 真机复验**（尤其 500 字重的金额行）。

### 3.3 换行与字距

```scss
.text {
  letter-spacing: 0;                    // 中文默认字距，不加
  line-break: anywhere;                 // 长串（如金额）允许断行，防溢出
  word-break: break-word;
}
.text-caps {
  letter-spacing: 0.02em;               // 仅英文全大写标签（如 "TOTAL"）才加
}
```

**WCAG 1.4.4 与 1.4.10 相关**：用户把系统字号调到 200% 时，布局必须不横向滚动（320px 宽仍可用）。
因此：所有固定宽度容器改用 `min-width`，不要给 `text` 写死 `width`。

阶段 9 实测后补充四条（原始写法在本节旧版本里是错的，已改）：

1. **不要用 `text-overflow: ellipsis` 给金额兜底。** 截断是**真的丢内容**（`¥12,3…` 无法还原），
   违反 1.4.4。金额没有断行点，正确做法是 `overflow-wrap: anywhere` 让它折行——难看，但一位数字不丢。
2. **`flex: 1` 会把 `flex-basis` 变成 `0%`。** 这在带 `flex-wrap` 的行里是个陷阱：
   换行算法按 hypothetical size 决定要不要换行，basis 为 0 意味着**它认为这一栏不需要任何空间**，
   于是该换行的不换、反而把邻居挤到几十像素宽。**该给内容留空间的 flex 项要写 `flex: 1 1 auto`。**
3. **flex 项默认 `min-width: auto`**（不许窄于内容），是 320px 顶破容器的最常见原因；需要收缩就显式写 `min-width: 0`。
4. **本项目的溢出不是滚动条，是静默丢字。** uni-app 给 `body` 加了全局 `overflow-x: hidden`，
   横向滚动条永远不会出现——溢出要么被祖先裁掉（屏幕上看不到），要么与邻居重叠。
   **所以"`scrollWidth` 没超标"不能作为验收依据**，详见 §7.4。


### 3.4 触控目标

WCAG 2.2 SC 2.5.8 要求触控目标 **≥24×24 CSS px**（AA）。本项目建议 **≥44×44**（Apple HIG / Material 的舒适值）——尤其「左滑编辑/删除」的滑动区域、分类网格的九宫格。

---

## 4. 可访问性合规核对（WCAG 2.2 AA）

| 条款 | 要求 | 本方案对应措施 | 状态 |
|---|---|---|---|
| 1.4.3 对比度（最低） | 正文 ≥4.5:1，大字 ≥3:1 | §2.2–2.6 所有前景/背景组合实测达标 | ✅ |
| 1.4.11 非文字对比度 | 控件与图形 ≥3:1 | 输入框边界 `#8A94A6`（3.06）、焦点环 `#C7430F`（4.95）、图表填充 ≥3.06 | ✅ |
| **1.4.1 颜色不作为唯一手段** | 不得只用颜色传达信息 | 收支金额强制带 `+`/`-`；环形图图例含名称+金额+占比；不达标色不得单独承载语义 | ✅ |
| 1.4.4 文字缩放 | 200% 无内容丢失 | 320px × 200% 实测 7 页全通过（§7.4）；金额折行而非截断 | ✅ |
| 1.4.10 回流 | 320px 无横向滚动 | 320px / 375px 两档实测无横向溢出；`flex-wrap` + `min-width: 0` 兜底 | ✅ |
| 1.4.12 文字间距 | 行高可放大到 1.5 倍不截断 | 行高已显式声明且 ≥1.4 | ✅ |
| 2.4.7 焦点可见 | 键盘焦点可见 | `App.vue` 定义 `:focus-visible`（`outline` 版，不被 `overflow:hidden` 裁）；全项目无 `outline: none` | ✅ 规则就位（Tab 导航见 §7.3） |
| 2.5.8 目标尺寸 | ≥24×24 CSS px | 12 处控件补到 ≥36px；翻月/完成/日历翻页用满 `$touch-target-min: 44px` | ✅ |
| 1.4.13 悬停/聚焦内容 | 可关闭、可悬停 | 弹窗遮罩可点关闭 | ✅ |

> 「⚠️」项不是配色/字体能解决的，属于交互与布局范畴，已列入 §7 落地步骤。

---

## 5. 深色模式映射（预留）

H5 端可通过 `@media (prefers-color-scheme: dark)` 生效。以下为实测达标的映射（补全卡片层级后）：

| Token | 亮色 | 深色 | 深色下对比度 |
|---|---|---|---|
| `bg-page` | `#F5F6F8` | `#14161A` | — |
| `bg-card` | `#FFFFFF` | `#24272E` | 对页面底 1.21:1（层级可辨） |
| `divider` | `#EDEFF3` | `#31363E` | 1.23:1（装饰） |
| `border-input` | `#8A94A6` | `#6B7484` | **3.17:1** ✅ |
| `text-primary` | `#1F2329` | `#E8EAED` | 12.40:1 ✅ |
| `text-secondary` | `#5A6472` | `#B4BCC8` | 7.81:1 ✅ |
| `text-tertiary` | `#6E7787` | `#98A1AF` | 5.73:1 ✅ |
| `text-disabled` | `#A8B0BD` | `#5A6270` | 2.43:1（禁用态，豁免） |
| `brand-700`（品牌文字） | `#C7430F` | `#FF8A5B` | 6.43:1 ✅ |
| 收入 | `#0B8038` | `#3DD68C` | 7.97:1 ✅ |
| 支出 | `#D92D20` | `#FF6B6B` | 5.39:1 ✅ |

> **上表数值全部以 `bg-card` = `#24272E` 为基准。**
> ⚠️ 本表初版曾把两套不同底色（`#1E2126` 与 `#22252B`）的计算结果混在同一列里——
> 表还在，底色却不止一个，等于每个数字各说各话。已按 `#24272E` 全量重算。
> **这正是下面那句"必须提亮"的反面教材：换背景必须重算，写表格的人自己也会忘。**
> 复现命令：`node scripts/check-contrast.mjs`

**注意**：深色模式**不是把亮色反相**。亮色下"深色文字 on 浅底"，深色下必须**把品牌色和语义色提亮**（`#C7430F` → `#FF8A5B`），因为暗底上低明度色会糊成一团。直接复用亮色 token 是深色模式最常见的翻车点——实测数据：

| 亮色 token 直接放到 `#24272E` 上 | 实测 | 判定 |
|---|---|---|
| `brand-700` `#C7430F` | 3.02:1 | ❌ 需 4.5 |
| 收入 `#0B8038` | 2.96:1 | ❌ 需 4.5 |

> 小程序端深色模式支持度参差，建议**先只做 H5**，小程序端保持亮色。

---

## 6. 迁移映射表（旧 → 新）

按"替换即可，无需思考"的方式给出。**逐条 grep 替换，注意同一颜色可能对应不同语义**。
下表已在阶段 1–4 中全量执行完毕（共 219 处替换、15 个 `.vue` 文件），故同时充当"已落地的对照表"。

### 6.1 文字

| 旧色值 | 出现处 | 新 token | 备注 |
|---|---|---|---|
| `#333` / `#333333` | 正文 | `text-primary` `#1F2329` | 批量替换 |
| `#666` / `#555` | 次要 | `text-secondary` `#5A6472` | `#555` 是"同义不同值"的漏网灰 |
| `#999` | 辅助 | `text-tertiary` `#6E7787` | ⚠️ **对比度修复重点**；**压在页面底 / 浅底上的改用 `text-secondary`** |
| `#bbb` / `#ccc`（提示、占位、箭头） | 最弱文字 / 可交互箭头 | `text-tertiary` `#6E7787` | 箭头是"控件"（需 3:1），不能用禁用色 |
| `#bbb` / `#ccc` / `#c0c0c0`（禁用态） | 禁用 | `text-disabled` `#A8B0BD` | WCAG 豁免，仅用于真禁用或纯装饰 |
| `#ddd` | 装饰分隔符（如排行里的 `•`） | `text-disabled` `#A8B0BD` | 不承载信息，豁免 |
| `#fff`（作为文字） | 深底上的字 | `text-inverse` `#FFFFFF` | |

### 6.2 品牌橙（必须按场景分流，不能一把梭）

| 旧色值 | 场景 | 新 token | 备注 |
|---|---|---|---|
| `#ff6b35` | 实心按钮底、Tab 选中底、开关选中、FAB | `brand-600` `#CF4A12` | ⚠️ 白字 4.52:1，对比度修复 |
| `#ff6b35` | 橙色文字（链接、选中文字、Tab 选中文字、FAB 标签） | `brand-700` `#C7430F` | ⚠️ 白字 4.95:1 |
| `#ff6b35` | 选中描边（2px 圈） | `brand-600` `#CF4A12` | 控件边界需 ≥3:1；旧值 2.84 不达标 |
| `#ff6b35` | 3px 选中指示条（纯装饰） | `brand-500` `#FF6B35` | **保持不变**——品牌识别靠色相 |
| `#ff6b35` + `#fff1eb` | 浅橙底徽标（如"当前账本"） | `badge-brand-text` `#B33A0C` + `badge-brand-bg` `#FFF1EB` | 浅底场景必须换深字 |
| `#e55a28` | 按下态 | `brand-800` `#B33A0C` | |
| `rgba(255,107,53,α)` | 品牌色投影 / 光晕 | `shadow-brand-soft` / `shadow-brand-glow` | 阴影也要跟着品牌色改 |
| `#ffb98a` + 白字 | 日历「今天」 | `brand-100` `#FFE3D6` + `brand-800` `#B33A0C` | 旧组合只有 **1.67:1**，全项目最差 |

### 6.3 语义色与状态

| 旧色值 | 出现处 | 新 token | 备注 |
|---|---|---|---|
| `#52c41a` | 收入 | `success` / `income` `#0B8038` | ⚠️ 对比度修复（旧 2.27） |
| `#ff4d4f` | 支出、删除按钮、危险操作 | `danger` / `expense` `#D92D20` | ⚠️ 对比度修复（旧 3.27） |
| `#4a90d9` | 链接、可点操作 | `info` `#1D63B8` | ⚠️ 对比度修复（旧 3.34）；左滑按钮底色也用它 |
| `#c8c7cc`（uni-easyinput 的 `$uni-border-1`） | 输入框边界 | `border-input` `#8A94A6` | 旧值 ≈1.3:1，控件边界需 ≥3:1 |

### 6.4 背景、边界与图表

| 旧色值 | 出现处 | 新 token | 备注 |
|---|---|---|---|
| `#f5f6f8` / `#f8f8f8` | 页面根背景 | `bg-page` `#F5F6F8` | |
| `#fff`（作为面） | 卡片、列表项、弹窗、白色按钮 | `bg-card` `#FFFFFF` | |
| `#f7f8fa` / `#f5f5f5` / `#f2f3f5` / `#eee` / `#eceff3` | **"面"**：输入框底、图标底、侧栏、键盘托底、未选中 Tab 底 | `bg-subtle` `#EEF1F5` | 与下面一行靠"线 / 面"区分 |
| `#f5f5f5` / `#f0f0f0` / `#f7f7f8` / `#fafafa` | **"线"**：`1px` 分隔线 | `divider` `#EDEFF3` | 纯装饰，豁免对比度 |
| `#7ed321` / `#50e3c2` / `#ff4081` / `#bd10e0` / `#f5a623` / `#4a90d9` | 图表调色板、首页区间图标、排行进度条 | `$chart-series` / `constants/chart.ts` | 整体替换，**单一真源见 §2.7** |
| `#9b9b9b`（调色板第 7 位） | 「其他」 | `#8A94A6` | 与 `border-input` 同值，但语义不同 |

> **5 个"差不多的灰"的归一结果**：页面底 1 个（`bg-page`）+ 浅底 1 个（`bg-subtle`）+ 分隔线 1 个（`divider`）+ 白 1 个（`bg-card`）＝ 4 个。
> 归一的意义不在于"好看"，而在于**下次改色只需动一处**。

---

## 7. 落地进度与实现说明

### 7.1 阶段进度

| 阶段 | 动作 | 状态 |
|---|---|---|
| **1** | 新建 `frontend/src/styles/tokens.scss` | ✅ 已完成 |
| **2** | 全局接入 tokens（`App.vue` 的 `page` 字体/配色 + 打通 SCSS 注入链路） | ✅ 已完成 |
| **3** | 替换对比度❌色 + 灰色归一 + 品牌橙按场景分流 | ✅ 已完成（219 处 / 15 个 `.vue`） |
| **4** | `pages.json` 的 `tabBar.selectedColor` → `#C7430F`、`color` → `#6E7787` | ✅ 已完成 |
| **5** | 字号归一：11px → 12px（6 处）；13px → 14px（16 处）；17/18/22/26px 并入 16/20/24/28px（9 处）；补全 `line-height` | ✅ 已完成（114 处token 化，31 处渲染值变化） |
| **6** | 金额元素加 `tabular-nums` + 右对齐（`@include amount` + `min-width` 撑定宽） | ✅ 已完成（5 处金额列） |
| **7** | 环形图：相邻段留白分隔 + Top6 + 「其他」聚合 | ✅ 已完成（`MAX_SLICES = 6`；白描边改为「缩短弧长露出底环」，文档原写法实测无效） |
| **8** | 焦点环 `:focus-visible`；核对触控目标 ≥44px | ✅ 已完成（焦点环改 `outline` 版；12 处控件补足） |
| **9** | 复验：320px / 375px 视口 × 正常 / 200% 字号，7 页共 21 屏 | ✅ 已完成（0 屏不合规；发现并修复 4 类真问题，方法见 §7.4）。**2026-09-13 结构变化后为 8 页 24 屏**（明细下线、统计改为独立页「报表」），脚本 `scripts/reflow-audit.mjs` |

### 7.2 实现机制（为什么这样接，以及踩过的坑）

**① token 怎么进到各个组件的 `<style>` 里。**
`@dcloudio/vite-plugin-uni` 的 `createCss()` 会把 **`uni.scss` 的全文当作 `scss.additionalData` 前置到每一个 SCSS 块**。
所以做法是：在 `uni.scss` 顶部 `@use '@/styles/tokens.scss' as *;`，**任何组件都不需要再手动 import**。
前置条件：每个 `<style>` 块必须带 `lang="scss"`——本项目原先 16 个块全是纯 CSS，阶段 2 已全部补上（纯编译方式变化，不改布局）。

- 用 `@use ... as *` 而不是 `@import`：后者已被 Sass 弃用（Dart Sass 3.0 将移除），且构建时会为每个组件刷一条弃用警告。
- ⚠️ **改完 `uni.scss` 必须重启 dev server**：`additionalData` 是在**配置解析时读取一次**的，热更新不会重新读取。
  症状是"生产构建正常、dev 报 `Undefined variable`"，极易误判成代码写错。

**② `uni.scss` 里那套 `$uni-*` 变量被对齐到了设计 token。**
但实测发现一个限制：**uni-ui 各组件会在自己的样式块里重新声明 `$uni-*` 且不带 `!default`**，
那种情况下本项目的赋值会被组件覆盖 → 改 `uni.scss` 对它们无效。
唯一吃到值的是带 `!default` 的变量，例如 `uni-easyinput` 的 `$uni-border-1`——**登录页输入框的边框就是靠它修的**
（原值 `#dcdfe6` ≈1.3:1 不达标，现 `#8a94a6` = 3.06:1 ✅）。
这属于 uni-ui 的库设计限制，不是配置错误，别再花时间"修"。

**③ `$uni-font-size-lg: 16;` 缺单位。** uni-app 模板自带的这个笔误会让任何引用它的组件产出非法 CSS（`font-size: 16`）。已修。

**④ `opacity` 会吃掉对比度。** 首页 banner 与"我的"页头卡的说明文字用了 `opacity: 0.9 / 0.85`，
在深色渐变上等效对比度只有 **3.95:1 / 3.69:1**——**颜色达标不等于观感达标**。
本次已把这 3 处文字的透明度去掉（实心白字 = 4.52:1 ✅）。装饰性 emoji 与 11px 箭头保留透明度（前者是装饰，后者按非文字 3:1 判定仍合规）。

**⑤ TS 侧也要用色。** 图例圆点、首页区间图标底色、左滑按钮底色都是 JS 内联样式，SCSS 变量进不去。
图表色已抽到单一真源 `frontend/src/constants/chart.ts`，并由校验脚本比对与 `tokens.scss` 的一致性。

### 7.3 尚未落地（诚实清单）

- **【阶段 9 · 已知接受】环形图中心注解在大字号下与环带重叠。**
  几何上无解：内孔直径 = `size 180 − 2 × thickness 24` = **132px**，
  而 `¥13918.05` 在 ×2 字号（40px）下实测需要 **198px**。
  同一金额在上方汇总卡里已完整可读，此处属**重复标注、非唯一信息源**，故不按"内容丢失"处理。
  彻底解决属**设计决策**（大字号时把总额移出圆环 / 环内只留占比），留给下一轮。
- **`.vue` 里 78 处字号的渲染值没有变化**（仅换成 token）。只有 31 处真的变了，逐条列在 §8。
  换句话说：阶段 5 的收益主要是"以后改字号只需改一处"，视觉上的修正是次要的。
- **`tokens.scss` 里两条语义注释原先不准确**：`$font-display-lg`（36px）只写了"记一笔页金额输入"，
  实际首页 banner 的"总支出"也是它；`$font-display`（28px）写"首页资产总额"，实际用在登录页。
  已在本轮修正。**注释是给下一个人看的，不是装饰。**
- **Top6 聚合只在展示层**：后端 `categoryBreakdown` 仍返回全部一级分类，前端 `MAX_SLICES = 6` 截断。
  若将来要按「其他」下钻，需要在后端聚合。
- **键盘 Tab 导航未打通**：`:focus-visible` 规则已就位（阶段 8），但 uni-app H5 把 `<view>`
  渲染成不可聚焦的自定义元素，实际能用 Tab 走到的只有原生可聚焦元素（`input` / `button`）。
  完整键盘可达性需要给交互元素补 `tabindex`，属独立任务。
- `utils/icon.ts` 的 `THEME_COLOR` / `COLOR_EXPENSE` / `COLOR_INCOME` **全项目零引用**，
  值已同步为新配色并加了说明，但它们与 token 是重复定义，可考虑删除。
- `$border-input` 压 `$bg-subtle` 只有 2.70:1；当前没有"浅底上的输入框"这种组合，若将来出现需换更深的边界色。

> 阶段 1–4 是「不改任何布局、只改颜色值」的纯收益项；阶段 5–9 会动布局与交互，风险更高。
> 两者均已验证：11 处对比度问题全部修复，复验 0 屏不合规（2026-09-13 结构变化后为 8 页 24 屏）。

---

### 7.4 阶段 9 复验方法与结论

工具为临时 Playwright 脚本（未入库，方法记录在此以便重建）：`verify9.mjs` 逐页巡检、`shot-full.mjs` 全页截图。
**巡检 3 档视口**：320×640 正常字号 / 375×812 正常字号（回归对照） / 320×640 字号 ×2。

**核心产出：问题必须分三桶看。** 只测一种会漏掉另外两种，而漏掉的那两种恰好更严重。

| 桶 | 判据 | 用户看到什么 | 会不会顶大 `scrollWidth` |
|---|---|---|---|
| ① 顶破文档 | 无裁切祖先、越出视口 | 横向滚动条 | **会** |
| ② 被祖先裁掉 | 祖先 `overflow:hidden/clip`，子元素越出祖先内容盒 | **文字不在屏幕上** | **不会** |
| ③ 内容宽于自身盒子 | `scrollWidth > clientWidth` 且 `overflow:visible` | 与邻居重叠、互相压字 | **不会** |

**为什么必须分桶**：`body` 有全局 `overflow-x: hidden`，横向滚动条**永远不会出现**。
所以本项目的溢出**不表现为滚动条，而表现为静默丢字或压字**。
只盯 `scrollWidth` 会得到一份"全绿"报告，而统计页那三个金额明明压在一起、根本读不出来。

**两个会产出"漂亮全绿"的测量坑（都实际发生过）：**

1. **模拟字号 ×2 不能边读边写。** `getComputedStyle` 返回的是"此刻"样式：按文档顺序遍历，
   给 `html` 写内联 32px 后，轮到 `body` 再读已是 32 → 写 64，`uni-page` → 128……逐层**复利**。
   结果 `.banner-expense`（声明 36px）的内层 `span` 被写到 144px，量出 791px 这种数字，
   整批"越界"结论全是假的。**正确做法：先在未变异的 DOM 上把值全部读出，再统一写回。**
2. **登录要按 `.submit` 点，不能按文字"登录"点。** 页面顶部导航标题也叫"登录"，
   脚本会静默点空——七页全部截成登录页，却给出"零溢出"的结论。
   （同理：判断是否进了首页要按"页面特征元素"，不能按 URL —— uni-app H5 会把首页规范成 `#/`。）

**结论：8 页 × 3 档 = 24 屏，0 屏不合规**（横向溢出 / 被裁文字 / 压邻居三类均为 0；
2026-09-13 起页面清单为 home / mine / report / account-select / category-expense / category-income / account / record），
另 1 处记为**已知接受**（环形图中心注解，见 §7.3）。

**过程中发现并修复的 4 类真问题：**

1. `.summary-item` 等 flex 项默认 `min-width: auto` 拒绝收缩 → 顶破容器（文档宽 335 > 320）。
2. `.legend-ratio` / `.rank-no` 写死 `width` → 大字号下文字被卡在盒子里溢出。
3. `.banner-expense` 被 `.banner { overflow: hidden }` **静默裁掉半截金额**
   （`¥13,918.05` 只显示到 `¥13,918`），而文档宽只有 328 —— 只盯 `scrollWidth` 会以为"才超 8px，不要紧"。
4. **统计页三栏金额互相压字**：`.value` 字号在阶段 5 由 18px 归一到 20px 后，
   每栏需求从 89px 涨到 105px，而三等分栏宽只有 96px。**这是阶段 5 的归一动作自己引入的**，
   由阶段 9 的复验抓出来。修法是给 `.summary-item` 加 `flex-wrap`，放不下时纵向排开。

**回归对照**：375px 与 320px 正常字号下，7 页渲染与修复前一致 ——
本轮新增的 `flex-wrap` **只在"本来放不下"时生效，是 no-op**，这正是它能安全加进去的原因。

---

## 8. 变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-12 | **落地阶段 5–9（本轮，会动布局与交互的批次，逐条实测）**：<br>① **阶段 5 字号归一**——114 处字面量 `font-size` 全部 token 化（文字 91 处走 `$font-*`、图标 23 处走新建的 `$icon-*` 8 档阶梯）。**其中 31 处渲染值真的变了**：11→12（6 处）、13→14（16 处）、17→16（1 处）、18→20（4 处）、22→24（3 处）、26→28（1 处）；其余 78 处仅换 token 不动值。<br>② **阶段 6 等宽数字**——新增 `@include amount`（`tabular-nums` + 右对齐），5 处金额列加 `min-width` 撑出定宽。**只写 `text-align: right` 是无效的**：宽度由内容决定的盒子里没有"右"可供对齐。<br>③ **阶段 7 环形图**——`MAX_SLICES = 6`，超出部分聚合为「其他」（占比按"其余之和 ÷ 总额"重算，不是把百分比相加）；相邻扇区改为**缩短弧长露出白色底环**来实现分隔。**文档原给的 `stroke="#FFFFFF" stroke-width="2"` 实测无效**：白描边与彩色弧同心同宽，被完全盖住。<br>④ **阶段 8**——`$focus-ring` 改用 `outline` 而非 `box-shadow`（本项目卡片/弹窗几乎全是 `overflow:hidden`，阴影版会被裁成半圈）；`App.vue` 加 `:focus-visible`；12 处触控目标补到 ≥36px，翻月等高频控件用满 `$touch-target-min: 44px`。<br>⑤ **阶段 9 复验**——320×640 与 375×812 两档视口 × 正常 / 200% 字号，7 页共 **21 屏，0 屏不合规**。修复 4 类真问题，其中**统计页三栏金额互相压字是①自身的后果**（`.value` 18→20px 后每栏需求 89→105px，栏宽只有 96px），由本轮复验抓出。方法见新增的 §7.4。<br>⑥ **订正 §1.3 的旧数字**：原写"共 8 档、11px 出现 3 处"，实测为 **17 档 / 114 处、11px 出现 6 处**（整档漏掉了 8 个图标档位）。这是"计数没被校验器覆盖"的第二个实例。<br>⑦ 修正 `tokens.scss` 两条不准确的语义注释；订正 `记账应用全栈规划.md` §0 残留的 `ucharts` 表述。<br>校验：`check:contrast` **115/115**（98 → 115：新增第 13 节「字号阶梯自洽 + **计数自洽**」与第 14 节「焦点可见性」；计数断言做过负向验证——把期望值 114 改成 113 立即 MISMATCH、退出码 1）、ESLint 0、单测 98/98、`vue-tsc` 仅剩 2 处既有错误（`category/index.vue` 的 `deletedChildren`）。 |
| 2026-09-12 | **落地阶段 1–4**（本次）：方案首次作用于真实代码，**只改颜色值、不动任何布局**。<br>① 新建 `frontend/src/styles/tokens.scss`（单一真源）；`uni.scss` 顶部 `@use '@/styles/tokens.scss' as *;` + 对齐 `$uni-*` 变量，借 uni 插件的 `additionalData` 机制注入全部组件。<br>② 16 个 `<style>` 块由纯 CSS 改为 `lang="scss"`。<br>③ **219 处替换 / 15 个 `.vue`**：对比度❌色修正、5 个"差不多的灰"归一、品牌橙按场景分流（装饰留 `brand-500`，实心底 `brand-600`，橙字 `brand-700`）。<br>④ 新增 `frontend/src/constants/chart.ts` 作为图表色 TS 真源；`pages.json` tabBar 改 `#C7430F`/`#6E7787`。<br>⑤ `scripts/check-contrast.mjs` 扩至 **98 项断言**，新增 token 文件自洽、SCSS↔TS 跨文件一致、旧色值残留、§1.2 计数自洽四项回归防护。<br>⑥ 文档订正：§1 标注为"迁移前基线"并附 **11 处**修复对照；§1.3 字号标注"待阶段 5"；§6 升格为"已落地对照表"；§7 重写为进度表 + 实现机制 + 诚实清单。<br>⑦ 顺带修掉 uni 模板笔误 `$uni-font-size-lg: 16;`（缺单位）。<br>⑧ **订正"9 处不达标"这个老数字**——本节 §1.2 表里实际有 **11 行 ❌**，"9"从初版起就是错的，且校验器只断言对比度值、不管这个计数，所以一直没被抓到。现全部改为 11，并在 `check-contrast.mjs` 新增第 12 节自动比对"声明值 == 表内 ❌ 行数"，让它不再漂移。 |
| 2026-09-12 | 初版：配色方案、字体方案、WCAG 2.2 AA 核对、深色模式映射、迁移映射表 |
| 2026-09-12 | **复核订正**（新增 `scripts/check-contrast.mjs` 后首跑即抓出下列问题）：<br>① **§5 深色模式整表重算**——原表把两套不同底色的结果混在同一列（`13.39/8.43/6.19` 是对 `#1E2126` 算的，`3.26/1.26` 是对 `#22252B` 算的，而表头写的是 `#24272E`）。现全部以 `bg-card #24272E` 为准：`13.39→12.40`、`8.43→7.81`、`6.19→5.73`、`6.95→6.43`、`8.61→7.97`、`5.82→5.39`、`2.63→2.43`、`3.26→3.17`、`1.26→1.23`。<br>② **§2.7 推翻初版误判**——"青柠 `#7ED321` 与薄荷 `#50E3C2` 色盲下几乎同色"为**错误结论**（实测距离 103.9，属区分度较高的一对）。替换依据改为实测真凶：`#FF6B35`↔`#F5A623` **17.1**、`#F5A623`↔`#FF4081` **23.0**、`#FF6B35`↔`#FF4081` **28.1**、`#BD10E0`↔`#50E3C2` **28.6**；并补充 3 组灰度不可辨色对（ΔL 0.001 / 0.007 / 0.043）。<br>③ 附录 A 补入校验脚本用法，使"可复现"可执行 |

## 附录 A：测量方法（可复现）

所有对比度按 WCAG 2.2 相对亮度公式计算：

```
L = 0.2126·R + 0.7152·G + 0.0722·B      （R/G/B 为 sRGB 线性化分量）
Contrast = (L_light + 0.05) / (L_dark + 0.05)
```

**色盲可区分度**：采用 Viénot/Brettel 线性近似模拟绿色盲（deuteranopia）与红色盲（protanopia），在模拟后的线性 RGB 空间取欧氏距离（×255）。
判定阈值：**≥40** 视为可区分；**环形图相邻段要求 ≥70**（因为相邻段是唯一需要"当场分辨"的一对）。

**灰度可辨性**：用相对亮度差 ΔL 衡量，ΔL < 0.05 视为灰度下难以区分。

### 校验脚本（已入库，别再手算）

```bash
node scripts/check-contrast.mjs      # 全量校验，全部通过退出码 0；有 MISMATCH 退出码 1
```

`scripts/check-contrast.mjs` 是零依赖的 Node 脚本，把本文档发布的**每一个**对比度 / 色盲距离 /
ΔL 都写成断言。它存在的意义只有一个：

> **文档里写着"实测值"，就必须有人能一键复现。否则那句话只是修辞。**

**后续任何颜色改动，先改脚本身份表里的期望值、再改代码**：
脚本报 MISMATCH 时只有两种正确处理方式——改文档，或改色值；**不能放着不管**。
（本脚本首跑就抓出了本文档自身的 10 处错误，见 §8 变更记录。）

## 附录 B：本方案与文档其他部分的关系

- 本文档只覆盖**移动端视觉规范**，不涉及组件结构、交互流程、接口。
- 涉及的具体色值改动，应在 `记账应用全栈规划.md` §12 变更记录中追加一行。
- 若本方案与 `记账应用全栈规划.md` §7 前端章节的描述冲突，**以本文档为准并回头修正规划文档**。
