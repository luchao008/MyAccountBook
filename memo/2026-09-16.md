# 2026-09-16

## 核对摘要 vs 仓库实际状态（只读核对）

对 DeepSeek 分享摘要逐条核对，**发现 3 处与仓库不一致**：

| 项 | 摘要 | 实际 |
|---|---|---|
| 未 push commit 数 | 9 个 | **8 个**（`git rev-list --count origin/deepseek-web..HEAD`） |
| 其余账号数 | 120 个仍 88 | 实际 **42 个为 88**、41 个仅 2 个分类（测试残留），无 120 个 |
| reflow-audit 覆盖 | 未提 export | **export 页不在 PAGES 清单**（已补） |

## 补跑 reflow-audit（本次主要工作）

**动因**：MEMORY.md 挂了两笔待办 —— 首页排行改版（09-15）、流水类型/分类筛选（09-15）都"用户口径不跑 reflow-audit"，且 09-16 新增的 export 页从未纳入清单。

**改动**：`scripts/reflow-audit.mjs` 的 `PAGES` 补一行
```
['export', '#/pages/export/index'],
```
（原清单 9 页 → 10 页；export 是新增独立页，按铁律"页面清单随结构变化必须同步"补入）

**结果**：**30 屏（10 页 × 3 档）0 不合规**，三桶判据（顶破 / 被裁 / 压邻居）全 0。
- 顺带解除两笔旧待办的疑虑：首页排行图标放大到 36px 在 320×200% 下**未溢出**；流水筛选改动同样通过。
- 截图：`/tmp/reflow-audit`

## 文档订正（同步屏计数与"未跑"状态）

按铁律 #7（同一概念所有出现位置一致），改了三份文档：

| 文件 | 改动 |
|---|---|
| `.workbuddy/memory/MEMORY.md` | L17 脚本表 `21 屏 = 7 页` → `30 屏 = 10 页`；L60 高频词表恢复为映射 `21 屏`→`30 屏`；L300 追加 export 入清单说明；L353 `24 屏`→`30 屏`；L377/380/381 首页排行"未跑"→"已补跑、实测未溢出"；L394 流水筛选"没跑"→"09-16 已补跑" |
| `记账应用全栈规划.md` | L26 保留 24 屏历史值，追加"2026-09-16 补跑为 10 页 30 屏，0 屏不合规" |
| `docs/移动端配色与字体方案.md` | §7.4 结论处保留 24 屏历史快照，追加引用块说明 09-16 补跑为 30 屏 |

**口径（方案甲，用户拍板）**：历史快照（带 09-13 / 阶段 9 时间戳的 24 屏）**不改**，只在"当前状态"处追加新值。理由：24 屏是当时的真实事实，抹掉它等于篡改历史。

## 未做（留给后续）

- `npm run seed:all` 未跑 → 88 → 89 仍只对 demo 生效
- commit / push 未做（本次仅改脚本 + 文档，工作树当前有未提交改动）

---

## iOS 风格 UI 样板（本轮新增，状态：待用户确认）

**背景**：用户要求「先了解项目现状 → 设计一套贴近苹果 iOS 的设计样板（配色/字体/圆角/间距/图标/阴影/动效）→ 确认后再逐页面改造」。

### 现状体检结论（实测，非估计）

- 技术栈 uni-app + Vue 3 + SCSS；**15 页面 / 22 组件 / 31 功能图标**
- 现有语言 FL-1「数字优先·通栏扁平」：页面底=卡片=`#FFFFFF`，靠发丝线分区；
  全项目 `box-shadow` 仅 **6 处**（2 处还是 inset/transition），`backdrop-filter` **0 处**
- **图标是零成本项**：现有 31 个图标本来就是 SF Symbols 画法（24 栅格 / 1.75px 描边 / round / currentColor），不用重画
- **字号阶梯也是零成本项**：现有 36/28/20/17/16/15/14/12 与 iOS 的 34/28/20/17/17/15/13/12 高度重合

### 最重要的实测发现：iOS 系统色几乎全部不过 AA

| 系统色 | 压白卡 | |
|---|---|---|
| `#007AFF` 蓝 | 4.02 | ❌ |
| `#34C759` 绿 | 2.22 | ❌ |
| `#5856D6` 靛 | 5.65 | ✅ **唯一达标** |
| `#FF9500` 橙 | 2.20 | ❌ |
| `#30B0C7` 青 | 2.57 | ❌ |
| `#8A8A8E` 次级灰 | 3.44 | ❌（Apple 自己的次级文字色也不达标） |

→ **结论：形取 iOS，色值不照抄。** 保留原生值作图形填充，另配一套 AA 校准值作文字/按钮。

### 三条关键设计决策

1. **`#0060DF` 一个值打通"蓝字"与"白字按钮"** —— 洞察是**对比度是对称的**（白底蓝字 5.62 == 蓝底白字 5.62），
   所以不需要像原来品牌橙那样拆两档。**token 数减半，改造面随之减半。**
2. **收入红 `#D92D20` 压 iOS 灰底从 4.83 跌到 4.33** → 不改色值，改为硬性约束「**收支金额只能落白卡/白行**」。
   在 iOS 分组列表里这条天然成立（每行都是白载体），实现时是顺势不是负担。
3. **不做 iOS 26 Liquid Glass**：`backdrop-filter` 在小程序端支持度参差、低端 Android 掉帧，
   改用 iOS 17–18 式半透明 + 毛玻璃（含 `@supports` 降级）。

### 交付物

| 文件 | 内容 |
|---|---|
| Ardot 画布 `726321994372126` | 6 个 Board：概览原则 / 配色 / 字体度量图标 / 材质阴影动效 / 组件 / **3 个页面样稿**（首页·记一笔·报表，375×812 真实排版） |
| `docs/UI设计方案·iOS风格样板.md` | 完整规范 + 与 FL-1 的差异对照表 + 5 阶段落地路径 |
| `scripts/check-ios-tokens.mjs` | **99 项断言**，含度量"计数"断言与动效/阴影档位 |

### 校验器做了正反向验证

正向 99 项全绿退出码 0；故意把校准蓝改成 `#8AB4F8` → 报 5 处 FAIL、退出码 1；还原后恢复全绿。

### 过程中修正的三处真实错误

1. **黄色文字 `#8A6D00` 压灰底只有 4.41**（白卡 4.92 是达标的）→ 加深到 `#7E6300`（5.72 / 5.13 双底达标）
2. **`tokens.scss` 里 `$dark-expense: #3dd68c` 注释写 "7.97:1"**，实测压 `#1C1C1E` 是 **9.07**、压 `#14161A` 是 9.66，7.97 来源不明
3. 我最初把"暗底上的 iOS 原生绿 `#30D158`"（8.42）与"项目的 `#3DD68C`"（9.07）记混了 ——
   两个都是绿、都在深色模式里，**光看记忆里的数字分不清，必须回算**

### 画布制作上踩的坑（Ardot）

**`M()` 里写错字面 ID 会连锁污染后续所有 Insert 的父级** —— 我写 `M("3:441","3:455")` 时 3:455 其实是另一个节点，
移动失败后**该批次后续所有节点都挂到了错误的父节点下**（且只报一条 `parent not found`）。
更麻烦的是**修复父级后宽度链不会自动重算**：`fill_container` 的卡片会塌成窄条、文字两两重叠。
**修法：修复层级后必须再显式把整条容器链的 `width: fill_container` 重写一遍**，再截图确认。

### 未做（留给后续）

- **未改动任何业务代码** —— 用户确认为前提
- 分类图标在样稿里用色块占位；其余 12 个页面未逐一预演
- `check-contrast.mjs` 的 155 项仍是 FL-1 基线，**阶段 5 前不要跑**（会大面积 MISMATCH，属预期）

---

## iOS 样板 v1.1（同日修订，用户反馈「线条太深」）

**用户原话**：「你做的样稿我感觉线条太深了，给你一些图你参考一下」+ 11 张参考截图。

### 做法：不靠目测，像素级采样

用 Pillow 对参考图（1206×2622，3x 屏）做了三类采样（脚本在临时目录，方法见下方"可复用"）：

1. **高频色统计** → 得出页面底 `#F8F8F8`、卡片纯白、主色 `#56C4C5` / `#E4AD77`、文字 `#222226`
2. **整行浅色像素占比扫描** → 精确定位每一根横线：**全部是 `#F1F1F1`，厚 1px 物理（≈0.33pt）**
3. **按色相聚类 + 分段** → 定位强调元素：青绿金额线（2pt）、橙色 Tab 下划线（26×3）、彩色图标

### 结论：用户的判断完全正确，且可量化

| | v1.0 | 参考图实测 | 差 |
|---|---|---|---|
| 分隔线 | `#C6C6C8`（1.71:1） | **`#F1F1F1`（1.13:1）** | **0.58** |
| 页面底 | `#F2F2F7`（偏蓝紫） | `#F8F8F8`（中性灰） | 卡片存在感 0.120 → 0.060 |
| 卡片 | 有 1px 描边 | **零描边** | — |

**关键：行高 56、进度条 3px、零阴影这三项两版一致** —— 问题不在结构，在线条与底色的刻度。

### v1.1 变更

分隔线 `#F1F1F1` · 底色 `#F8F8F8` · 卡片去描边 · 圆角 10→16 · 文字 `#222226`/`#6B6B72`/`#9A9AA0`
主色 **`#A85F12` 暖橙金**（双轨：`#E4AD77` 仅图形 / `#A85F12` 文字与按钮）
+ **青绿三档**（`#56C4C5` 图形 / `#2E9496` 大字 3.63 / `#0E7375` 小字 5.63）
+ Tab 改文字 + 26×3 短下划线 · 按钮圆角 25 近药丸

### 两个值得记住的发现

1. **参考图的颜色本身不达标**：暖橙金 `#E4AD77` 配白字只有 **1.99:1**（比我的 iOS 蓝 4.02 还差一半）；
   青绿 `#56C4C5` 压白 **2.08:1**，连"大字 3:1"都不到 —— 参考图里那个青绿大金额也不合格。
   → 处理方式仍是**一色两值**（图形用原值、文字用校准值），且 `#A85F12` 的"白底金字 == 金底白字"
   对称性成立（都是 4.87），token 数没膨胀。
2. **底色变浅意外救回了收入红**：`#D92D20` 压 `#F2F2F7` 是 4.33（跌破），压 `#F8F8F8` 是 **4.55（达标）**。
   初版被迫加的"金额只落白卡"硬约束，余量从负变正 —— 但只有 0.05，约束仍建议保留。

### 校验

`check-ios-tokens.mjs` 重组为 **79 项**（v1.0 是 99 项，减掉重复的辅助色逐项断言，新增"线条深度差"）。
**负向验证通过**：把 `gold` 改回 `#E4AD77` → 7 处 FAIL、退出码 1；还原后全绿。

### 可复用的方法（值得沉淀）

**从参考截图提取设计规范**的采样三步法，本次一次成型：

1. **高频色统计**（`Counter` 降采样）→ 背景/卡片/主色/文字
2. **整行浅色像素占比扫描** → 找出所有横线及其精确色值与厚度
3. **按色相条件筛查 + y 分段** → 定位强调色元素（下划线、强调线、图标底色）

⚠️ 坑：`sed -i ''` 在 zsh 下会失败；改用 Python 做文件内替换更可靠。
⚠️ 坑：Ardot 里 `I()` 的 binding 名混入西里尔字母（`NV2rс`）会被静默跳过该行，用纯 ASCII 命名。
⚠️ 坑（本轮踩了两次）：**给已有容器改 `layout` 时必须同时重设 `width`/`height`** ——
否则尺寸 fallback 成 FIXED 并裁掉内容（实测卡片区被锁死在 100px，内容整块消失）。
**`M()` 之后子节点的 `fill_container` 也不会自动重算**，要沿容器链重写一遍宽度。
**跨 op 引用新节点必须用 binding 名**：写错字面 ID 会把节点移到别的父级下，且只报一条 `parent not found`。

### 未做

- 未改动任何业务代码（等确认）
- 分类图标在画布上用色块占位，落地时接 `color-icons.ts`（项目已有该资源）
---

## 一问一答确认的两个决策

用户明确要求：**任何疑问或选择都要一次问一个问题确认，给选项、也留"其他"的口子**。
已写入 `~/.workbuddy/MEMORY.md`（跨项目习惯）。

### 决策 1 · Hero 用浅金底 + 深金字

底 `#FDF6EF → #FBF0E4` 渐变 · 字 `#8F5312` · 两端 **5.74 / 5.47** 达标。

**过程中发现的两个坑（值得记住）**：

1. `#FDF6EF` 与页面底 `#F8F8F8` 的亮度差只有 **1.01** —— 纯浅金 Hero 会糊进背景。
   → 底必须略深（渐变暗端做到 1.06），让它靠**色相 + 微弱亮度**被看见。
2. 字不能用 `#A85F12`：它在渐变暗端只有 **4.33**（跌破）。改用更深的 `#8F5312`。
   → **通用教训：渐变上放文字，必须按最暗的那一端校验，不是亮端。** 亮端过 ≠ 暗端过。

### 决策 2 · 支出绿改青绿 `#0F7B7C`

| 位置 | 旧 | 新 | 对比度 |
|---|---|---|---|
| 列表/汇总金额 | `#0B8038` | **`#0F7B7C`** | 5.07（白卡）/ 4.77（灰底） |
| 支出徽标 | `#0b6b33` + `#e7f6ed` | **`#0F7B7C`** + **`#F2FAF9`** | 4.78 |
| 深色模式支出 | `#3DD68C`（正绿） | **`#3DD6C8`**（青绿） | 9.44（`#1C1C1E`）/ 7.73（`#2C2C2E`） |

⚠️ **坑**：不能拿 `#2E9496`（大号金额用的青绿）去当列表金额 —— 它压灰底只有 **3.41**，跌破。
**青绿比正绿更怕背景变深**，不同字号档位必须配不同的青绿值（图形 / 大字 / 小字三档）。

收入红 `#D92D20` 一个字没动。

脚本同步为 **92 项断言**，全绿。

---

## 补齐"最终版"（用户选定：补完再动手）

已写入文档 §10 / §11 / §12。四件事：

### 1. Hero 衔接已修（画布）

状态栏 + 导航 + Hero 合并进同一个渐变容器（原来三层底色各异，过渡生硬）。
这正是项目流水页 `.header` 已经在用的做法 —— **同一套模式复用，不新造**。

### 2. §10 逐页改造清单（15 页）

每页列出"通用改动 + 该页特有改动 + 风险点"。风险最高三页：**流水（1987 行，5 个弹层）**
> **报表（两级分类口径不能碰）** > **记一笔（数字键盘 + 九宫格）**。
建议顺序：首页 → 记一笔 → 报表 → 流水 → 其余 → 主容器收口。

### 3. §11 旧值 → 新值映射表

⚠️ **判断：`check-contrast.mjs` 的 155 项基线现在不能改。**
它守卫的是**代码的当前状态**，不是设计意图。提前改会得到最糟的状态 ——
**脚本一直红、代码一行没改**，于是没人再跑它，守卫作用归零。
正确顺序是：设计定稿 → 改代码 → 改脚本断言 → 跑通。
所以改为交付映射表，改造时照表逐行改。
（`check-ios-tokens.mjs` 无此问题：它守卫"设计规范"，与代码无关。）

⚠️ **纠正我上一轮的说法**：`reflow-audit.mjs` 的缺口**不是"新组件"** ——
它按 URL 扫 DOM，页面在清单里，新组件会被自动扫到。
**真实缺口是弹层等交互态扫不到**：数字键盘、流水页 5 个弹层、三列滚轮，脚本都不会去点开 ——
而这些恰是 v1.1 改动最密集的地方。要补需在巡检里加"先点击 → 再测量"。

### 4. color-icons：接入零成本，但有个既有性能问题（实测）

**接入是零改动**：`CategoryIcon.vue` 已按 icon 值自动分流
（彩色 key → `ColorIcon`、单色 key → `SvgIcon`、emoji → `SvgIcon` 兜底）。
用户挑过彩色图标的分类，列表里本来就会显示彩色。

**但实测（并纠正文档里"懒加载"的错误说法）**：
- `color-icons.ts` 源文件 **632 KB**，**gzip 后 212 KB**（216,637 字节）
- 图标 **470 个**（colorful 329 + life 141）
- `utils/colorIcon.ts` 是**静态导入**并在顶层遍历建 Map
- **15 个文件**引用它，**包含首页** → **首屏就在加载这 212 KB**

这是既有问题（非 v1.1 引入），但 v1.1 改造是顺手解决的好时机。
**已定方案：延迟加载**（动态 import 分包 + 索引未就绪时用 `SvgIcon` 单色兜底）。
⚠️ 这次改动要**打破一处刻意设计**：`isColorIconKey()` 现在是"查得到即彩色"，
懒加载后首屏无索引可查，必须改成格式判断（前缀白名单）。
改完必须专门复核"单色 key 与 emoji 不被误判成彩色"—— 那正是当年把三种情况
收敛进 `CategoryIcon` 想避免的事。

### 5. 图表序列重排（用户选定"整套按参考图重排"）

**参考图实测**：环形图**青绿主导**（`#56C4C5` 占 29609px），另有蓝 `#5F8EF4` /
紫 `#6F54F4` / 橙 `#F5862D`。
**但 4 个色里 2 个连 3:1 都不到**（青绿 2.08、橙 2.52）→ **只能借色相骨架，色值必须重配**。

**踩到的坑**：按参考图色相顺序直接排，闭环最小可区分度只有 **10~16** ——
因为这些色相**全挤在冷区**（青 185° / 蓝 213° / 紫 251°），色盲下几乎同色。
而原项目序列是**冷暖交替**排的，所以有 90.15。

**两个关键洞察（值得复用）**：
1. **闭环序列的"旋转"不改变相邻关系** —— 末位与首位相接，转一圈相邻对完全相同。
   所以把青绿从第 4 位转到首位，可区分度**一分不损**。
2. 但「其他」必须固定第 7 位（前端 `MAX_SLICES = 6` 截断），简单旋转会破坏它。
   → 固定「首 = 青绿、末 = 灰」，中间 5 个**全排列枚举**（120 种），最优解 **73.63**。

**最终序列**（只改 **1 个色值** + 重排）：

```
#0E7C86 青绿 / #7c3aed 紫 / #0e7c42 翠绿 / #c2185b 品红 / #1d63b8 蓝 / #c2410c 橙红 / #8a94a6 灰
```

- 闭环最小 **73.63**（阈值 70，**余量仅 3.63**）
- 前 6 色压白 **4.95–5.95**，都能当白字底
- 代价：90.15 → 73.63（重排的代价，记录在案）

⚠️ **不要再动青绿与灰** —— 它们是最弱的一对。
⚠️ 两个真源要同步：`constants/chart.ts` 与 `tokens.scss` 的 `$chart-series`。

**改动的实质**：青绿 `#0e7490`（193°）→ `#0E7C86`（185°，更贴参考图），其余 6 色值原样不动。

### 6. 深色模式品牌金（我推荐，用户采纳）

亮色金 `#A85F12` 压 `#1C1C1E` 只有 **3.49**（跌破）→ 必须单独配一个值。
选 **`#E4AD77`**（8.54 页面底 / 7.00 卡片），让品牌金在亮/暗下形成一对
（`#A85F12` ↔ `#E4AD77`），与 iOS 把 `#007AFF` 提到 `#0A84FF` 是同一个做法。

**一个有价值的对称**：`#E4AD77` 在**亮色**下只能做图形（压白卡 1.99），
在**深色**下却能当文字（压暗底 8.54）。
→ **同一个色值在两套主题里的"职责"不同**，别看到"亮色禁用"就以为它全局都不能用。

### 7. Logo 跟着主色改金（已定，待执行）

现状 `frontend/src/static/logo.svg` = 品牌橙 `#CF4A12` 方块 + 白 ¥（favicon 与登录页共用）。
**决定改成深金 `#A85F12`**（白 ¥ 压它 4.87，比原来的 4.52 还清楚）。

判据：这个 logo 的结构就是"品牌色方块 + ¥"，**它本身就是主色的载体**，
不像 Spotify 那类已独立成立的 logo —— 橙色图标配金色 UI 会像两个产品。

执行时机：与登录页改造一起（阶段 4），**三步都要做**：
改 `logo.svg` 色值 → 重跑生成脚本产出新 PNG → 登录页/favicon 自动跟随。
⚠️ 别只改 SVG 不重跑脚本，否则源文件是金色而页面显示旧橙 PNG。

至此所有待确认项已关闭。`check-ios-tokens.mjs` 增至 **108 项断言**，全绿。

---

## 界面全量翻新执行计划（交付给执行者的施工图）

用户要求：制定一份完整翻新计划，供其他 AI 软件据此执行。产出 `docs/界面全量翻新执行计划.md`。

### 结构（8 章）

| 章 | 内容 |
|---|---|
| §0 | 三条铁律：顺序不可颠倒 / 每阶段有退出条件 / 机械操作必须全局搜 + 回读 |
| §1 | 目标 G1–G5（全部可验收）+ 范围（做/不做）+ **19 个改造单元总账** |
| §2 | 风格方向：表面三层 / 文字阶 / 金三值 / 青绿三档 / 语义色 / 深色 / 度量 / 阴影 / 动效 / 毛玻璃 |
| §3 | 6 个阶段，每阶段含 任务 → 交付物 → **可执行退出条件** |
| §4 | 19 张任务卡（A01–A19，含风险标注）+ 共享组件归属 |
| §5 | 验收标准：门禁命令表 + 全局验收 + **反例清单** |
| §6 | 约束与红线：业务红线 / 工程化 6 条 / 6 个反复咬人的坑 / uni-app 静默陷阱 / 何时停下来问人 |
| §7–§8 | 风险登记册（R1–R8）+ 文件清单 / 命令速查 / 脚本现状快照 |

### 阶段划分

0 冻结基线 → 1 token 基础设施（只增不删）→ **2 三张样板屏（决策门）** → 3 列表页 8 单元 →
4 表单页 6 单元 → **5 全局收口（决策门）**。合计 **5 + 8 + 6 = 19 个单元**。

关键设计：**只有两个决策门**（阶段 2 判观感、阶段 5 判交付），中间全是机械替换 + 跑命令。
并明确写了「什么情况下必须停下来问人」（仅 4 种），其余一律按计划执行 —— 这是让另一个 AI 能独立执行的前提。

### 三处数据修正（核实后才发现的）

1. **规范文档 §10 标题写「15 个页面」，实测 `pages.json` 注册 16 个** ——
   漏掉 `account-new` / `account-category` / `account-import` 三个账本子页。
   已同时修正规范文档 §10（补 16/17/18 三行）与本计划的 A18 / A13 / A19。
   ⚠️ 这正是「同一概念的所有出现位置必须全局搜一遍」那条铁律的又一次实例。
2. **`reflow-audit.mjs` 实际是 13 条路由 × 3 档 = 39 屏**，不是记忆里的「30 屏 = 10 页 × 3」。
   而且仍缺 4 页（`login` / `category-new` / `icon-picker` / `recycle`）+ `MineView` + 全部弹层。
   记忆已修正。
3. **`check-contrast.mjs` 目前 155 项全绿**（实测，退出码 0）—— 它守卫的是「代码现状」。
   提前改成 v1.1 值会造成「脚本一直红、代码一行没改」→ 守卫作用归零。
   计划里明确写成**阶段 5 才改**，并给出正确的顺序：设计定稿 → 改代码 → 改断言 → 跑通。

### 记忆维护

MEMORY.md 又发现 4 处过时/矛盾，已修：
- `check-ios-tokens` 99 项 → **108 项**（两处）
- `reflow-audit` 30 屏 → **39 屏**
- §3.2 写「iOS 样板改主色为蓝 `#0060DF`」→ 已改成 v1.1 的金 `#A85F12`
- 最严重：一处写「基线更新已从阶段 5 提前到阶段 1」，另一处写「不在本次更新」—— **自相矛盾**，
  已统一为「阶段 5 才改」。
→ 教训：**记忆里的数字必须带「实测于何时」的标记**，否则会和后续决策打架。

### 交付物

- `docs/界面全量翻新执行计划.md`（新增，约 560 行）
- `docs/UI设计方案·iOS风格样板.md`（修正 §10 页面数与 reflow 覆盖缺口）
- 画布 `726388232008878`：P1 执行计划总览 / P2 19 个改造单元 + 验收门禁 + 红线

---

## iOS 翻新 · 阶段 0 基线快照

**分支**：`feat/ui-v1.1`（从 `deepseek-web` 切出，带 3 个未提交文档改动过去，已于本轮 commit）
**执行时间**：2026-09-16 14:0x–14:2x

### 实测基线

| 检查 | 命令 | 实测 | 计划/MEMORY 里写的 |
|---|---|---|---|
| 单测 | `npm run test` | **146 / 146**（6 suites） | ~~113~~ ❌ 过期 |
| ESLint | `npx eslint .` | 0 错误 | 0 ✅ |
| Prettier | `npm run format:check` | 通过 | ✅ |
| 前端类型 | `cd frontend && npx vue-tsc --noEmit` | 0 | 0 ✅ |
| iOS token | `node scripts/check-ios-tokens.mjs` | **108 项**全过 | 108 ✅ |
| 配色断言 | `node scripts/check-contrast.mjs` | **155 项**全过 | 155 ✅ |
| 重排巡检 | `node scripts/reflow-audit.mjs` | **39 屏（13 页 × 3 档）0 不合规** | 39 屏 ✅ |
| 回归 | `bash scripts/regression-test.sh` | **74 / 74**（修脚本后） | ~~52~~ ❌ 过期 |

### 阶段 0 抓到并修掉的一个真红灯（重要）

首次跑回归是 **PASS=63 / FAIL=9**，9 条同一根因：

> **账本级分类改造（09-16）后 `accountId` 成为分类接口必传参数，而回归脚本没跟上。**

| 失败项 | 表面返回 | 真因 |
|---|---|---|
| `valid token 200` / `list 200` / `filter by type 200` | 422 | `/api/categories` 缺 `accountId` |
| `dup name 409` / `update 200` | 422 / 404 | 同上 |
| `delete missing 404` | 400 | **默认账本分类禁删（D16）先于「不存在」判定** |
| `type mismatch 400` / `category filter 200` | 200 / 422 | 交易没带 `accountId`，落到默认账本 |
| `cross-user category 404` | 400 | 跨用户断言没带自己的 accountId |

**修法**（`scripts/regression-test.sh`，本阶段唯一改动的代码文件）：

1. 「token 有效性」探针 `/api/categories` → **`/api/accounts`**（前者现在必传 accountId，当探针本身就错）
2. `[category]` 段先建 **`copyAll:false` + 空 categoryIds 的临时非默认账本**，分类用例全在它上面跑
   （默认账本禁删分类，用它验不出「删不存在分类应 404」）
3. `[transaction]` 段的交易补 `accountId`，与所用 `categoryId` **同账本**
4. **新增 2 条不变量断言**：`missing accountId 422`、`category from other account 404`
5. 跨用户隔离断言补上「用自己的 accountId 去读别人的分类」

**结果：PASS=74 / FAIL=0**（72 → 74，净增 2 条）。

> ⚠️ `docs/账本级分类设计文档.md` §8.2 **早已预警过这件事**，列了
> `verify-category-page` / `verify-category-batch` / `verify-export` / `verify-flow`，
> **但漏了 `regression-test.sh` 自己**。
> 教训：那份清单是「我改过哪些脚本」的记忆，不是「谁调了这些接口」的搜索结果 ——
> **要靠 `grep accountId src/` 反查调用方，不能靠回忆。**

---

## iOS 翻新 · 阶段 1 基础设施（token）

**提交**：`feat/ui-v1.1` 上的第二个提交。**三个文件，零页面改动**（`git status` 只有 3 个 M）。

| 文件 | 改动 | 净增 |
|---|---|---|
| `frontend/src/styles/tokens.scss` | 新增 §10 v1.1 token（78 个变量）+ §11 三个 mixin | +163 / −10 |
| `frontend/src/constants/chart.ts` | `CHART_SERIES` 重排 | +22 / −10 |
| `frontend/src/pages.json` | 全局导航栏底色 `#FFFFFF` → `#F8F8F8` | +1 / −1 |

### 「只增不删」是硬约束，已核验

`git diff tokens.scss | grep '^-' | grep -cE '^-\$'` = **0** ——
**没有任何既有变量被删除或改动**。§1–§9 的 FL-1 token 原样保留，
新增的 `$v11-*` 与它们并存。所以每一页都能独立切到 v1.1 或留在 FL-1，**可逐页回滚**。
唯一的「删除」是 `$chart-series` 数组自身的 6 行旧色值 —— 那是**双写真源的同步**，不是 token 消失。

### 计划书里两处必须现场修正的地方

1. **`$v11-bg-card` 在计划书的变量表里漏了。** 但计划书 §1.2 给的 `@mixin ios-card`
   引用了它 —— 照着抄会直接 `Undefined variable` 编译失败。已补 `#FFFFFF`。
   （又一次「同一概念的所有出现位置必须全局搜一遍」：**变量表和 mixin 是两处，
   计划书只写对了一处**。）
2. **`$chart-series` 数组内不能写裸 `#rrggbb` 注释。**
   第一版我在数组里写了「替代原 `#0e7490`」，`check-contrast.mjs` §10 立刻报
   「双写不一致」，实测解析出 **8 个**色值。该脚本自己在注释里就写过这条教训
   （「解析范围写宽了」和「值不一致」报的是同一条错），这次是被自己的注释绊倒。
   → 改法：旧色值说明挪到**数组外**，数组内注释只留描述。

### 退出条件验收（全部通过）

| 检查 | 结果 |
|---|---|
| `npx vue-tsc --noEmit` | **0** ✅ |
| `npx eslint .` | **0** ✅ |
| `npm run format:check` | 通过 ✅ |
| `npm run test` | **146 / 146**（与阶段 0 一致，无回归）✅ |
| `node scripts/check-ios-tokens.mjs` | **108 项**全过 ✅ |
| `cd frontend && npm run build:h5` | **DONE Build complete** ✅（SCSS 编译通过，新 mixin 无语法错） |

**页面视觉无任何变化** —— 因为还没有任何页面引用 `$v11-*`（预期如此）。

### `check-contrast.mjs` 现在有 1 项预期的红

```
发现 1 处不一致（已通过 154 项）：
  · 可承载白字的实心序列最低值（首页区间图标依赖）
      实测 4.95，文档/预期 5.18
```

**这是预期的，不是改坏了。** 两个原因叠加：

1. 脚本守卫的是「代码当前状态」（FL-1 基线 155 项），而代码已开始向 v1.1 迁移；
2. 它算的是**去掉中性灰后的前 6 色**，而 v1.1 重排后青绿 `#0E7C86` 进了前 6 位
   （4.95），把最低值从 5.18 拉到 4.95。**4.95 仍然 ≥4.5，契约本身没破** ——
   破的是脚本里那个写死的旧期望值。

→ 按计划铁律，**基线只在阶段 5 改**。现在改会得到「脚本一直红、代码一行没改」，
守卫作用归零。阶段 5 一次性切断言 + 做负向验证。

### 顺带查到的两处**文档过期**（待阶段 5 统一订正）

| 项 | 文档写的 | 实测 |
|---|---|---|
| Swagger 覆盖 | 26 操作 / 17 路径 / 5 分组 / 61 模型 | **31 操作 / 22 路径 / 5 分组 / 65 模型** |
| 单测 / 回归用例数 | 113 / 52 | **146 / 74** |

> 又一轮「文档里的数字必须带『实测于何时』标记」的实例。这两处不阻塞改造，
> 记在此处，阶段 5 一次性收口时改（连同 `check-contrast` 基线）。

---

## 接手续做：阶段 2 收尾 + 阶段 3/4 全量落地

上一轮由另一个 AI 完成阶段 0/1 并起了阶段 2。本轮接手后先核实真实进度（**不看文档看代码**），
再补齐剩余工作。

### 核实结果：阶段 2 只做了一半

| 状态 | 单元 |
|---|---|
| 已完成 | A01 TabBar、A02 首页、A03 记一笔、AmountKeyboard、TrendChart |
| **未做** | **A04 报表、A05 主容器** —— 阶段 2 的决策门其实没闭合 |

判据是「谁真的引用了 `$v11-*`」：只有 4 个文件（tokens.scss + record + HomeView + AmountKeyboard + TabBar）。

### 本轮做完的

1. **阶段 2 收尾**：报表页新增 iOS Large Title、面板升白卡片、Tab 短下划线；
   主容器底色改灰。阶段 2 决策门至此闭合。
2. **阶段 3/4 全量换色**：27 个文件 / **374 处 token 迁移**（脚本做，每处断言唯一命中）+
   圆角 13 处 + 焦点环 + logo 三步改金（含新增可复现生成脚本 `gen-logo.mjs`，仓库原本没有）。
3. **验收全绿**：build:h5 DONE / vue-tsc 0 / eslint 0 / format ✓ / 单测 **146/146** /
   ios-tokens **108** / reflow-audit **39 屏 0 不合规**。

### ⚠️ 机械替换踩到的两处坑（都已在提交里修掉）

**坑一：只换背景没换前景 → 白字压浅金，等于消失。**
流水页渐变头由「深橙」改「浅金」后，nav 图标 / 标题 / 结余数字仍是 `$text-inverse`，
压在 `#FDF6EF` 上只有 **1.07:1**。这类错误不报错、不警告、脚本也抓不到，只有看图才发现。
→ 已全部改为 `$v11-hero-ink`（5.74 / 5.47 双端达标）。
**通用教训：换背景色时，前景色必须逐个显式确认，不能靠"看起来还行"。**

**坑二：FL-1 里页面底与卡片同白，同一个 token 两用。**
`$bg-canvas`（页面底）在 v1.1 要变灰，但它同时被用在**列表行 / 卡片**上 ——
一改名，这些"白载体"跟着变灰（账本列表、我的页用户卡、流水分组正文都中招）。
→ 已按「它是页面本身，还是浮在页面上的一块」逐一判回 `$v11-bg-card`。
**通用教训：改名映射表必须按"字面值"而不是"名字"来定 —— `$bg-page` 听着像页面底，字面值却是 #FFFFFF。**

### 顺带发现并记录

- **v1.1 调色板缺口（数学上无解）**：没有「能压白卡被看见 + 金字保持 4.5:1」的浅金表面。
  `$v11-gold-soft` 压白卡 1.07（等于不存在），加深到能看见则金字跌到 4.06 以下。
  → 浅金底只能作氛围，**必须配一条金色细边**才成为「可点表面」。已写进 `tokens.scss §10`，
  涉及 4 处用法（分类选中、日期「今天」、回收站「恢复」）。
- **`CHART_SERIES[6]` 魔法下标** → 收敛为 `CHART_OTHER_COLOR`（与 TrendChart 同一类隐患）。
- **流水页 `$text-inverse × 6`** 全项目扫描后确认：其余在 `$v11-gold` 实心底上，是正确的。
- **`ICON_FRAME_THEME` 是死代码**且引用了不存在的 `$ink-1/$ink-4` → 标注待阶段 5 删除。
- **计划书里 A17「新增 iOS 开关」是错的**：数据导出页没有 Tab、没有开关，
  参考图那几行（账户/成员/商家/项目）本项目没有数据模型，代码已明确「不做假控件」。
  已订正计划书 §3.4 与 §4 的 A17。

### 导航栏标题的规则（本轮实测确认，已写进计划书 §2.2）

一级目的地（能从底栏直达）用 **Large Title**（报表）；深层 push 页用**紧凑居中标题**（其余）；
首页 / 流水用**渐变 Hero** 顶替标题行。iOS 本身就这么分，别为了"统一"全改大标题。
⚠️ 大标题用 `$font-display`（28/36），不要写 30/38 —— 字阶是固定 8 档，阶梯外会被校验判红。

### 阶段 5 仍未做（本轮的剩余清单）

1. `check-contrast.mjs` **基线未切换**（现有 1 项预期红：青绿进前 6 位，最低值 5.18→4.95，契约未破）
2. `reflow-audit.mjs` 覆盖缺口：4 页（`login` / `category-new` / `icon-picker` / `recycle`）
   + `MineView`（URL 不可寻址）+ **全部弹层交互态**
3. `color-icons.ts` 延迟加载（首屏 212 KB）
4. 深色模式（H5）
5. 清理废弃 token（`$divider` / `$border-card` / `$brand-*` 等）+ `ICON_FRAME_THEME` 死代码
6. 文档状态同步（`移动端配色与字体方案.md` / `扁平化.md` 标注已被取代）




---

## 阶段 5 收口（待办 1 / 2 / 3 / 5 / 6 完成）

> ⚠️ **顺序上先做了 5（清 token）再做 1（切基线）** —— 因为 `check-contrast` 的 §9/§14/§15.4
> 断言的就是那些旧 token，先切基线会立刻全红。计划书里列的顺序是 1→5，实际依赖是反的。

### 待办 5 · 清理 FL-1 旧 token + 死代码
- `tokens.scss`：删 42 个「被 v1.1 取代 **且** 全项目零引用」的变量
  （品牌橙阶 / 表面三层 / 文字阶 / `$income`系 / `$dark-*` / `$focus-ring*` / `$chart-stroke*` /
  未使用的收支徽标对）+ mixin `focus-visible-ring`。
- **有意保留**：`$font-*`/`$lh-*`/`$icon-*`/`$weight-*`（是"允许集"的定义，删了等于把守卫一起删掉）、
  `$space-*`/`$radius-sm|md|pill`、`$chart-series`（TS 真源的镜像，由 §10 跨文件比对）、
  `$v11-dark-*`（深色模式预留）。
- 补了一个**真缺口**：v1.1 竟然没有收入色 token —— 支出早已迁到 `$v11-teal-amount`，
  收入还在用 FL-1 的 `$income`（迁移漏网）。新增 `$v11-income-amount: #D92D20`，迁移 5 处代码 + 1 处注释。
- 删死代码 `ICON_FRAME_THEME`（`icons.ts`，它引用的 `$ink-1`/`$ink-4` 早已不存在）。
- 顺手修一处**文档错误**：`tokens.scss` 原写「相邻扇区 2px 白色描边」——
  实测环形图的白缝是**底环 `CHART_TRACK` 露出来的**，不是给扇区加描边。
- 踩到并修掉：`App.vue` 是**迁移到一半**的状态（`outline` 用了 v1.1、`outline-offset` 还是旧变量）
  → 编译直接 Undefined variable。已加断言防复发（见待办 1）。

### 待办 1 · check-contrast 基线切到 v1.1
- §9 自洽表（值 / 注释 / 实测 三重比对）33 条 FL-1 → **28 条 v1.1**（含 7 条深色）。
- §10 实心序列最低值 5.18 → **4.95**（青绿进前 6 位；契约 ≥4.5 未破，余量 0.45）。
- §13 计数 219→218 / 217→216。**先查明原因再改数字**：`record` 页删掉了死样式 `.picker`
  （template 里已无该元素），不是漏改。
- §14 焦点环断言改 `$v11-focus-ring` ← `$v11-gold`；**新增**「App.vue 无半迁移」断言。
- §15.4 存在性清单换 v1.1；§15 / §6 / 文件头标注为 **FL-1 历史取证**（不删 ——
  "当初为什么那样选"比结论更难重建）；§1–§8 保留（只用字面量计算，不读文件）。
- **负向验证**：改错 `$v11-line` 色值 / 改错焦点环色 / 把 App.vue 退回半迁移 → 三项都报错且退出码 1。
  ⚠️ 过程中抓到**自己的一个 bug**：正则 `\$v11-gold` 会匹配 `$v11-gold-fill` 的前缀 → 假通过。
  已加 `(?![\w-])` 边界，并把这条教训写进脚本注释。
- 结果：**156 项全过**（155 + 新增 1）。

### 待办 2 · reflow-audit 补覆盖
- 13 页 → **24 条**（18 页 +「我的」页内视图 + 流水 6 个弹层），39 屏 → **72 屏**。
- 新增机制：每条可带 `steps`（先点开）+ `guard`（存在性断言）。
  **守卫是关键** —— 没有它，写错的选择器 / 失效的地址会静默量到别的屏、然后报绿。
  首轮实测就抓到 `.nav-btn` 第 1 个是**返回键**（点下去跳回首页），守卫立刻抛错。
  `steps` 必须在字号放大**之前**执行（放大只作用于"当前已存在的元素"）。
- 补覆盖**立刻抓到 2 个真实缺陷**：
  ① `category-new` 在 320px + 200% 字号下「一级分类（顶层分组）」溢出一行；
  ② 排序弹层里没有「排序」二字（只有排序选项）→ 守卫文本必须取自**实际渲染出来的**文案。
- 末尾新增**人工验收清单**（滚轮滚动后 / 日历展开 / 输入提交态 / 键盘 Tab / safe-area）。

#### 修 ① 时挖到的通用规律（已提炼进 MEMORY.md 第 10 条）
`一级分类（顶层分组）` 在 200% 字号下内容 300px 挤进 288px 盒子，`word-break: break-all`
与 `overflow-wrap: anywhere` **都无效** —— 中文禁则不允许在「（」前断行，整句成了不可断的整体，
浏览器在"溢出不足一个字"时选择不换行、直接吃掉右边距。
但把盒子压到 200px 时它**会**换行（说明断行能力本身没问题）。
→ **修法是拆句**（值 + 说明分成两个元素 + 容器 `flex-wrap: wrap` 兜底），不是调 `word-break`。

### 待办 3 · color-icons 延迟加载
- **632 KB / gzip 212 KB 从静态 import 改成动态 import 分包**。实测已是独立 chunk、
  `index.html` **无预加载** → 首屏不再下载。
- 生成脚本现在多产 `constants/color-icon-meta.ts`（**1.1 KB，同步可用**）：
  `isColorIconKey()` 需要同步答案，而索引要等分包。
- `isColorIconKey()` 从「查得到即彩色」改成**按形态判定**（`<集合>:<名字>`），
  由 `CategoryIcon.vue` 在数据到位后**复核**（取不到 → 退回单色渲染，避免陈旧 key 变空白）。
- ⚠️ **必须显式依赖 `colorIconsReady`**：索引是普通 Map，不依赖它则分包到了图标仍空白且不报错。
- `icon-picker` 的 Tab 改为**从元数据派生**（原来手写 3 个；加了新集合不会报错，只会少一个入口）。
- 验证：`verify-color-icon` **5/5**、`verify-icon-picker` **10/10**（含"多彩 Tab 有图标"）。

### 待办 6 · 文档状态同步
- 三份文档加「已被 v1.1 取代」横幅 + **哪些仍有效**：
  配色方案（§1.2/§1.3 计数、§3.2 字号、§7.3 焦点）、扁平化（§5 图标、§6.1 间距、§6.2 字号、
  §7.4 三桶判据）、主文档（TL;DR 顶部加 v1.1 状态块）。
- ⚠️ 改这两份设计文档时**不能动 §1.2 / §1.3 / §3.2 的结构** —— `check-contrast` §12/§13 在读它们。
- 顺手改掉主文档 TL;DR 的过时数字（单测 113→146、回归 52→74、30 屏→72 屏、155 项→156 项）。

### 记忆整理：这次是**拆结构**，不是再压措辞
- **MEMORY.md 31 KB → 10.4 KB（-66%）**。
- 做法：把明细（实现约定 / 颜色字体 / 重排 / 溢出判据 / uni-app 陷阱 / 页面约定 / 账本级 /
  启动 / 未决）移到新建的 **`docs/工程约定与踩坑.md`**，MEMORY.md 只留
  **索引 + 11 条铁律级经验 + 当前状态**。
- 前两次压缩只改措辞、不挪内容，所以每次都重新长回去 —— **这是根因**。

### 本轮新发现（都是既有问题，非改造引入）
1. **校验脚本会静默失效**：`verify-color-icon` / `verify-icon-picker` 用的 `.group-header`
   早已改名成 `.row.root-row` → 超时失败 → 之后没人再跑它（守卫事实上已经死了）。
2. **`verify-icon-picker` 的清理逻辑把测试分类留在库里**：调分类接口**漏传 `accountId`**
   （账本级改造后必填）→ 40000 → 报"没找到刚创建的分类"（看着像保存失败）→
   每次跑都在**默认账本**里留一条（而默认账本分类禁删，只能进 DB 清）。
   已修成"大声报出真实 HTTP 错误"，但**根治要用临时非默认账本**（像 `verify-category-batch`
   那样）—— **未做**。本轮已手动清掉自己产生的 2 条残留（分类数复核 = 89 ✓）。
3. **`verify-category-delete.mjs` 同样过期**（`.group-header` + `.group-name`/`.group-count`/`.action`），
   未纳入本轮范围。
4. **彩色图标验证脚本原本依赖数据**：它去分类页找彩色图标，而 demo 的 89 个分类**全是 emoji**
   → 永远报 `found:false`（看起来像组件坏了）。已改到 `icon-picker` 页，与数据无关。

### 遗留（明确未做）
- **深色模式接入**（token 已就位，`$v11-dark-*` 全部有断言锁值）。
- `verify-category-delete.mjs` 的选择器过期。
- `verify-icon-picker.mjs` 改用临时非默认账本（否则每次跑都要手动清库）。
