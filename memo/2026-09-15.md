# 2026-09-15

## 首页「本月各分类支出排行」按参考图调整（已完成）

用户给了两张图（图1 = 参考设计，图2 = 当前实现），要求把**记账页**（HomeView.vue）的
「本月各分类支出排行」改成图1 的样子。改动仅限该文件，**未动业务逻辑与接口**。

### 改动清单（frontend/src/components/views/HomeView.vue，+64 / −15）

| # | 项 | 改前 | 改后 |
|---|---|---|---|
| 1 | 表头布局 | 标题一行、「记账笔数 / 总支出」另起一行 | **同一行两端对齐**（`display:flex` + `space-between` + `align-items:baseline`），放不下时 `flex-wrap` 自动换行 |
| 2 | 「总支出」数值 | 与标签同为灰字 | 单独包 `<text class="summary-val">`，用**支出绿 `$expense`**（白卡 5.05:1 ✅）+ `$weight-medium` |
| 3 | 分类图标 | `CategoryIcon :size="16"` | **`:size="36"`**（图1 的大图标） |
| 4 | 分类名 | `$font-body-sm`（14px）/ 与图标 `gap: 4px` | **`$font-body`（15px）** / `gap: 8px` |
| 5 | 行距 | `padding: 10px 0` | **`padding: 12px 0`** |
| 6 | 进度条 | `height: 4px` / `border-radius: 2px` / `margin-top: 8px` | **`height: 6px`** / `$radius-pill` / `margin-top: 10px` |
| 7 | 折叠 | 直接渲染 `rank.slice(0, 10)`，**无展开按钮** | **默认 5 条 + 「点击展开 ⌄ / 收起」**；数据保留全量（`ranking.value = rank`），切换时重置为折叠态 |

### 折叠的实现

```ts
/** 折叠时展示的条数（与参考图一致：默认 5 条 + 「点击展开」） */
const COLLAPSED_COUNT = 5;
const expanded = ref(false);

/** 折叠态：默认只显示前 COLLAPSED_COUNT 条；展开后显示全部 */
const visibleRanking = computed(() =>
  expanded.value ? ranking.value : ranking.value.slice(0, COLLAPSED_COUNT)
);
```

模板里 `v-for` 由 `ranking` 改为 `visibleRanking`，列表末尾加：

```html
<view v-if="ranking.length > COLLAPSED_COUNT" class="rank-toggle" @click="expanded = !expanded">
  <text class="rank-toggle-text">{{ expanded ? '收起' : '点击展开' }}</text>
  <SvgIcon :name="expanded ? 'icon-chevron-up' : 'icon-chevron-down'" :size="12" />
</view>
```

与 RankList.vue（报表页用）的 `.toggle` 是**同一套做法**，交互一致。

### 决策记录（用户逐个拍板）

| 问题 | 选择 |
|---|---|
| 排行列表加不加「点击展开」 | **A. 完全照图1**（默认 5 条 + 展开） |
| 展开后显示多少条 | **A. 全部展示**（本月一级分类最多 13 + 「未分类」，页面不会失控） |
| 「总支出」数值用什么颜色 | **A. 保持项目规范**：支出绿 `#0B8038`（图1 里是蓝的，但项目 2026-09-14 已定“收入红 / 支出绿”，不为此破例） |
| 进度条色序要不要照图1 | **A. 保持现状**（沿用 `SOLID_SERIES`，与环形图同源 —— 同一分类在首页与报表页颜色一致） |
| 验证跑到什么程度 | **B. 精简**（`vue-tsc` + `check:contrast`，不跑 `reflow-audit`） |
| 计数不一致怎么处理 | **A. 按项目惯例更新期望值** |
| 要不要补跑 reflow-audit | **B. 不跑** |
| 要不要补文档 | **A. 全套补**（memo + 规划文档 §12 + MEMORY.md） |

### 顺带修：CRLF 行尾

HomeView.vue 整份文件是 **CRLF** 行尾。MEMORY.md 里记过这个坑
（CRLF 会让 `curl -w` 返回值带 `\r`，`[ "$2" = "$3" ]` 判等失败 → 6 个回归用例假失败）。
本次用 `perl -i -pe 's/\r\n/\n/g'` 归一为 LF，否则 `edit` 工具匹配不上（**就是这一步卡住了两次**）。

> 教训：**`edit` 的 old_string 匹配不上时，先 `od -c` 看一眼行尾符**，别反复重试。

### 计数断言 +1（第 15 次触发）

新增的 `.rank-toggle-text` 带了 1 条 `font-size: $font-body-sm`，于是
`check:contrast` §13④ 报 MISMATCH：**178 → 179、176 → 177**。
按项目惯例（只能“改文档”或“改色值”）更新了三处：

- `scripts/check-contrast.mjs`：期望值改 179/177，追加「2026-09-15 更新十五」注释
- `docs/移动端配色与字体方案.md` §1.3：追加**订正（十五）**
- `.workbuddy/memory/MEMORY.md` 复核清单：`178 / 176 / 2 / 0` → **`179 / 177 / 2 / 0`**

### 验证（全绿）

```
npx vue-tsc --noEmit        → 0 错误
npm run check:contrast      → 155 项全部通过
```

未跑：`reflow-audit.mjs`（320px×200% 三桶判据）—— 用户明确选择不跑。
本次动了行内排版（图标 16→36px、表头改横向布局），
**理论上存在大字号下挤占的风险，留待下次真机/浏览器验证**。

### 涉及文件

| 文件 | 变更 |
|---|---|
| `frontend/src/components/views/HomeView.vue` | 布局 + 折叠交互（+64 / −15），CRLF → LF |
| `scripts/check-contrast.mjs` | §13④ 期望值 178/176 → 179/177 + 注释 |
| `docs/移动端配色与字体方案.md` | §1.3 追加订正（十五） |
| `.workbuddy/memory/MEMORY.md` | 复核清单同步 179 / 177 / 2 / 0 |

---

## 流水页新增「流水类型」筛选（已完成）

需求：流水页的筛选里加一个「流水类型」（支出 / 收入），参照用户给的参考图
（底部弹层、多选列表、每行右侧橙色圆形勾选框、标题栏「取消全选」、底部「确定」）。

### 关键前提：后端早就支持了

TransactionService.applyFilters() 里本来就有 if (q.type) qb.andWhere('t.type = :type')，
DTO 也有 type?: 'income' | 'expense' —— **本次只改前端，后端一行没动**。

### 改动

**新增** frontend/src/components/FlowTypePicker.vue
- 底部弹层，**z-index: 1100**（叠在筛选面板的 1000 之上，关掉它直接回到筛选面板、无闪烁）
- 顶栏：✕ | 「选择流水类型」| 「取消全选 / 全选」（右侧品牌橙文字，随状态切换）
- 列表：options 数组驱动，当前 2 项（支出 / 收入）；右侧 20px 圆形勾选框，
  选中填 $brand-600 + 白勾（**形状与底色同时变，不只变颜色** —— WCAG 1.4.1）
- 底部：品牌橙实心「确定」
- 选项写成数组而不是写死两行，**将来后端 ENUM 扩展时只改这一处**

**改** frontend/src/components/FlowFilterPanel.vue
- FlowFilter 加 types: string[]
- 「时间」下面加一行「类型」（icon-filter + 标签 + 右侧 typeLabel + 箭头），点它 emit('pick-type')
- 新增 typeLabel computed：**全选 / 全不选都显示「全部」**，只有恰好选中 1 种才显示「支出」或「收入」
- reset() 一并把 types 复位为全选

**改** frontend/src/pages/flow/index.vue
- filterModel 加 types（默认全选）
- filterOnlyParams()：**只在恰好选中 1 种时才传 type**，否则 undefined
- hasFilter：(types || []).length === 1 才算「已筛选」
- filterSummary：加 type 字段，同样只在恰好选中 1 种时才有值
- viewAll()：类型复位为全选
- 新增 typeOpen 状态 + onTypeApply() 处理器
- 筛选条件摘要弹层加「流水类型」行（icon-filter）

### 决策记录（用户逐个拍板）

| 问题 | 选择 |
|---|---|
| 交互形态 | **照参考图**：底部弹层 + 多选列表 + 圆形勾选框 + 取消全选 + 确定 |
| 列表放哪些类型 | **C. 放 2 项并预留扩展**（参考图的转账/借入/借出…在本项目没有对应字段，**不放假选项**） |
| 入口位置 | **A. 加进筛选面板**（与「金额/备注」同属筛选条件，语义最一致） |
| 全选/全不选语义 | **A. 都视为「不过滤」** —— 全不选若真返回空集，用户会以为「账本没数据」，实际是筛掉了自己 |
| 弹层怎么叠 | **B. 类型弹层叠在筛选面板之上**（z-index 1100），关掉直接回到面板、无闪烁 |
| 是否计入「已筛选」提示 | **A. 计入，但仅当恰好选中 1 种时**显示在摘要里；全选不算筛选 |
| 「重置」是否清类型 | **A. 一并复位为全选**（与其它条件一致，「回到什么都没筛」） |

### 一个实现取舍：「确定」直接生效，不回到面板再点一次

类型弹层是**独立弹层**，它的「确定」直接写 filterModel.types 并 reloadAll()，
**不经过筛选面板的 draft** —— 用户在这一层点「确定」的预期就是立即生效（参考图也是这个交互），
若还要回面板再点一次，用户会以为点了没用。筛选面板下次打开会从 props.model 重新同步（watch visible），
所以不会残留旧值。

### 踩坑：edit 静默失败 + 漏改

FlowFilterPanel.vue 的 typeLabel computed 与 reset 里的类型复位**第一次没落进去**
（edit 那批只报了一处成功），vue-tsc 报 TS2339: Property 'typeLabel' does not exist 才暴露。
→ **教训：连续 edit 同一文件后要回读确认**（MEMORY 第 9 条本来就写着，这次又验证了一次）。

另：FlowFilterPanel.vue 与 flow/index.vue 也都是 **CRLF 行尾**，改前先归一为 LF，
否则 edit 匹配不上（这个坑一天内踩了第三次）。

### 计数断言 +4（第 16 次触发）

新增的 FlowTypePicker.vue 带 4 条 font-size（标题 / 取消全选 / 行标签 / 确定按钮），
于是 check:contrast §13④ 报 MISMATCH：**179 → 183、177 → 181**。按惯例更新三处：
scripts/check-contrast.mjs（追加「更新十六」注释）、
docs/移动端配色与字体方案.md §1.3（追加**订正（十六）**）、
.workbuddy/memory/MEMORY.md 复核清单（179 / 177 → **183 / 181**）。

### 验证（全绿）

```
npx vue-tsc --noEmit      → 0 错误
npm run check:contrast    → 155 项全部通过
```

未跑：reflow-audit（用户口径同上一轮，不跑）；build:h5 未跑。

### 涉及文件

| 文件 | 变更 |
|---|---|
| frontend/src/components/FlowTypePicker.vue | **新增**（弹层组件，约 170 行） |
| frontend/src/components/FlowFilterPanel.vue | +40：类型行 + types 字段 + typeLabel + pick-type 事件 + reset 复位；CRLF → LF |
| frontend/src/pages/flow/index.vue | +56：types 状态 + 传参 + hasFilter/filterSummary/viewAll/摘要行 + 弹层挂载；CRLF → LF |
| scripts/check-contrast.mjs | §13④ 期望值 179/177 → 183/181 + 注释 |
| docs/移动端配色与字体方案.md | §1.3 追加订正（十六） |
| .workbuddy/memory/MEMORY.md | 复核清单同步 183 / 181 / 2 / 0 |

---

## 修 bug：类型弹层选完回面板点「确定」被旧值覆盖（已完成）

**用户报告**：筛选弹窗确定后，选的流水类型失效、变回全选。

### 根因

`FlowFilterPanel.vue` 只用 `watch(() => props.visible)` 同步草稿，
而**类型弹层是叠在筛选面板之上的**（z-index 1100 > 1000）——
筛选面板的 `visible` 全程为 `true`，那个 watch **根本不会触发**，
`draft.types` 始终停留在打开面板那一刻的旧值（全选）。
于是「类型弹层选支出（已生效）→ 回面板 → 点确定」时，
`emit('apply', { ...draft })` 把旧的全选值**覆盖**回去。

### 修法

把 `types` 也纳入**受控同步**（与已有的「时间三件套」同一套做法）：

```ts
watch(
  () => [props.model.timeLabel, props.model.start, props.model.end, props.model.types],
  () => {
    draft.timeLabel = props.model.timeLabel;
    draft.start = props.model.start;
    draft.end = props.model.end;
    draft.types = [...(props.model.types || [])];
  },
  { deep: true }
);
```

并在注释里写明：**将来再加叠层子弹层时，把它的字段也加到这里，别指望 watch(visible)**。

### 新增验证脚本 scripts/verify-type-filter.mjs（11 项）

精确复现用户报的场景：打开面板 → 点类型 → 只选「支出」→ 弹层确定 →
**回面板再点确定** → 检查类型是否还在。

**负向验证**（项目铁律第 10 条）：临时把 `types` 从受控同步里拿掉，脚本报 **FAIL=6**
（不是静默通过），恢复后回到 **PASS=11**。

### 写脚本时暴露的第二个问题：脚本自己会卡死

第一次跑负向验证时**超时被 SIGTERM**：bug 回退后第 [4] 步判 FAIL，
脚本继续跑到第 [5] 步的 `.filter-tip-action`（此时不存在）→ Playwright 默认等 30s → 卡死，
**连统计都打不出来**。

修法两处：① `page.setDefaultTimeout(6000)`；② 提示条不存在时**跳过后续依赖它的断言**并显式记 FAIL。

> 教训：**「跑不出结论」和「跑出结论说失败」是两回事。**
> 验证脚本必须在「前置条件不成立」时也能给出结论，而不是挂在等待上。

### 验证（全绿）

```
npx vue-tsc --noEmit                 → 0 错误
npm run check:contrast               → 155/155
node scripts/verify-type-filter.mjs  → 11/11（正向）+ 6 FAIL（负向，符合预期）
```

---

## 流水页筛选新增「分类」，并反转「分类维度不带时间」的旧约定（已完成）

需求：筛选面板里加「分类」（参考图里它在「时间」之后），并明确「底栏『分类』只是换个展示形态，筛选条件照常生效」。

### 后端（3 处）

`summaryByCategory()` 是**裸 SQL**（不走 `applyFilters`），原先只应用 type / accountId / 关键词 / 金额区间，
**不读 start/end/categoryIds**。本次补齐：

```ts
if (query.start) { conditions.push('t.record_date >= ?'); params.push(query.start); }
if (query.end)   { conditions.push('t.record_date <= ?'); params.push(query.end); }
// categoryIds：与 applyFilters 完全同口径 —— 传一级时其下二级也要命中
if (query.categoryIds) {
  const ph = catIds.map(() => '?').join(',');
  conditions.push(`(t.category_id IN (${ph}) OR t.category_id IN (
     SELECT c2.id FROM categories c2 WHERE c2.parent_id IN (${ph})
   ))`);
  params.push(...catIds, ...catIds);
}
```

⚠️ **这是裸 SQL，没法复用 `applyFilters`，只能逐条对齐** —— 注释里写明「改 applyFilters 语义时务必回来同步这里」。
DTO 的 `groupBy` 描述同步更新。

### ★ 约定反转（重要）

**旧约定**（2026-09-14 用户确认）：按分类分组时**不带时间限制**，理由是「分类维度看的是整个账本结构」。

**新约定**（本次）：**底栏「分类」只是换个展示形态，筛选面板里的条件（含时间、分类）都照常生效。**

三处代码注释都明确标注了「2026-09-15 约定反转」，避免后人看到旧注释又改回去：
`summaryByCategory()`、`filterOnlyParams()`、`baseParams()`。

### 前端

**新增** `components/FlowCategoryPicker.vue`（约 260 行）：
- 底部弹层 `z-index: 1100`（与 FlowTypePicker 同级，都叠在筛选面板之上）
- 顶栏 `✕` | 「选择分类」| 「取消全选 / 全选」
- 单一列表：**收入一级在前、支出一级在后**（各按 sort），一级可折叠、二级缩进
- **默认全展开**；折叠状态记「被折叠的」（`collapsed` 记 Set），于是新增分类会自动展开，不需要 watch 列表
- 一级勾选框支持**半选态**（横线 + `brand-50` 底）；勾一级 = 连带其下全部二级
- **核心不变量**：`root.id ∈ draft` ⟺ 该一级的全部二级也在 draft 里。
  因为后端「传一级会连带其二级」，若 root.id 在而某个二级被取消，那个取消动作会被后端吃掉。
  勾/取消二级后要重算 root.id 是否该留在 draft 里。
- 提交时**压缩**：一级已勾 → 只发一级；否则逐个发二级
- 全选 / 全不选 → 都发空数组（= 不过滤）
- 不加搜索框、不加「未分类」项（照参考图）

**改** `FlowFilterPanel.vue`：「时间」后插入「分类」行；`categoryIds` 纳入**受控同步**；
`categoryLabel`（恰好 1 个 → `fullNameOf`；多个 → 「已选 N 项」，N 按大类去重）；`reset()` 复位。

**改** `flow/index.vue`：`filterModel.categoryIds`；`filterOnlyParams` 传 `categoryIds`；
**`baseParams()` 分类分支不再剥离 start/end**；`hasFilter`/`filterSummary`/`viewAll` 纳入；
摘要弹层加「分类」行；顺手删掉一个**死绑定** `@pick-category="openCategoryLevelPicker"`（panel 从未 emit 过它）。

### 新增 scripts/verify-category-filter.mjs（12 项）

覆盖行顺序、默认全选/全展开、勾二级→文案、**★ 回面板再点确定仍生效**、提示条、摘要、查看全部复位。

**负向验证**：注释掉 `categoryIds` 的受控同步 → 脚本报 **FAIL=6**（不是静默通过），恢复后 PASS=12。

⚠️ 脚本里**不写「永远通过」的占位断言**：「分类维度下时间也生效」是后端行为，
由 `test/transaction.service.test.ts` 覆盖，UI 侧不重复（否则就是「报告里写了问题却判定通过」）。

⚠️ uni-app H5 把 `<view>` 渲染成 `<uni-view>`，Playwright 直接 click 常被 hit-test 拦
（"subtree intercepts pointer events"），点击一律走 `evaluate`。

### 踩坑

1. **edit 批量应用时静默部分失败**：一次发 4 个 edit，只落了 2 个（无报错）。
   → 违反 MEMORY 第 9 条「同一文件多处修改必须串行」的扩展：**同一批 edit 后必须回读确认**。
2. **edit 报错 ≠ 整批没生效**：负向验证时第一个 edit 成功、第二个失败，
   结果 `start`/`end` 条件被删掉，差点提交坏代码。**改完必须回读**。
3. `perl` 删除多行代码时转义复杂易失败，改用 `node -e` + 正则替换更可靠。

### 计数断言第 17 次触发

`FlowCategoryPicker.vue` 带 4 条 `font-size` → `check:contrast` §13④ **183 → 187、181 → 185**。

### 验证（全绿）

```
npx tsc --noEmit                     → 0（后端）
npx jest                             → 132/132（原 129 + 新增 3）
bash scripts/regression-test.sh      → 72/72
npx eslint .                         → 0
npm run format:check                 → 通过
cd frontend && npx vue-tsc --noEmit  → 0
npm run check:contrast               → 155/155
node scripts/verify-category-filter.mjs → 12/12（正向）+ 6 FAIL（负向）
node scripts/verify-type-filter.mjs     → 11/11（回归）
```

### 涉及文件

| 文件 | 变更 |
|---|---|
| `frontend/src/components/FlowCategoryPicker.vue` | **新增**（约 260 行） |
| `frontend/src/components/FlowFilterPanel.vue` | +67：分类行 + categoryIds 字段 + categoryLabel + 受控同步 + reset；CRLF → LF |
| `frontend/src/pages/flow/index.vue` | +84：categoryIds 状态 + 传参 + baseParams 反转 + hasFilter/filterSummary/viewAll + 弹层挂载 + 摘要行 + 删死绑定；CRLF → LF |
| `src/transaction/transaction.service.ts` | +40：summaryByCategory 补 start/end/categoryIds + 注释反转 |
| `src/transaction/dto/transaction.dto.ts` | groupBy 描述更新 |
| `test/transaction.service.test.ts` | +48：3 条新用例（不限时间 / 按时间过滤 / categoryIds 连带二级） |
| `scripts/verify-category-filter.mjs` | **新增**（12 项） |
| `scripts/verify-type-filter.mjs` | 删掉未使用的 exists 函数（ESLint） |
| `scripts/check-contrast.mjs` | §13④ 期望值 183/181 → 187/185 |
| `docs/移动端配色与字体方案.md` | §1.3 追加订正（十七） |
| `.workbuddy/memory/MEMORY.md` | 复核清单同步 187 / 185 / 2 / 0 |

---

## 修 bug：分类弹层列表滑不动 + 文字穿透到「确定」按钮上（已完成）

**用户报告**：选择分类弹层里列表滑不动，字穿透到按钮上。

### 根因（MEMORY 里记过三轮的坑，我又犯了）

uni-app 的 `scroll-view` **不吃 flex 推导**，而我给两个新弹层写的是失败方案①：

```scss
.body { flex: 1; min-height: 0; }   /* ❌ scroll-view 会按内容撑开，溢出并盖住底部按钮 */
```

项目里 `flow/index.vue` 的时间弹层注释详细记过这三轮失败：

| 方案 | 症状 |
|---|---|
| `flex:1 + min-height:0` | 按内容撑开，滚轮**溢出并盖住「确定」**（点击被拦截） |
| `flex:1 + height:0` | footer 被挤出容器（整体向上溢出） |
| `height: calc(72vh - 144px)` | 依赖 max-height 精确对齐，脆 |

**最终方案（本次照做）**：高度由 **JS 按视口算**，不依赖任何 flex 推导。

### 修法（两个弹层都改）

1. `scroll-view` 加 `:style="{ height: bodyHeight + 'px' }"`
2. `bodyHeight = max(160, round(windowHeight × 0.72) − 152)`（header/footer 各 76）
3. `.sheet` 补 **`overflow: hidden`**（只写 `max-height` 时它**约束不住 flex 子项**，内容超长会直接溢出）
4. `.body` 由 `flex: 1; min-height: 0` 改为 **`flex: none`**
5. `FlowTypePicker.vue` 补 `ref` 导入（新增 `windowHeight` 用到）

> `FlowTypePicker` 目前只有 2 项、看不出问题，但**同结构同样脆**，一并修。

### 验证脚本补了布局断言（第 [7] 段，5 项）

- 滚动区高度确定（>0）
- **量的是分类弹层（88 行），不是筛选面板**
- 滚动内容确实可滚（`scrollHeight > clientHeight`）
- 「确定」按钮完整可见（不超出 sheet 也不超出视口）
- **滚动区底部不越过「确定」按钮顶部**（正是用户报的穿透）

### 写断言时又踩一个坑：类名重名导致量错元素

第一版 `document.querySelector('.body')` 量到的**是筛选面板**（`childCount=5`，正是它的 5 行），
因为**筛选面板与弹层用了同一套类名**（`.sheet` / `.body` / `.btn-confirm`），且筛选面板在 DOM 中更靠前。
于是断言 `scrollHeight > clientHeight` 报 FAIL（253 > 253）—— **看起来像布局没修好，实际是量错了元素**。

修法：断言里限定 `document.querySelectorAll('.sheet')` 的**最后一个**，
并**新增一条断言专门盯这件事**（`bodyChildCount === 88`）—— 否则下次还会量错。

### 验证（全绿）

```
cd frontend && npx vue-tsc --noEmit  → 0
npm run check:contrast               → 155/155
node scripts/verify-category-filter.mjs → 17/17（原 12 + 新增 5 项布局断言）
node scripts/verify-type-filter.mjs     → 11/11（回归）
npx eslint .                         → 0
npm run format:check                 → 通过
```

实测布局数值：`bodyH=433`、`scrollHeight=3887`（可滚）、`btnBottom=796 ≤ 812`（可见）、
`bodyBottom=736 ≤ btnTop=752`（不压按钮）。

---

## 首页分类支出排行：每项可点击跳转到流水页（已完成）

需求：记账页面「本月各分类支出排行」的每一项都能点击，带参跳到流水页（时间、流水类型、分类）。

### 落点参数（用户给的参考图定下来的）

```
/pages/flow/index
  ?groupBy=category    # 底栏高亮「一级分类」
  &level=1
  &categoryIds=<一级id> # 只筛这一个分类（后端会连带其下二级）
  &start=2026-09-01&end=2026-09-30   # 本月区间
```

参考图里落点页面：底栏高亮「一级分类」、分组标题是分类名（如「行车交通」）、
顶部标题是日期区间（`2026.9.1-9.30`）、分组下展开成按日期的流水明细。

### 决策记录（逐个拍板）

| 问题 | 选择 |
|---|---|
| 跳转后的分组维度 | **B. 分类维度** |
| 按一级还是二级分组 | **B. 一级**（与参考图底栏「一级分类」一致） |
| 带不带时间范围 | **A. 带本月区间** |
| 「未分类」那一项 | **A. 不可点** |

### 一个关键判断：「未分类」为什么不做成可点

分类筛选弹层刻意**不提供「未分类」项**（照参考图），后端也没有筛选未分类的能力。
若让它跳过去，看到的是「不带分类筛选的全部流水」，与用户点它的预期不符。
**与其给一个名不副实的跳转，不如不给** —— 因此 `isClickable()` 判 `!!item.categoryId`，
不可点的项不加 `.rank-item-link` 类，也就没有任何可点暗示。

### 改动

**`HomeView.vue`**（+48）：
- 排行项加 `:class="{ 'rank-item-link': isClickable(item) }"` + `@click="goCategoryFlow(item)"`
- 新增 `isClickable()` / `goCategoryFlow()`
- `.rank-item-link:active { background: $brand-50 }`（扁平体系里按下态靠底色加深，不用缩放/阴影）

**`flow/index.vue`**（+23）：`onLoad` 新增接收 `groupBy` / `level` / `categoryIds`；
`groupBy === 'category'` 时切分组维度与层级，`categoryIds` 写入 `filterModel.categoryIds`。
⚠️ 放在 `unit` 分支**之后**，这样两处都传 groupBy 时以 URL 为准。
⚠️ 标题不覆盖：分类场景下 `start`/`end` 分支已把 `pageRangeText` 设成日期区间，与参考图一致。

### 新增 scripts/verify-rank-jump.mjs（12 项）

覆盖：可点状态（含「未分类」不可点）/ 落点四个参数 / 底栏高亮「一级分类」/
顶部标题是本月区间 / 分组标题是所点分类名 / 返回首页。

**负向验证**：把 `isClickable()` 改成恒 `true` → 报 **FAIL=1、退出码 1**（非静默通过），恢复后 PASS=12。
（做法：`cp` 备份 → `node -e` 改文件 → 跑脚本 → `cp` 恢复 → `md5` 比对确认）

### 写脚本时踩的坑：`<text>` 选择器

第一版用 `.rank-name text` 取分类名，读出的是 `'?'` —— **uni-app 把 `<text>` 渲染成 `<uni-text>`**，
那个选择器匹配不到任何东西。改用 `.rank-name` 的 `textContent`（图标是 svg，不带文字）。

### 验证（全绿）

```
cd frontend && npx vue-tsc --noEmit  → 0
npm run check:contrast               → 155/155（计数保持 187/185，本次无字号变化）
node scripts/verify-rank-jump.mjs    → 12/12（+ 负向 FAIL=1）
npx eslint .                         → 0
npm run format:check                 → 通过
```

### 涉及文件

| 文件 | 变更 |
|---|---|
| `frontend/src/components/views/HomeView.vue` | +48：点击跳转 + isClickable + 按下态；CRLF → LF |
| `frontend/src/pages/flow/index.vue` | +23：onLoad 接收 groupBy/level/categoryIds；CRLF → LF |
| `scripts/verify-rank-jump.mjs` | **新增**（12 项） |

---

## 流水左滑复制/删除 + 软删除 + 流水回收站（已完成）

需求（用户给 4 张截图）：
1. 每一笔流水要支持左滑出现 复制、删除
2. 删除有提示，删除后在流水回收站存在 7 天
3. 新增一个流水回收站的页面，入口放在我的页面中
4. 复制页面可以复用编辑页面，顶部的 title 要变

### 决策记录（逐个拍板）

| 问题 | 选择 |
|---|---|
| 删除方式 | **软删除 + 查询时惰性清理** → 后确认 **B. 惰性真删**（查回收站时先物理删超期记录） |
| 左滑做几个按钮 | **A. 只做「复制」「删除」**（参考图的「退款」本项目无数据模型，不做假控件） |
| 左滑交互 | **A. 用 `uni-swipe-action`**（uni-ui 已装） |
| 回收站交互范围 | **A. 只做「恢复」**（超期自动清理，用户不需要手动永久删除） |
| 复制的时刻 | **B. 保留原单**（复制常用来补录同日多笔） |
| 左滑生效范围 | **C. 流水页分组明细 + 搜索结果 + 日历页当日明细** |
| 删账本的级联删除 | **A. 不进回收站**（保持真删；账本都没了，恢复的流水挂哪？） |

### 后端

**① 迁移 `1789290000000-AddTransactionDeletedAt.ts`**（已执行）：`transactions` 加
`deleted_at datetime(6) NULL` + 索引 `idx_user_deleted (user_id, deleted_at)`。

**② 所有常规查询加 `deleted_at IS NULL`**（共 8 处）：

| 位置 | 方式 |
|---|---|
| `transaction.service` 的 `applyFilters()` | QueryBuilder，**一处生效覆盖列表+按粒度汇总** |
| `transaction.service` 的 `summaryByCategory()` | 裸 SQL，手写（复用不了 applyFilters） |
| `transaction.service` 的 `findById()` | `deletedAt: IsNull()` |
| `statistics.service` 的 4 处裸 SQL | monthly / trend / categoryBreakdown / overview |
| `statistics.service` 的 2 处 QueryBuilder | summaryInRange / monthly |
| `account.service` 的 5 处交易查询 | previewDelete / remove / previewMerge / merge |

**③ `delete()` 改软删除**：写 `deletedAt = new Date()`，不真删行。

**④ 新增 `listDeleted()`**：先**惰性真删** `deleted_at < NOW(6) - 7d`，再返回剩余的。
⚠️ 惰性清理**只在本接口做** —— 写操作集中一处，不让「读列表」带上写库副作用。

**⑤ 新增 `restore()`**：⚠️ **不能用 `findById`**（它带 `deletedAt IS NULL` 过滤，会把已删的滤掉），
要单独 `findOne` 后置 `deletedAt = null`。

**⑥ controller** 新增 `GET /transactions/deleted` 与 `POST /transactions/:id/restore`。
⚠️ **路由顺序**：`/deleted` 必须排在 `/:id` 之前（否则 deleted 被当 id 传给 BIGINT 列 → 500）。

**⑦ 单测 +4 条**（136/136）：删除后详情404/列表不含/汇总不计、回收站能查+恢复、
已删除不能再删+不能恢复未删的、**★ 超期惰性真删**（直改 deleted_at 为 8 天前，验证既不在列表也已物理删）。

### 前端

**① 新增 `utils/txnSwipe.ts`**（共享 composable）：流水页 3 处 + 日历页 1 处行为必须一致，
各写一遍必然分叉（某处忘了刷新，或文案不一样）。

**② `flow/index.vue`**：分组明细行 + 搜索结果行包 `<uni-swipe-action>`；
`const { SWIPE_OPTIONS, onSwipe } = useTxnSwipe(() => reloadAll());`

**③ `calendar/index.vue`**：当日明细加左滑。⚠️ 本页已有 `onSwipe`（月份 swiper 的 @change），
解构时**改名 `onSwipe: onTxnSwipe`** 避免 TS2300。

**④ `record/index.vue` 加复制态**：`onLoad` 接收 `copyFrom`；
`isCopy` ref；标题改「复制支出 / 复制收入」；保存走 POST 新建（`editId` 保持为空）。
⚠️ **时刻要单独拦一道**：`recordTime.value || (!isEdit && !isCopy && !userClearedTime ? nowTime() : '')` ——
不加 `!isCopy` 的话，原单无时刻时会被填成「现在」，与「保留原单」矛盾。

**⑤ 新增 `pages/recycle/index.vue`**：自绘顶栏 + 说明条「删除的流水会保留 7 天，超期自动清除」
+ 按删除日期分组的卡片列表 + 每行「恢复」按钮（`$brand-100` 底 + `$brand-800` 字 5.40:1 ✅）。
⚠️ 参考图的「成员」本项目没有，不做假数据。

**⑥ `pages.json`** 注册 `pages/recycle/index`（navigationStyle: custom）。
**⑦ `MineView.vue`** 加「流水回收站」入口（icon-inbox，放「账本与分类」组）。

### 新增 scripts/verify-recycle.mjs（24 项）

覆盖：左滑容器与两个按钮、删除弹窗文案（**点取消，不碰数据**）、复制态跳转与标题、
「我的」入口、软删后详情 404、回收站能查到、恢复后消失且详情 200。

**负向验证**：把 `txnSwipe.ts` 的「删除」改成「删除X」→ 报 **FAIL=4、EXIT=1**，恢复后 PASS=24。

### ★ 写脚本踩的四个坑（都写进了脚本注释）

**① 严重：拿用户数据做实验。** 第一版直接点 DOM 里第一个「删除」按钮 ——
那对应的是**列表第一条真实流水**（不是测试那条，因为测试数据在别的月份）。
侥幸被后面的「恢复」救回来，但这是**危险的假通过**。
→ 修法：弹窗验证走「取消」；删除/恢复走 **API 精确到自建 id**。
**教训：有写操作的 E2E，每一步都要能定位到自己造的那条数据，定位不到就不要点。**

**② uni-app 真实类名**（实测）：左滑容器是 `.uni-swipe`（不是 `uni-swipe-action-item`），
按钮文案是 `.uni-swipe_button-text`，按钮组默认 `translateX(144px)` 移出屏幕右侧。

**③ modal 关闭时 DOM 节点保留**：uni-app 关闭 modal **只清空文字，`.uni-modal` 节点不删**
（probe 实测：点取消后 `bodyHasText: false` 但 `modalStillThere: true`）。
→ 判据要用「文字是否消失」，不能用「节点是否存在」。

**④ 遗留数据污染**：前几轮失败的运行留下了 2 条金额相同的测试流水，
导致「点第一个恢复」恢复的不是自己那条。→ 跑之前要确保没有遗留测试数据。

### 验证（全绿）

```
npx tsc --noEmit                     → 0
npx jest                             → 136/136（原 132 + 新增 4）
bash scripts/regression-test.sh      → 72/72
npx eslint .                         → 0
npm run format:check                 → 通过
cd frontend && npx vue-tsc --noEmit  → 0
npm run check:contrast               → 155/155（计数 187/185 → 197/195）
node scripts/verify-recycle.mjs      → 24/24（+ 负向 FAIL=4）
```

### 涉及文件

| 文件 | 变更 |
|---|---|
| `src/entity/transaction.entity.ts` | +deletedAt 字段 + idx_user_deleted 索引 |
| `src/migration/1789290000000-AddTransactionDeletedAt.ts` | **新增**（已执行） |
| `src/transaction/transaction.service.ts` | 软删除过滤 + delete 改软删 + listDeleted + restore |
| `src/transaction/transaction.controller.ts` | +GET /deleted + POST /:id/restore |
| `src/statistics/statistics.service.ts` | 6 处加软删除过滤 |
| `src/account/account.service.ts` | 5 处加 deletedAt: IsNull() |
| `test/transaction.service.test.ts` | +4 条用例 |
| `frontend/src/utils/txnSwipe.ts` | **新增**（共享 composable） |
| `frontend/src/pages/recycle/index.vue` | **新增**（回收站页） |
| `frontend/src/api/transaction.ts` | +restoreTransaction +getDeletedTransactions +deletedAt |
| `frontend/src/pages/flow/index.vue` | 两处左滑 |
| `frontend/src/pages/calendar/index.vue` | 一处左滑（onTxnSwipe 改名） |
| `frontend/src/pages/record/index.vue` | 复制态（copyFrom / isCopy / 标题 / 时刻） |
| `frontend/src/components/views/MineView.vue` | +流水回收站入口 |
| `frontend/src/pages.json` | 注册 recycle 页 |
| `scripts/verify-recycle.mjs` | **新增**（24 项） |
| `scripts/check-contrast.mjs` | §13④ 期望值 187/185 → 197/195 |
| `docs/移动端配色与字体方案.md` | §1.3 追加订正（十八） |

---

## 按随手记导出文件补全分类（已完成）

用户上传随手记导出的 xlsx（随手记默认账本20260915154105.xlsx，666 行，支出/收入两个 sheet），
要求「查看文档里的一级分类和二级分类，当前项目缺少的帮我补上」。

### 做法：用脚本解析真实 xlsx，不人工誊录

第一版我**手工誊录**了 Excel 里的分类组合跑对比 —— 结果直接踩坑：列表太长被工具截断
（Unexpected end of input），而且人工誊录本身就可能漏项。
改为**直接解析 xlsx 文件**（openpyxl，本机已装；降级方案是 zipfile + ElementTree 手解 XML）。

> 教训：**有原始文件就不要手工誊录**。誊录既容易漏，又把「可复现」变成「靠人眼」。

### 对比结果

| | Excel 用到的组合 | 项目 preset | 缺失 |
|---|---|---|---|
| 支出 | 35 组 | 13 一级 / 54 二级 | **1** |
| 收入 | 6 组 | 2 一级 / 19 二级 | **0** |

唯一缺失：**「衣服饰品 / 🧦」** —— 用户在随手记里把二级分类**直接命名成 emoji**（🧦 = 袜子），
导出时它原样出现在「二级分类」列。

### 用户的决定

- **补法**：**按原样添加 emoji**（name: '🧦'、icon: '🧦'），不改成「袜子」
- **seed 范围**：只跑 npm run seed（**仅 demo 账号**），不跑 seed:all

### 改动

src/category/category-preset.ts 的「衣服饰品」下新增：

    /*
     * ⚠️ 这个名字就是 emoji 本身（用户要求"按原样添加"）—— 它是随手记导出文件里
     *    的原始写法：用户当时把二级分类直接命名成了 🧦（袜子）。
     *    项目允许这样（name 与 icon 都是同一串），只是分类列表里会显示成"🧦"。
     */
    { name: '🧦', icon: '🧦' },

**分类总数 88 → 89**（支出 13 一级 + **55** 二级、收入 2 一级 + 19 二级）。

seed 实测输出：

    [seed] 用户已存在，跳过：demo
    [seed] 分类：一级 15 个 + 二级 74 个；清理旧分类 0 个，改挂交易 0 笔

### 计数同步（88 → 89）—— 按「当前状态改、历史记录不改」处理

| 文件 | 处理 |
|---|---|
| src/category/category-preset.ts 头注释 | 改（89 + 说明） |
| scripts/verify-category-filter.mjs §7 的 bodyChildCount 断言 | 88 → **89**（附注释说明"改 preset 后要重跑 seed 才生效"） |
| 记账应用全栈规划.md：目录树注释 / §8.1 步骤10 / 分类构成表 / §10 待明确 | 改 |
| docs/前端接口联调文档.md §0 测试账号说明 | 改 |
| docs/UI设计方案·扁平化.md §5.3 | 改 |
| frontend/src/constants/icons.ts 注释 | 改 |
| .workbuddy/memory/MEMORY.md 复核清单 + 分类筛选条目 | 改（并注明"只跑了 seed，其余 120 账号仍 88"） |
| **历史 memo / 变更记录**（memo/2026-09-12.md、.workbuddy/memory/2026-09-12.md、规划文档 §12 旧条目） | **不动** —— 那是对当时实测值的准确记录 |

### 库内实况（直接查库）

    users: 121 / total_cats: 3863
    demo: 89  ← 本次 seed 生效
    其余账号: 88（未跑 seed:all，符合用户选择）

### 验证（全绿）

    npx tsc --noEmit                     → 0
    npx jest                             → 136/136
    npx eslint .                         → 0
    npm run format:check                 → 通过
    cd frontend && npx vue-tsc --noEmit  → 0
    npm run check:contrast               → 155/155
    node scripts/verify-category-filter.mjs → 17/17（childCount=89、默认全选 89/89）

### 涉及文件

| 文件 | 变更 |
|---|---|
| src/category/category-preset.ts | +「衣服饰品 / 🧦」；头注释 88 → 89 |
| scripts/verify-category-filter.mjs | bodyChildCount 88 → 89 + 注释 |
| 记账应用全栈规划.md | 4 处计数 88 → 89 |
| docs/前端接口联调文档.md | 测试账号说明 |
| docs/UI设计方案·扁平化.md | §5.3 |
| frontend/src/constants/icons.ts | 注释 |
| .workbuddy/memory/MEMORY.md | 复核清单 + 分类筛选条目 |

---

## 数据导出独立页（已完成）

需求（用户给 3 张参考图）：「流水导出」做成一个**专门的页面**，可以设置时间与分类。

### 决策记录（逐个拍板）

| 问题 | 选择 |
|---|---|
| 与现有入口的关系 | **A. 新建独立页**，flow 页「更多 → 流水导出」改为**跳转**（替换原来直接导出） |
| 做哪几个配置项 | **A. 只做实际支持的**：日期 / 支出分类 / 收入分类（**不做**账户·成员·商家·项目，它们在本项目没有数据模型） |
| 顶部两个 Tab | **A. 只做「流水导出」**（报表导出仍在报表页，不搬家） |
| CSV 列 | **A. 照随手记导出文件**，但**去掉本项目没有的列** → 最终 6 列 |
| 「导出流水图片」开关 | **A. 不做**（需要 canvas 绘制，成本高且是另一套逻辑） |
| 日期选择器 | **A. 复用** flow 页那套（6 预设 + 三列滚轮）→ 抽成公共组件 |
| 抽组件还是重写 | **A. 抽公共组件**（不动 flow 页的两份逻辑会分叉） |
| 分类选择 | **A. 复用 FlowCategoryPicker，加 type 过滤** |
| 账户（账本） | **C. 不做账本行** |
| 导出范围 | **A. 当前账本** |
| 数据量 | **A. 前端循环分页拉全量**（零后端改动） |
| CSV 里「账户」列 | **B. 不导出这一列** |
| 最终列 | **A. 交易类型 \| 日期 \| 一级分类 \| 二级分类 \| 金额 \| 备注** |

### 改动

**① 新增 \`components/TimeRangePicker.vue\`**（从 flow 页抽出）
- 6 预设（全部时间/本月/上月/本年/去年/自定义）+ 三列滚轮自定义区间
- **完全受控**（\`modelValue\` + \`update:modelValue\`），不持有自己的"已选区间"
- **不负责 reload** —— flow 页要等筛选面板的「确定」才刷新，导出页则在点「导出」时才用，时机不同
- 「自定义」不立即应用（点它只展开滚轮，点「确定」才 emit）

**② flow 页改用组件**，删掉内联实现（模板 76 行 + script 121 行 + 死样式 22 个块）
- 新增 \`timeRangeModel\`（computed 双向绑定到 filterModel 三字段）+ \`onTimePicked\`

**③ \`FlowCategoryPicker\` 加两个可选 prop**
- \`type?: 'income' | 'expense'\` —— 只列该类型（不传时仍列全部，流水页不受影响）
- \`title?: string\` —— 弹层标题（默认「选择分类」）

**④ 新增 \`utils/csv.ts\`**
- \`csvCell\` / \`toCsv\` / \`downloadCsv\` —— 原本在 flow 页与 ReportView **各写了一遍**（完全相同的代码），导出页是第三处

**⑤ 新增 \`pages/export/index.vue\`**（独立页，navigationStyle: custom）
- 3 行配置 + 底部橙色「导出」按钮
- **循环分页拉全量**（size=100，安全上限 200 页 = 2 万条）
- 分类 id → 一级/二级两列：未分类 / 挂一级 / 挂二级 三种情况都处理
- **不做假控件**：成员、商家、项目、导出流水图片、账户行 都没有

**⑥ flow 页导出入口改跳转**：\`doExport\` → \`goExport\`（\`navigateTo('/pages/export/index')\`）
**⑦ MineView 加「数据导出」入口**；\`pages.json\` 注册新页

### 新增 scripts/verify-export.mjs（21 项）

覆盖：入口（我的页 + flow 页跳转）/ 3 行配置 / **不出现假控件** / 日期默认本月 /
时间弹层 6 预设 / 支出分类只列支出 / 收入分类只列收入 / **真的下载到 CSV**（文件名格式 + 6 列列头 + 有数据行）。

**负向验证**：去掉 \`FlowCategoryPicker\` 的 \`type === 'income'\` 分支 → **FAIL=1、EXIT=1**，恢复后 PASS=21。

### ⚠️ 修了两个「早已失效」的旧脚本断言（与本次改动无关）

跑 \`verify-flow.mjs\` 时发现 4 个 FAIL，排查后确认**都是脚本自身过时**，不是本次改坏：

1. **搜索框选择器**：脚本用 \`.search-bar\` / \`.search-input input\`，
   但流水页在 09-14 就把搜索改成了**全屏 \`.search-page\`**（类名 \`.search-box-input\`）。
   → 已同步（\`.search-bar\` → \`.search-page\`、\`.search-input input\` → \`.search-box-input input\`）。
2. **盲目点 \`.group-head\`**：流水页 09-14 起 \`loadGroups()\` 末尾会**自动展开第一个分组**，
   脚本又点一次是**收起** → 断言拿到 txn=0，且**连带 [1.5] 吸顶断言全部失效**
   （分组收起后页面高度不足 812px，\`window.scrollTo(0, 300)\` 滚不动，所以顶栏不会变白）。
   → 改成**幂等**：只在未展开时才点。

修完后 \`verify-flow\` **31/31 全过**。

> 教训：**脚本失效是静默的**。这次是因为我动了 flow 页才顺带跑到它；
> 如果一直没动那个文件，这 4 条断言会一直"红着"或（更糟）一直"绿着"却测不到东西。

### 计数断言第 19 次触发

时间选择器抽成组件（+7）+ 新增导出页（+6）− flow 页删掉的死样式（−6）
→ \`check:contrast\` §13④ **197/195 → 204/202**。

### 验证（全绿）

\`\`\`
后端：npx tsc --noEmit → 0；npx jest → 136/136；regression-test.sh → 72/72
      npx eslint . → 0；npm run format:check → 通过
前端：npx vue-tsc --noEmit → 0；npm run check:contrast → 155/155
脚本：verify-export.mjs        → 21/21（+ 负向 FAIL=1）
      verify-flow.mjs          → 31/31（修了 2 处过时断言）
      verify-category-filter.mjs → 17/17
      verify-type-filter.mjs   → 11/11
      verify-rank-jump.mjs     → 12/12
\`\`\`

### 涉及文件

| 文件 | 变更 |
|---|---|
| \`frontend/src/components/TimeRangePicker.vue\` | **新增**（从 flow 页抽出的公共组件） |
| \`frontend/src/pages/export/index.vue\` | **新增**（数据导出页） |
| \`frontend/src/utils/csv.ts\` | **新增**（CSV 工具，三处共用） |
| \`frontend/src/pages/flow/index.vue\` | 改用 TimeRangePicker + 导出入口改跳转 + 删死代码/死样式（−389 / +42） |
| \`frontend/src/components/FlowCategoryPicker.vue\` | +\`type\` / +\`title\` 两个可选 prop |
| \`frontend/src/components/views/MineView.vue\` | +「数据导出」入口 |
| \`frontend/src/pages.json\` | 注册 \`pages/export/index\` |
| \`scripts/verify-export.mjs\` | **新增**（21 项） |
| \`scripts/verify-flow.mjs\` | 修 2 处过时断言（搜索框选择器 + 幂等展开） |
| \`scripts/check-contrast.mjs\` | §13④ 期望值 197/195 → 204/202 |
| \`docs/移动端配色与字体方案.md\` | §1.3 追加订正（十九） |
