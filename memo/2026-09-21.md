# 2026-09-21 · NAS 部署端口调整（host 方案已回退）

> 承接 09-20 中台部署。本日先尝试全量 host 网络（为 IPv6），
> 后经 luchao 决定**回退到 bridge**，最终只保留端口调整。均为实测反馈驱动。

> ⚠️ **重要**：本日一度改成 `network_mode: host`（提交 cfb949c），
> 但 luchao 随后决定**不用 host、改回 bridge**（见 §十一）。
> 下方 §二~§五 是当时的 host 方案记录（**已废弃**），
> 最终生效的是 §十一 的 bridge 状态。保留记录以备将来参考。

## 一、本日提交（3 个）

| 提交 | 内容 |
|---|---|
| `309d9c2` | fix(deploy): docker-compose.nas.yml 端口改 IPv4+IPv6 双绑 |
| `cfb949c` | feat(deploy): NAS 改用 host 网络，对外只开 8080/8081 |
| `7373f7c` | fix(deploy): web 端口 8080 -> 8082（8080 在群晖上访问不通） |

分支：`develop`。

## 二、为什么从 bridge 改成 host

**问题**：bridge 模式下即使把端口映射写成 `[::]:8080:80` 双绑，
群晖上外网 IPv6 仍可能访问不通 —— 因为 **bridge 模式的 IPv6 取决于
Docker daemon 是否开启 IPv6**，而群晖默认常常没开。

**方案**：改 `network_mode: host`，容器直接共享宿主网络栈 →
群晖的 IPv6 地址 / 防火墙规则**直接生效**，IPv4/IPv6 由宿主机统一决定。

**代价**：端口即宿主端口，无映射；需自行保证端口不冲突。

## 三、最终网络拓扑（docker-compose.nas.yml）

| 服务 | 网络 | 端口 | 对外 | 说明 |
|---|---|---|---|---|
| web | host | **8082** | ✅ IPv4+IPv6 | App 前端 nginx |
| admin | host | **8081** | ✅ IPv4+IPv6 | 中台 nginx |
| backend | host | 7001 | ❌ 仅回环 | `HOST=127.0.0.1` |
| db | host | 3306 | ❌ 仅回环 | `--bind-address=127.0.0.1` |

- web/admin 的 nginx：`listen 8082` + `listen [::]:8082`（admin 同理 8081）；
  `proxy_pass http://127.0.0.1:7001`（host 网络下不再是服务名 backend）
- backend：`DB_HOST=127.0.0.1`（不再是 db）、`HOST=127.0.0.1`（仅回环监听）
- db：`command: --bind-address=127.0.0.1`

**安全设计**：对外只开 8082/8081；backend(7001)、db(3306) 都只绑回环，
外网碰不到 —— 这是 luchao 明确要求的（选 B 方案）。

## 四、代码改动

- `src/config/config.default.ts`：koa 加 `hostname: process.env.HOST || '0.0.0.0'`
  （默认 0.0.0.0，不影响本机 dev；生产用 HOST=127.0.0.1 限回环）。
  已核对 Midway koa 源码：`hostname` 会传给 `server.listen({ host })`。
- `deploy/nginx.conf` / `nginx.admin.conf`：listen 8082/8081 + [::]，反代 127.0.0.1:7001
- `deploy/Dockerfile.frontend` / `Dockerfile.admin`：EXPOSE 8082 / 8081
- `deploy/docker-compose.yml`（bridge 通用版）：映射**目标端口**同步改 8082/8081
  （仍保持 bridge，供本机验证用）
- `deploy/.env.example`：WEB_PORT=8082
- `deploy/README.md`：架构段落 + 端口说明更新

## 五、⚠️ 端口 8080 → 8082

luchao 实测：群晖上 **8080 访问不通**（大概率被 DSM 或其他套件占用），
故 web 端口改为 **8082**。admin 保持 8081。

## 六、本日验证

| 项 | 结果 |
|---|---|
| `check:contrast` | 158 项全过 |
| `docker compose -f docker-compose.nas.yml config` | 语法正确，全 network_mode: host |
| 镜像内 nginx 配置 | 确认 listen 8082/8081、proxy_pass 127.0.0.1:7001 |
| Midway koa hostname 支持 | 源码确认（framework.js:269/283） |
| 运行时端到端 | **未做**（本机 3306 未起，DB 连不上；由 luchao 在 NAS 验证） |

## 七、构建产物（deploy/out/）

| 文件 | 时间 | 说明 |
|---|---|---|
| backend.tar | 08:42 | 含 host/TZ 相关 |
| admin.tar | 08:42 | nginx 监听 8081 |
| frontend.tar | 13:30 | nginx 监听 **8082**（最新） |
| mariadb.tar | 09-18 | 未变 |

## 八、NAS 部署步骤（本次）

```bash
cd /volume1/docker/account-book
docker load -i backend.tar && docker load -i frontend.tar && docker load -i admin.tar
docker compose -f docker-compose.nas.yml down   # 网络模式变了，必须重建
docker compose -f docker-compose.nas.yml up -d
```

访问：App `http://NAS_IP:8082`、中台 `http://NAS_IP:8081`（IPv6 同理）。

## 九、注意

- **端口冲突**：host 模式下 8082/8081/7001/3306 都是宿主端口，
  确保没被占用（冲突时改 nginx `listen` + compose 注释 + 重建镜像）。
- **`down` 别加 `-v`**：否则删 db 数据卷（数据在
  `/volume1/docker/account-book/data` bind mount，不受影响，但仍谨慎）。
- **db 限回环**：只有本机能连 3306；若之前用别的机器连过，将连不上。

## 十一、⚠️ host 方案已回退（最终生效状态）

**决定**：luchao 最终选择**不用 host，改回 bridge**。回退内容：

| 文件 | 回退后 |
|---|---|
| `docker-compose.nas.yml` | 去掉所有 `network_mode: host`；db 去 `--bind-address`；backend `DB_HOST` 改回 `db`、去 `HOST`；web/admin 恢复 `ports` 映射（`0.0.0.0:8082:80` + `[::]:8082:80`，admin 同理 8081） |
| `nginx.conf` / `nginx.admin.conf` | `listen 80`（去 `[::]` 行）；`proxy_pass http://backend:7001` |
| `Dockerfile.frontend` / `Dockerfile.admin` | `EXPOSE 80` |
| `docker-compose.yml` | 映射目标改回 `:80` |
| `config.default.ts` | 移除 `koa.hostname` |
| `README.md` | 架构段落改回 bridge 说明 |

**最终生效状态**：
- 网络：bridge（默认 compose 网络），web/admin 通过 `backend` 服务名反代
- 端口：**web 8082、admin 8081**（luchao 要求保留 8082，因群晖 8080 访问不通；
  端口选择与网络模式无关）
- backend(7001)、db(3306) 仅内网

**为什么保留 8082 不回 8080**：8080 访问不通是"群晖宿主端口被占用"，
与 bridge/host 无关，改回 8080 会重新踩坑。

**注意**：bridge 模式下 `[::]:8082:80` 能否通 IPv6，取决于 Docker daemon
是否开 IPv6（群晖默认常没开）—— luchao 已知悉并接受。

## 十、仍遗留

1. 中台管理员需在 NAS 手动创建（中台无注册入口）：
   `docker compose -f docker-compose.nas.yml exec backend node dist/scripts/admin-create.js <用户名> <密码>`
2. 模板残留未清完（`/auth` 下 code-login/qrcode-login/forget-password/register 等）
3. 长期：深色模式 / 顶部安全区 / 键盘 Tab 导航 / CI
