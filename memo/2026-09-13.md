# 2026-09-13

## 单页容器的滚动位置记忆（已完成）

昨天把 4 个 Tab 页折叠成单页容器（`pages/main/index.vue`）后，切换视图会回到顶部。
本次补上**各视图独立记忆滚动位置**。

### 实现要点

- 位置表用**普通对象**而非 ref：`onPageScroll` 触发极频繁，
  走响应式等于每次滚动都推一次依赖，而这里不需要响应式。
- **恢复期间暂停记录**（`restoring` 标志）—— 这是唯一容易踩的坑：
  切换视图时页面总高从「旧视图高度」突变成「新视图高度」，浏览器会立刻调整
  scrollTop，这个中间态同样触发 `onPageScroll`；不禁用就会把它写进**新视图**
  的位置表，污染真实记忆。
- 恢复动作放 `nextTick` 后再 `setTimeout(0)`：v-show 切换后高度由新视图决定，
  在高度更新前滚动会被旧高度截断。
- `pageScrollTo` 的 `complete` 部分端不回调 → 另设 120ms 兜底解除 `restoring`，
  否则它会永久卡住，导致位置再也不被记录。

### 验证

`build:h5` 通过；配色 155 项全绿；dev 200。

**手动验证**：明细页滚到中部 → 切首页 → 切回明细，应停在原位置。

## 账本选择页底栏改为就地切换（已完成）

**需求**：账本选择页（启动页，底栏只有「首页 / 我的」两项）点「我的」**不要跳转**。

**先测再改**（`node scripts/diagnose-nav.mjs`，真实 Chrome）：

| 路径 | hash | 页面栈 | 返回按钮 |
|---|---|---|---|
| 记账页点「我的」 | 不变 | 1 → 1 | 0 |
| 账本选择页点「我的」（改前） | `→ ?tab=mine` | 1 → **2** | **1** |
| 账本选择页点「我的」（改后） | 不变 | 1 → 1 | 0 |
| 账本选择页点**账本**（改后，回归） | `→ /pages/main/index` | 1 → **2** | **1** |

**为什么不能把账本选择页并入主容器**：上一轮明确要求"选账本进首页后左上角有返回按钮"，
那是 `navigateTo` 压栈的结果；本次要求「我的」**不压栈**。两个诉求方向相反，
只能让「我的」留在**本页内**做视图切换 —— 页面本身仍是独立启动页，两条诉求共存。

**实现**：
- `account-select` 页加 `activeView`（`select` / `mine`）；底栏从"导航模式"改**受控模式**
  （`:current` + `@change`），点击只 `emit('change')` → 页内切视图，不产生任何跳转。
- 「我的」直接 `<MineView />` 复用（`v-if` 挂载，避免与主容器同时常驻两份实例）。
- `MineView` 底部留白改用 CSS 变量 `--view-bottom-gap`（默认 88px = 52px 底栏 + FAB 外溢；
  账本选择页没有 FAB，宿主覆盖为 52px，否则多出 36px 空白）。

**顺带修**：`frontend/index.html` 补 `<link rel="icon" href="/static/logo.png">` ——
之前控制台一直挂着 `favicon.ico 404`。这个 404 是靠 `msg.location()` 定位的，
`page.on('response')` 抓不到它（值得记住）。

### 探针坑（写诊断脚本时会踩）

`.uni-page-head-hd` **恒存在**（没有返回按钮时内容是 `<!---->`），
拿"这个容器在不在"判断"有没有返回按钮"会 100% 误报"有"。
必须数它里面的 `.uni-page-head-btn` —— 本次第一版探针就是这么骗过我的。

## 记账页底栏改版：记一笔从「悬浮」变「底栏内的凸起项」（已完成）

**需求**：底栏布局参考图 —— 记一笔是底栏里的第 3 项（圆形按钮向上凸出、文字与其它项同一行），
而不是浮在底栏上方。

**改动**：
- `TabBar` 支持 `raised: true` 的项：渲染成圆按钮，**靠底对齐**（文字与其它项同行），
  圆的顶部因此自然越出底栏顶边；默认项扩成 5 项（记账 / 明细 / **记一笔** / 统计 / 我的）。
- `TabBar` 不硬编码"记一笔去哪"——它照常 `emit('change', 'record')`，由容器 `switchTo` 兜住
  （`navigateTo('/pages/record/index')`，保留页面栈，返回后当前视图和滚动位置都还在）。
- **删除 `RecordFab.vue`**（悬浮按钮不再需要；全仓只有容器引用它）。

### 这一节的重点是"两次实测打脸"

写死在 CSS 里的注释全是错的，是量出来才发现：

| 版本 | 实测 | 症状 |
|---|---|---|
| ① `min-height: 56px` | `barHeight = 79`、`overhang = −1` | 凸起项内容（48+2+17+10）**把底栏一起撑高**，圆被整个吞进底栏 |
| ② 改 `height: 56px` | `btnH = 25` | 高度固定后内容是"溢出"态，而 **flex 子项默认 `flex-shrink: 1`**，48px 的圆被压成药丸 |
| ③ `height: 56px` + `flex-shrink: 0` | `overhang = 22` ✓ | 圆的 48×48、越出 22px、凸起项文字与其它项差 1px |

**两条通则**（都是这次踩出来的）：
1. **`min-height` 挡不住内容撑高容器** —— 要让某个子项"越出"父容器，父容器高度必须写死，
   否则它一定被最高的子项撑开，"凸出"就永远不发生。
2. **溢出 ≠ 禁收缩**。固定父高之后，子项进入溢出状态，flex 会先把它压扁（`flex-shrink: 1`），
   尺寸写死 48px 也没用，必须显式 `flex-shrink: 0`。

底栏 56px（原 52）+ 凸出 22 = 顶端 78px，各视图留白 88px 仍够，未改。

**颜色说明**：参考图的圆是浅橙，本项目用 `$brand-600 #CF4A12`（白加号 4.52:1 ✅）。
`$brand-500 #FF6B35` 上压白字只有 2.84:1，不能用于承载白字 —— 这是既有的品牌色分档规则，未破例。

### 计数断言又抓一次

删除 `RecordFab.vue` 后，`$font-*` 从 105 降到 **104**、总数 107 → **106**
（`RecordFab` 的 `.fab-label` 是那 1 处）。已同步校验脚本与 §1.3。

**顺带补全一处文档缺陷**：`docs/移动端配色与字体方案.md` 的订正编号是「一、二、五」——
（三）（四）只写在脚本注释里、文档漏了。本次补成连续的一~六。

## 两个 bug 修复（用户报告）

### ① 内容不足一屏也有滚动条

**根因（实测，非推测）**：uni-app H5 把页面内容放在 `uni-page-wrapper` 里，其高度 =
`100vh − 导航栏(44px)`。而页面里写的是 `min-height: 100vh` —— 把导航栏那 44px
**多算了一遍**，于是**每个这样的页面恒溢出 44px，与内容多少无关**。

`scripts/diagnose-scroll.mjs` 的实测数据完全对得上：

| 页面 | 内容区高 | `.page` 的 min-height | 溢出 |
|---|---|---|---|
| statistics / mine / 账本选择页 | 768 | 812 | **恰好 44px** |
| home / detail | 768 | 812 | 143 / 165（这两页内容本来就超屏，该滚） |

**修法**：收敛成一个 token，全项目 **10 处**替换：

```scss
// tokens.scss
$page-min-height: calc(100vh - var(--window-top, 0px) - var(--window-bottom, 0px));
```

`--window-top` 是 uni-app 自己暴露的（实测值 `calc(44px + 0px)`），
所以 `navigationStyle: custom` 的页面会自动退化成 `100vh`，不需要特判。

修复后：statistics / mine / 账本选择页 **44 → 0**；home / detail 仍滚（内容确实超屏）✓

### ② 分类删除的提示文案错了两处，而且方向相反

`pages/category/index.vue` 把子分类数**当参数从模板传**，两处都传错：

- 删一级分类传 `onDelete(root, 0)` → **硬编码 0**，
  "该分类下还有 N 个二级分类，会一并删除"这句**永远不出现**
- 删二级分类传 `childrenOf(root.id).length` → 这是**它的兄弟数量**，
  于是删一个二级却提示"该分类下还有 7 个二级分类会一并删除"

**修法**：子分类数改为在函数里自己算 ——

```ts
function onDelete(item: CategoryItem) {
  const childCount = childrenOf(item.id).length;   // 从数据算，不靠调用方传
```

**消除"调用方可以传错"这个入口**，而不是把两处值改对（后者下次还会错）。

验证：`scripts/verify-category-delete.mjs`（打开弹窗读文案，**只读不删**，一律点取消），
四项断言全过。实测：一级「居家物业」→「该分类下还有 7 个二级分类，会一并删除；…」（与页面角标 7 一致）；
二级「公共交通」→ 只有「相关账单会变为「未分类」。确定删除「公共交通」？」。

## 图标库 + 图标选择器 + 新建二级分类独立页（已完成）

**需求**：引入图标库，在新建二级分类时用图标选择器挑图标，参考图形态。
（**自定义上传已由用户取消** —— 所以后端上传接口没做，"自定义"Tab 也随之去掉，改成按图标集划分的 3 个 Tab。）

### 选型：没有引入"运行时图标库"，而是生成图标数据

用户最初说的是 vue-icons，但调研发现 **`vue-icons` 的两个候选包
（`@kalimahapps/vue-icons` / `vue-icons-lib`）收录的全是单色线性图标集**，
做不出参考图的彩色效果。用户确认要彩色后，方案定为：

- 数据来源：Iconify 的离线图标集包（devDependency，不进产物）
  - `@iconify-json/flat-color-icons` 329 个扁平彩色（48 网格）→「多彩」Tab
  - `@iconify-json/noto` 精选 141 个（128 网格）→「生活」Tab
  - 「标准」Tab 复用项目原有的 `CATEGORY_ICONS`（单色，15 个）
- 生成脚本 `scripts/gen-color-icons.mjs` → `frontend/src/constants/color-icons.ts`
- 渲染组件 `components/ColorIcon.vue`（多色）+ `components/CategoryIcon.vue`（自动选渲染器）

**为什么不直接引运行时库**：项目已有「图标数据内置 + 自绘组件」的模式
（`constants/icons.ts` + `SvgIcon.vue`）。再引一个运行时库等于两套体系并存，
且只有被选中的图标需要进产物（noto 全量 3800 个，我们只要 141 个）。
生成脚本留在项目里 ⇒ 这份数据是**可复现**的，不是一次性粘贴。

体积：源文件 632 KB / **gzip 212 KB**（懒加载的二级页面，可接受）。

### 三个坑（都是实测出来的）

1. **noto 的 SVG id 会撞车**（生成期必须处理）
   noto 大量使用 `<linearGradient id="svgIDa">` + `fill="url(#svgIDa)"`。
   这些 id 在各自文件里唯一，但**放到同一页面就重复了** —— 浏览器解析 `url(#id)`
   取的是文档中第一个同 id 元素，于是第 2 个及之后的图标会拿到第 1 个图标的渐变，
   **颜色整体错乱**。图标选择器一屏要渲染几十个，必然触发。
   修法：生成时给每个 id 加集合前缀（`colorful-` / `life-`），同时替换 `url(#…)` 引用。

2. **flex 容器用 `min-height` 会把固定底栏顶出屏幕**
   图标选择页写 `.page { min-height: $page-min-height; display:flex; flex-direction:column }`，
   `.grid { flex: 1 }`、`.tabs { flex: none }`。结果**底部 Tab 整条不见了**（截图才看出来）：
   `min-height` 下容器高度是 auto，`flex: 1` 的子项按**内容高度**撑开，把 Tab 推到了屏幕外。
   修法：改 `height: $page-min-height` —— 内部靠 scroll-view 滚动的页面，容器需要**确定高度**。

3. **uni-app H5 里 `.input` 不是 `<input>`**
   写测试脚本时 `page.locator('.input').fill()` 会报
   "Element is not an `<input>`" —— uni-app 把它渲染成 `<uni-input>` 外壳 + 内部真 input。
   选择器要写成 `.input input` 穿透一层。

### 端到端验证

`scripts/verify-icon-picker.mjs`（10 项断言，**会真建一个分类并在最后删掉**）：

```
✓ 导航栏标题为「新建二级支出分类」   ✓ 底部有 3 个图标集 Tab
✓ 多彩 Tab 329 个 / 生活 141 个 / 标准 15 个
✓ 选完图标后回到新建页   ✓ 图标已回传（渲染出彩色图标）
✓ 再次进入时高亮回填当前图标   ✓ 保存后回到分类管理页
✓ 列表里出现新分类
后端实际保存的 icon = colorful:add-database
```

另有 `scripts/verify-color-icon.mjs` 专门盯 `v-html` 渲染 ——
这是本次最大的技术风险（uni-app 的模板编译器对 `v-html` 有特殊处理，
若把它在 `<svg>` 上也改写，图标会**静默渲染成空白**，不报错不警告）。

### 数据一致性

- `$font-*` 计数 104 → **110**，总数 106 → **112**（两个新页面 +6 处），已同步脚本与 §1.3
- 验证用的临时数据（把分类图标改成 `colorful:home`）已**还原为原值**（`F09F8FA0`）
- 端到端脚本创建的测试分类已用接口删除（id 5057 / 5058 两次跑各一个）

## 项目当前状态（供下次接续）

- 前端：单页容器架构（pages 下 6 个：account-select / main / login / record / category / account）
- 导航：自定义 `TabBar.vue`（受控模式 + 导航模式双用），原生 tabBar 已移除
  （**必须保留 `"tabBar": { "list": [] }`，否则 H5 白屏**）
- 视觉：FL-1 主体完成（明细/统计/我的/键盘/启动页）；登录页与设置类三页未逐页改造
- 校验：`npm run check:contrast` **155 项**；字号计数 **106 / 104 / 2 / 0**
- 底栏：`TabBar.vue` 5 项（记账/明细/**记一笔凸起**/统计/我的），高度固定 56px，
  凸起圆 48px 越出顶边 22px；`RecordFab.vue` 已删除
- 后端：25 接口、单测 98、回归 47

## 待办（P2）

登录页与设置类三页的视觉收尾；
**结构改造后仍需重跑 320px × 200% 三桶判据复验**（一直未做）。


---

## 分类管理页重构：批量操作 + 一级分类可选图标（并修掉 P0）

详细记录见 `.workbuddy/memory/2026-09-13.md` 同名小节。

### 先修 P0：分类功能当时是整块坏的

`category.entity.ts` 声明了 `isHidden`，却**没有迁移建这一列**，而 `synchronize: false`，
于是所有走 Category 实体的查询抛 `Unknown column 'Category.is_hidden'`：
`GET /categories`、`GET /categories/:id`、`GET /transactions` 全部 500，
明细页 / 记一笔分类选择器 / 分类管理页**都用不了**（`statistics/*` 走裸 SQL 幸存）。
新增迁移修复。**教训：实体与迁移必须同步提交。**

### 「跨账本复制」不成立，没有照搬参考图

多账本设计文档 §2.3 明确写着分类是**用户级共享**，库里 `categories` 也没有 `account_id` ——
同一个分类本来就作用于所有账本，没有东西可复制。经确认底栏改为
**删除 / 隐藏 / 恢复显示**，后两者按选中项状态**自动禁用不适用的一方**。

### 后端 3 个新能力

`visibility=all|visible`（**默认 all**，否则管理页看不到隐藏项 = 功能死锁）、
`batch-delete`（父子同时选中不重复计数；含无效 id 整单失败不静默少删）、
`batch-hide`（父被选中时不冗余写子）。单测 98 → **107**。

### 前端按参考图重构

自绘顶栏 + 分组行 + 固定底栏；批量态有选择框与三个底栏操作。
三个取舍：收支类型改为**页面级参数**（配套「我的」页两个入口）；
**不放拖拽手柄**（拖动排序没做，不做假控件）；保留行内编辑（改名/换图标没有别的入口）。
「新建二级分类」页泛化为**新建一级 / 新建二级 / 编辑**三用途，共用同一个图标选择页。

### 把挂了三天的待办做成脚本，一跑抓到 3 个真问题

新增 `scripts/reflow-audit.mjs`（三桶判据，27 屏）。首次运行就抓到：
明细页 `.summary` 在 ×2 下顶破 **325 > 320**、新建的分类页底栏把文字挤成 **21px**、
长分类名被 ellipsis 截断。全部修复，最终 **27 屏 0 不合规**。

### 三处"我自己写的东西骗了我"

① `bad` 判据漏了「被祖先裁掉」这一桶 → 两屏「被裁 1 处」仍显示 ✅
（**报告写了问题却判定通过，比没有脚本更糟**）；
② hash 路由下 `goto` 同一 URL **不重新挂载** → `?tab=` 无效（巡检到了首页的元素）
且内联字号**逐次累乘**（60→120→240px）—— **同一个坑一小时内踩两次**；
③ 页面级脚本把测试①的状态漏到测试②，让测试②的断言**假失败**。
→ **验证脚本必须自己建立前置条件。**

### 顺带把一直红着的 lint 修绿

`scripts/**/*.mjs` 缺 8 个双上下文全局（浏览器侧 `document`/`window`… + Node 侧 `fetch`），
`npm run lint` 从 09-13 起一直是 49 个错误 → 补齐后 **ESLint 首次 0 错误**。

### 验证

`check:contrast` **155/155**、单测 **107/107**、回归 **47/47**、ESLint **0**、prettier 通过、
`build:h5` 通过、`vue-tsc` **0**、`verify-category-batch` **17/17**、`verify-category-page` **40/40**、
`reflow-audit` **27 屏 0 不合规**。Swagger 实测 16 路径 / 25 操作 / 5 分组 / 55 模型。


---

## 底栏「明细」改为「报表」并改成跳转（明细下线、统计独立成页）

详细记录见 `.workbuddy/memory/2026-09-13.md` 同名小节。

### 需求与拍板

需求：底栏「明细」改名「报表」，点击**不切换视图、改为页面跳转**。
项目里原本**没有「报表」这个页面**（设计文档 §7 页面清单是 首页/明细/统计/我的/记一笔），
所以落点必须先定，不能猜。两问两答：

- **报表落点 → 把统计拆成独立页**（`pages/statistics/index`），底栏那一格叫「报表」
- **明细去留 → 退出底栏并下线**（动手前先核查引用：全库只有容器自己引用 `DetailView`，
  首页/我的都没有指向明细的入口 → **下线后交易流水全应用没有任何入口**，此后果已明确报告）

### 关键实现：TabBar 的 item 级 url

要让某一格「跳转」而不是「切视图」，给 item 加 `url` 即可，**两种模式都成立**。
`go()` 里 url 分支必须排在受控模式判断**之前** —— 反过来的话跨页项会被当成容器里并不存在的
视图 `emit`，父级没有对应分支 → **点了没反应且不报错（死按钮）**。

于是底栏一格有两种语义：**无 url = 切视图，有 url = 离开本页**；判断只看 url 不看模式。
默认项 5 格 → 4 格（记账 / 报表 / 记一笔 / 我的）；容器只剩 **记账 / 我的** 两个视图。

### 三处顺带纠正的判断

1. **`--view-bottom-gap` 的默认值要等于事实**。`StatisticsView` 的底栏留白原写死 88px，
   但它现在只被独立页承载（无底栏）→ 改为 `var(--view-bottom-gap, 0px)`。
   默认值写错方向时，忘记设的代价是「页底一片空白」（容易被忽略），而不是「内容贴底」（一眼可见）。
2. **报表页刻意不带底栏**：导航模式的底栏靠 `navigateBack` 回栈，而它**不会重跑 `onLoad`**，
   `?tab=` 不生效 —— 想让它能切「记账 / 我的」得另设计，不能直接套导航模式。
3. **底栏 4 格时凸起项几何上无法居中**（实测落在 62.5%，偏移 46px ≈ 底栏宽 12.3%）：
   3 个视图 + 1 个动作时槽位中点是 12.5/37.5/62.5/87.5%，要居中必须左右同宽 = 5 格。
   这是信息结构决定的，不是样式没调好。

### 复验脚手架必须跟着改，否则会自己骗自己

- `reflow-audit.mjs` 的页面清单里 `?tab=detail` / `?tab=statistics` 都已失效 → 同步为
  home / mine / **report（独立地址）**；否则会重现「巡检到错页却全绿」。
- `diagnose-nav.mjs` 写死点「明细」的序列会直接失败 → 改为容器内切换序列，
  **并新增「报表必须跳转」的三项断言**（hash 变 + 栈 +1 / 目标页有返回按钮 / 内容真渲染）
  与「返回后页面栈复原」。

### 验证（全绿）

`diagnose-nav` **12 项全过**（报表 hash `#/pages/main/index → #/pages/statistics/index`、
栈 1→2、返回后 2→1；记账/我的仍为就地切换；账本选择页不受影响）；
`reflow-audit` **8 页 × 3 档 = 24 屏 0 不合规**；`check:contrast` **155/155**
（字号计数按实订正 115/113 → **104/102**，已追加订正（九））；ESLint **0**；prettier 通过；
`vue-tsc` **0**；`build:h5` 通过；单测 **107/107**；回归 **47/47**。

顺带订正两处**上一轮漏掉的旧数字**：MEMORY.md 复核清单里的 Swagger 计数（23/14/49 → **25/16/55**）、
规划文档里残留的「115 项断言」（→ **155**）。

---

## 报表页按参考图重构（含折线图、年/月粒度、吸顶）

### 后端：新增 `/statistics/report` 聚合接口

一次返回该时段的 `{summary, expenseCategories, incomeCategories, trend}`：

- `period=YYYY` → 整年聚合，`trend` 返回 12 个月（无数据月份补 0）
- `period=YYYY-MM` → 单月聚合，`trend` 为 `[]`（单月看"跨月走势"没意义）
- 旧 `monthly` / `category` **保留不动**（首页仍在用）
- DTO 用 `PERIOD_PATTERN = /^\d{4}(-\d{2})?$/` 一个正则覆盖两种粒度

### 前端：双 Tab 报表页

- **顶栏自绘**（`navigationStyle: custom`）：返回 + 居中「报表」
- **Tab 只留「基础统计 / 分类」**，其余（账户/成员/项目/商家）本就不存在，未新增
- **基础统计**：账本流水统计（白底深墨大数字，去插画）+ 记账里程碑（时段笔数）+
  收入来源 + 支出分布 + 月度收支趋势（年粒度才有）
- **分类**：支出/收入分类统计（环形图带**引出线标注** + 排行条）
- **时段选择弹窗** `PeriodPicker.vue`：`picker-view` 双列滚轮，月/年模式切换
- **排行条** `RankList.vue`：默认 5 条 + 点击展开；行内「名次+图标+名称+占比·金额」+ 占比进度条
- **折线图** `TrendChart.vue`：uCharts（`qiun-data-charts`）封装，双线 + 触摸 tooltip
- **CSV 导出**：底栏「导出报表」，导出当前 Tab + 当前时段（H5 Blob 下载）
- **三态统一**：加载中 / 请求失败（带重试）/ 空数据（该时段无记录）
- **宽屏限宽**：`@media (min-width: 600px)` → `max-width: 480px` 居中（仅本页，独立页无底栏）

### 死代码清理

- 删除 `components/views/StatisticsView.vue`（旧单页统计，被 ReportView 取代，12 处 `$font-*`）
- 页面 `pages/statistics/index.vue` 改为只承载 `ReportView`

### 吸顶

「顶栏 + Tab」用 `position: sticky; top: 0` 吸顶；日期栏**不**吸顶（随内容滚走）。
实测滚动后 `stickyTop` 保持 0、`periodTop` 变负（= 滚走）。

### 踩坑（4 个）

1. **`easycom` 覆盖不到 `qiun-data-charts`**：默认 `autoscan` 只认 `^uni-(.*)`，
   实测报 `Failed to resolve component`，**canvas 静默空白、不报错**。
   修法：`pages.json` 显式加 `"^qiun-data-charts$"` 规则；**改完必须重启 dev server**（easycom 只解析一次）。
2. **`vue-tsc` 被第三方 `.vue` 顶爆**（TS6504）：`frontend/tsconfig.json` 的 `exclude` 加 `src/uni_modules`。
3. **`check-contrast` 把第三方组件当自家代码扫**：`uni_modules` 里的 `#333`/`font-size:14px`/`outline:none`
   全被算成违规（130 处旧色值、133 处字面量）。修法：三个 `.vue` 遍历函数都跳过 `uni_modules`。
   这不是放水 —— 第三方组件**不受本项目 token 体系约束**，扫它只会制造噪音。
4. **编辑工具写入 CRLF 污染脚本**：`regression-test.sh` 被插入 CRLF 行后，
   `curl -w "%{http_code}"` 的返回值尾部带 `\r`，`[ "$2" = "$3" ]` 判等失败 → 6 个用例假失败
   （**看起来像产品 bug，实际是行尾符**）。统一 `perl -i -pe 's/\r\n/\n/g'` 归一。

### 验证（全绿）

- 单测 **107 → 113**（新增 `report` 用例：年/月粒度、12 月补 0、分类聚合、空时段、隔离）
- 回归 **47 → 52**（新增 `report` 年/月/坏参数/无 token/按账本 5 例）
- `check:contrast` **155/155**（字号计数按实订正 104/102 → **119/117**，已追加订正（十））
- `reflow-audit` **24 屏 0 不合规**（首跑抓到 report 页 320×2 下排行行溢出 R=351，
  用 `flex-wrap: wrap` + `margin-left:auto` 修复 —— 与旧图例同一套做法）
- `verify-report` **21/21**（新增脚本：顶栏/Tab/弹窗/年粒度趋势/canvas/环形图/导出/吸顶）
- ESLint **0**、prettier 通过、`vue-tsc` **0**、`build:h5` 通过
- Swagger 实测 **26 操作 / 17 路径 / 5 分组 / 61 模型**

### 用户复查后追加的两处修复

**① 默认时段：基础统计看年、分类看月。**

两个 Tab **各自记忆时段**（`tabState`），默认值刻意不同：
基础统计看「走势与结构」→ 默认**当年**（趋势图 12 个月才有意义）；
分类看「这个月花在哪」→ 默认**当月**。切 Tab 用各自的时段重新拉数据，互不干扰。

**② 环形图引出线的文字不显示（根因是模板标签被框架改写）。**

实测 DOM 抓出来的真相：

```
<polyline points="..." stroke="#c2410c"/>                        ← 正常渲染
<uni-text x="230" y="211" ...><span>未分类 89.27%</span></uni-text>  ← SVG 不认识，空白
```

**uni-app 会把模板里的 `<text>` 编译成 `<uni-text>` 自定义元素，而 SVG 不认这个标签。**
于是引出线（`polyline` 是原生 SVG 标签）画出来了、文字却完全不渲染，
**不报任何错**，build / vue-tsc 全绿也发现不了 —— 只有真实浏览器里能看出来。

修法：文字改为 **HTML 覆盖层**（绝对定位 `<view>`），用 `calc(50% ± Npx)` 相对容器中心定位
（不写死像素：容器宽度随屏幕变化，写死要么窄屏溢出被 `overflow-x:hidden` 静默裁掉、要么宽屏离环太远）。

**③ 名称可截断、百分比必须完整。**

不能用 `{{name}} {{ratio}}%` 一行文本 —— 那样两者会被一起截断（"60.18…"），
而百分比截断比名称截断严重得多。改用 flex 让**名称可压缩（`flex: 0 1 auto` + `min-width: 0`）、
百分比不可压缩（`flex: none`）**；左侧标签加 `flex-direction: row-reverse`，
让阅读顺序始终是「由环向外」。

375px 主目标下名称可用宽度实测 **63px**（≈5 个汉字），320px 最低支持下降为约 3 字但**百分比始终完整**。

**验证**：`verify-report` 从 21 项扩到 **25 项**（新增：标注文字已渲染 / 百分比完整无截断 /
名称可用宽度 / 标注未越出容器）；`reflow-audit` **24 屏 0 不合规**；
`check:contrast` **155/155**（字号计数再订正 119/117 → **120/118**）。
**这两条断言都做过负向验证**：文字没渲染时"标注=0"立即失败、可用宽度断言一度写错
（`parseFloat("calc(…)")` 返回 NaN）也确实报了失败，改对后才转绿。

**④ 用户第二轮复查：边距过大 + 左侧标签顺序反了。**

- **边距**：SVG 外扩量 `PAD` 24 → **16**（文字是 HTML 覆盖层、不占 SVG 空间，这个值只管引线）；
  面板上下留白 `$space-3` → `$space-1`；环直径 90 → **130**（引线同步收紧：外推 6→4px、水平段 10→6px）。
  实测 SVG 画布 132px → **162px**。
- **左侧标签顺序**：我之前给 `.ring-label.left` 加了 `flex-direction: row-reverse`，
  本意是"名称贴环"，实际把左侧变成了「百分比 名称」，与右侧不一致（参考图两侧都是名称在前）。
  **直接删掉该规则**，两侧统一为「名称 → 百分比」。
- **新增断言**「标注阅读顺序均为『名称 → 百分比』」—— 这条正是为上面这个 bug 写的：
  它盯的是"左右两侧结构一致"，而 `row-reverse` 恰好只破坏了一侧。
- 可用宽度断言改为**动态推算 gap**（从「标签左边界 − 容器中心」反推），
  不再硬编码 61 —— 硬编码值在把 size 从 100 调到 130 后立即失效，
  第一次跑就报了 `❌ 可用=37px`。阈值随之下调到 46px（≈4 汉字）。

### 流水页 + 日历页（新增，2026-09-14）

**需求**：底栏「记账」改为「流水」并改为**页面跳转**；流水页参考图实现，
顶栏四个图标分别是：① 底部弹窗 ② 日历页 ③ 快速搜索 ④ 记一笔。

**后端扩展（列表接口新增 6 个能力 + 1 个新接口）**

| 能力 | 参数 |
|---|---|
| 分类多选 | `categoryIds=a,b,c`（**传一级会连带命中其下二级**，与统计口径一致） |
| 关键词 | `keyword`（匹配**备注或分类名**） |
| 金额区间 | `minAmount` / `maxAmount` |
| 排序 | `order=time\|amountDesc\|amountAsc` |
| 分组汇总 | **新接口** `GET /transactions/summary?unit=year\|quarter\|month\|week\|day` |

**关键设计：列表与汇总共用同一套筛选。** 抽成模块级 `applyFilters()` ——
两处各写一遍必然出现"筛选了列表却没筛选汇总"，且改一处漏一处。

**踩坑 ①：`key` 是 MariaDB 保留字。** `SELECT ... AS key` 直接语法错误，
接口 500。用 `groupKey` 作别名、返回时映射回 `key`。
> 教训：SQL 别名不要用可能撞保留字的短词（`key`/`order`/`desc`…）。

**踩坑 ②：路由顺序 —— `/summary` 必须在 `/:id` 之前。** 反过来的话
`summary` 会被当成 id 传给 BIGINT 列 → 500。已按此顺序注册并加注释。

**踩坑 ③：编辑工具又写 CRLF**（第三次），导致 6 个回归用例假失败 + 一次
`edit` 找不到文本。归一后恢复。

**前端新增 3 个文件**：`pages/flow/index.vue`（主页面）、`pages/calendar/index.vue`（日历）、
`components/FlowFilterPanel.vue`（筛选面板）；另加 `utils/period.ts`（分组键 ↔ 日期区间，
必须与后端 `GROUP_FORMAT` 完全一致，尤其是 **ISO 周**）。

**决策记录（用户逐条确认）**：
- 流水页**不带主导航底栏**（只有筛选栏），靠左上角返回
- 头图用**渐变底 + 白字**（`$gradient-banner`，白字最差端 4.52:1 ✅）
- 主列表用**分组汇总接口**，展开时才拉该组明细
- 弹窗只做**导出 / 筛选 / 排序**三项（「流水自定义」「复制跨账本」不做）
- 筛选面板只保留**时间 / 分类 / 金额 / 备注**（去掉项目没有的账户/成员/商家/项目）
- 分类筛选**多选**；两个关键词（顶部搜索、筛选面板）**独立**，都搜「备注 + 分类名」，
  冲突时**筛选面板优先**
- 日历格子显示**两行：上行支出、下行收入**（只有支出时一行）
- 日历页含 **FAB**（本页无底栏，不与凸起项重复）
- 底部筛选栏只做「月 / 分类」两项

**修的两处 reflow 溢出**：
- `flow` 320×2：头图「15,852.80 结余」一行放不下（R=329）→ `flex-wrap`
- `calendar` 三档全中：格子写死 `width: 44px`，而 320px 下每格只有 43.4px →
  改流式 `width: 100%` + `max-width: 44px`。
  **且我第一版修错了**：加了 `overflow: hidden` + `text-overflow: ellipsis`，
  但 uni-app 把 `<text>` 渲染成 inline `<span>`，ellipsis 不生效 →
  变成"被祖先静默裁掉"（三个视口全报「被裁」）。正解是**让金额本身更短**
  （`shortMoney` 分档：≥1万→"1.17万"、≥1000→"3.3k"、≥100→取整、<100→1位小数）。
  > 又一次印证：**"被裁掉"在本项目是最危险的溢出形态**（屏幕上没有、scrollWidth 测不到）。

**新增验证脚本** `scripts/verify-flow.mjs`（23 项），覆盖：分组展开（含按日分组）、
更多弹窗、粒度切换、分类多选、搜索、日历页（格子/金额/FAB）。
其中搜索那条我第一版写成 `groups >= 0`（永远为真），已改为"进入确定结果态
（有分组 或 空状态，且非错误/加载）"。

**验证**：单测 **114 → 124**、回归 **55 → 67**、`reflow-audit` **27 屏 0 不合规**、
`verify-flow` **23/23**、`verify-report` **27/27**、`check:contrast` **155/155**
（字号计数 120/118 → **163/161**）、ESLint 0、prettier ✓、`vue-tsc` 0、`build:h5` ✓。

### 主容器底栏去掉「我的」（4 格 → 3 格，凸起项终于居中）

**需求**：选完账本后跳转的页面（`pages/main/index`）底栏不要「我的」。

**改动**：
- `TabBar.vue` 的 `DEFAULT_ITEMS` 从 4 格改为 **3 格**（记账 / 记一笔 / 报表）
- `pages/main/index.vue`：`VALID_KEYS` 收敛为 `['home']`，删除 `MineView` 引用、
  `mineRef`、`?tab=mine` 分支、`scrollPositions.mine` —— **不留"没有入口的视图"**
- 「我的」入口保留在**账本选择页**（启动页）的底栏，功能不丢

**顺带解决了一个老问题：「记一笔」居中。**

4 格时它落在 62.5%（距正中偏移 46px），这是"3 视图 + 1 动作"的几何必然，已在 MEMORY 记为
"别去调它"。现在 3 格，槽中心是 16.7% / **50%** / 83.3%。

⚠️ **但我第一次实现时搞错了**：把「记一笔」放在最后一格，实测落在 **83.3%**，
**比 4 格时还偏**。浏览器实测数据打脸后才发现：

> **居不居中取决于"它排第几"，不是"总共几格"。**
> 3 格并不会自动居中 —— 凸起项必须排在**正中间那一格**。

调序为「记账 / 记一笔 / 报表」后实测 `raisedCenterPct = 50`，真正居中。
这条已写进 `TabBar.vue` 的注释与 MEMORY，避免下次又被"格子少就居中"的直觉骗。

**另一个必须同步的地方：验证脚本的页面清单。**

`reflow-audit.mjs` 里的 `['mine', '#/pages/main/index?tab=mine']` 已失效 ——
`VALID_KEYS` 只剩 home，这个 URL 会**静默落到首页**：不报错、量的是首页却记为 mine，
正是项目反复踩过的"巡检到错页还全绿"。`diagnose-scroll.mjs` 里的 `['我的','mine']` 同理
（点不存在的格子什么都不会发生，却仍输出一份"零溢出"的漂亮结论）。两处都已同步，
`reflow-audit` 从 **24 屏 → 21 屏**。

> 已知覆盖缺口：「我的」现在只存在于账本选择页的**页内视图**（URL 不可寻址），
> 这两个脚本都不再覆盖它。要补需在巡检里先点一次底栏「我的」，属独立任务。

**踩坑（TS）**：`ViewKey` 收敛成单字面量 `'home'` 后，
`Record<ViewKey, boolean>` 被 Vue 的 `UnwrapRef` 推成 `never`（TS2322 实测）。
改用 `Record<string, …>` 并把原因写进注释 —— 保留"将来加视图只改 VALID_KEYS"的可扩展性。

**验证**：`vue-tsc` 0、ESLint 0、单测 **114/114**、回归 **55/55**、
`check:contrast` **155/155**、`reflow-audit` **21 屏 0 不合规**、`verify-report` **27/27**。

### 分类 Tab 精确到二级分类

**需求**：分类 Tab 下的统计要精确到二级（基础统计 Tab 保持一级）。

参考图里两个 Tab 的粒度本就不同：图 4（基础统计）的支出分布是「金融保险 26.52%」这类**一级**，
图 5（分类）的支出分类统计是「私家车费用 60.18%」这类**二级**。所以后端要**同时产出两级口径**。

**做法：一次查询产出两级，不在 SQL 里查两遍。**

SQL 按「叶子分类」分组（挂二级就是二级、直接挂一级就是它自己、未分类为 NULL），
再在 JS 里把叶子按 `parentId ?? leafId` 归并出一级。为什么不查两遍：
两遍 SQL 在大表上重复扫描，而且两次结果之间若有写入会导致口径对不上（一级之和 ≠ 二级之和）；
一次查询、一次聚合天然自洽。

接口新增两个字段（旧的 `expenseCategories` / `incomeCategories` 保持一级语义不变）：

```
expenseCategories     一级  基础统计 Tab 的「支出分布」
incomeCategories      一级  基础统计 Tab 的「收入来源」
expenseCategoriesL2   二级  分类 Tab 的环形图与排行
incomeCategoriesL2    二级  分类 Tab 的环形图与排行
```

二级条目额外带 `parentId` / `parentName`（所属一级），一级条目这两项恒为 null。
**两级各自的 ratio 之和都是 100%** —— 同一批交易换了个分组方式而已，已写进单测。

**实测（demo / 2026 年）**：

| 一级 | 金额 | 二级 | 金额 | 所属一级 |
|---|---|---|---|---|
| 居家物业 | 158.80 (41.99%) | 未分类 | 131.40 (34.74%) | — |
| 未分类 | 131.40 (34.74%) | 水电煤气宽带 | 99.00 (26.18%) | 居家物业 |
| 行车交通 | 88.00 (23.27%) | 私家车费用 | 88.00 (23.27%) | 行车交通 |
| | | 日常用品 | 59.80 (15.81%) | 居家物业 |

**前端**：分类 Tab 的环形图、排行列表、CSV 导出全部改用二级口径（CSV 多一列「所属一级」）；
基础统计的排行仍用一级。笔数用二级之和（两级 count 之和相同）。

**验证**：单测 113 → **114**（新增「两级口径」用例：二级带 parentName、一级由二级归并、
未挂父的二级 parentId 为 null、两级占比之和均为 100%）；回归 52 → **55**；
`verify-report` 26 → **27**（新增「分类 Tab 使用二级口径」——直接从接口取二级分类名，
检查页面上真的出现了它们）。
