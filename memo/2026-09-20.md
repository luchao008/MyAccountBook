# 2026-09-20 · 中台模板清理 + 用户列表按钮修复

> 承接 09-19。本会话做两件事：清理 Vben Admin 模板残留、修复中台用户列表
> 操作按钮「有属性无样式」的问题。均为 luchao 实测反馈驱动。

## 一、本次提交（2 个）

| 提交 | 内容 |
|---|---|
| `8ea0be3` | feat(admin-web): 中台清理模板残留并修复认证/展示问题 |
| `bf498e4` | fix(admin-web): 用户列表操作按钮显式 import 组件并做三色区分 |
| `6ddac55` | docs(memo): 2026-09-20 交接文档 |
| `d7274d1` | feat(deploy): 中台（admin）纳入 NAS 部署 |

分支：`develop`（提交前后均确认过，未再误提交到 main）。

## 二、后端修复：Midway v4 ctx 注入（8ea0be3）

**文件**：`src/admin/admin-auth.controller.ts`

**问题**：`async me(@Inject() ctx: Context)` 在**方法参数位置**用 `@Inject()`。
Midway v4 没有 `@Ctx` 装饰器，方法参数的 `@Inject()` 被当作「按名字注入依赖」，
触发 `MidwayDefinitionNotFoundError`（`Definition for "[object Object"`）。

**修复**：改为**属性注入** `@Inject() ctx: Context;`，方法内用 `this.ctx.admin.adminId`。
已加注释说明此坑。

## 三、中台模板残留清理（8ea0be3）

### 3.1 删除的路由模块 / 页面

- 路由：`router/routes/modules/demos.ts`、`router/routes/modules/vben.ts`
- 页面：`views/demos/antd/index.vue`、`views/dashboard/workspace/index.vue`、
  `views/dashboard/analytics/` 下 5 个 analytics-*.vue

### 3.2 /analytics 改为欢迎页

**决策**：保留 `/analytics` 路径（因 `DEFAULT_HOME_PATH='/analytics'` 定义在共享包
`packages/constants`，且后端 `homePath: '/analytics'`），改为极简欢迎页。

- `router/routes/modules/dashboard.ts`：从「概览（分析页+工作台）」改为**只留欢迎页**。
  菜单父级 name=`Home` title=`page.home.title`；子项 name=`Welcome` path=`/analytics`
  `affixTab: true`（固定首标签）icon `lucide:home`
- `views/dashboard/analytics/index.vue`：只用 `<Page title="记账应用中台" description="请从左侧进入用户管理" />`
- locales：`page.json` 的 `dashboard` 段（title/analytics/workspace）→ `home` 段（title/welcome）

### 3.3 用户下拉 + 通知/锁屏清理

**文件**：`layouts/basic.vue`（158 → 62 行）

删除：
- `menus` 数组（「文档 / GitHub / 问题&帮助」三项）及相关 import
- 假通知铃铛（4 条假数据 + Notification 组件 + #notification slot）
- #lock-screen slot + LockScreen import
- 假邮箱 `ann.vben@gmail.com`、`tag-text="Pro"`

保留：UserDropdown（:avatar/:text/@logout）、AuthenticationLoginExpiredModal、水印逻辑。

**文件**：`preferences.ts` 关闭部件
```ts
widget: {
  notification: false,   // 中台 v1 无通知体系
  lockScreen: false,     // 中台 v1 无锁屏体系
},
```

> 顶栏模板按钮（搜索/偏好齿轮/主题/语言/全屏）luchao 明确说"不用了"，**未动**。

### 3.4 注册时间列格式化

`list.vue`：`createdAt` 列加 `formatter: 'formatDateTime'`。

## 四、⚠️ 操作按钮「有属性无样式」的根因（bf498e4）

### 现象

用户列表操作列按钮无论 `ghost` 还是 `primary` **都没有任何样式**；
DevTools 里 `<a-button>` 被渲染成**原生未知元素**（`custom-element`），属性在但样式不生效。

### 根因（关键）

**Vben Admin v5 的 `bootstrap.ts` 只 `app.use(router)`，不全局注册 ant-design-vue。**
所以模板里写 `<a-button>`/`<a-space>` 这种 kebab 标签，Vue 解析不到组件，
fallback 成原生标签 → 无任何 antd 样式。对比状态列的 `<Tag>` 因**在 script 里显式
import** 所以正常。

### 修复

```ts
// list.vue script
import { Button, message, Modal, Space, Tag } from 'ant-design-vue';
```
模板改用 PascalCase `<Button>` / `<Space>`。

### 配色（luchao 要求两个按钮不同色）

ant-design-vue 4.2.6 的 Button **没有 `warning` prop**（`type` 只支持
primary/default/dashed/text/link，危险色只有 `danger` 布尔）。最终三色方案：

| 操作 | 颜色 | 实现 |
|---|---|---|
| 通过 / 启用 | 蓝 | `type="primary"` |
| 停用 | 橙 | 内联样式，项目 warning token `hsl(42,84%,61%)` |
| 驳回 / 删除 | 红 | `type="primary" danger` |

> 教训：**Vben v5 模板里的 antd 组件必须显式 import，不能靠 kebab 标签。**

## 五、环境与踩坑（本机）

| 项 | 值 |
|---|---|
| node | `/usr/local/bin/node`（v24.14.1） |
| npm / pnpm | `/usr/local/bin/npm`、`/usr/local/bin/pnpm` |
| 默认 PATH | **不含** /usr/local/bin → 须 `export PATH=/usr/local/bin:$PATH` |
| admin-web dev | 端口 **5666**（`pnpm dev`，vite v6） |
| 后端 | 端口 **7001**（`mw dev --ts`） |
| App 端 uni-app | 端口 5173（`frontend` 的 `npm run dev:h5`） |
| 数据库 | MariaDB `127.0.0.1:3306` root/root123456 db=account_book |
| admin typecheck | `export PATH=/usr/local/bin:$PATH && cd admin-web/apps/web-antd && pnpm run typecheck` |

**坑**：
- 项目自带 `grep` 工具报 `Could not find @vscode/ripgrep-darwin-x64` → 改用 `bash("grep")`
- 本机 `docker` 命令不可用
- `test/` 下**无任何 admin 用例**（`grep 'admin' test/` 无匹配）；11 个测试全是 App 端

## 六、管理员账号

- 用户名：`ops_admin`（nickname 运维管理员，id=1）
- 密码：本会话重置为 `ollie008`
- 重置命令：`npm run admin:create -- ops_admin <新密码>`（幂等：存在则重置密码并置 active）
- 密码 bcrypt 哈希存储，无法反查；重置脚本源码 `src/scripts/admin-create.ts`

## 六点五、中台纳入 NAS 部署（d7274d1）

**背景**：admin-web 此前**完全没有纳入部署流程**（deploy/ 只涉及 App 前端 + 后端 + DB），
本次从零设计并落地。luchao 选定方案：

| 决策 | 选择 |
|---|---|
| 部署形态 | 独立端口（admin 单独容器 + nginx），不复用 App 前端的 nginx |
| API 访问 | 同域反代（admin nginx 把 /api 反代 backend:7001），**不依赖 CORS** |
| 端口 | 8081（App 是 8080） |
| 应用标题 | 「记账应用 · 管理后台」 |
| 构建组织 | 改造 build-and-export.sh，一次出三个镜像（统一入口） |
| compose 文件 | 沿用 NAS 上在用的 `docker-compose.nas.yml`（硬编码方式），已补 admin 服务 |

**新增文件**：
- `deploy/Dockerfile.admin`：nginx:alpine，拷 `admin-web/apps/web-antd/dist`
- `deploy/nginx.admin.conf`：托管静态 + `/api` 反代 backend

**修改文件**：
- `deploy/docker-compose.yml` + `docker-compose.nas.yml`：加 `admin` 服务
- `deploy/.env.example`：加 `ADMIN_PORT=8081`
- `deploy/build-and-export.sh`：加中台构建（`pnpm build:antd`）+ admin 镜像导出，
  并显式补 `export PATH=/usr/local/bin:$PATH`
- `deploy/README.md`：补中台部署、地址、`admin:create` 初始化
- 根 `.gitignore`：加例外纳入 4 个 admin `.env`（原本被 `.env`/`.env.*` 规则忽略，
  **从未被跟踪** → 构建配置无法复现，这次修正）
- `admin-web/apps/web-antd/.env`：标题改「记账应用 · 管理后台」
- `admin-web/apps/web-antd/.env.production`：API 地址由模板 mock
  （`https://mock-napi.vben.pro/api`）改为 `/api` —— **不改的话生产构建指向 mock 服务器**

**构建产物**（`deploy/out/`，2026-09-20 20:04）：
`backend.tar`(473M) / `frontend.tar`(68M) / `admin.tar`(64M)

**验证**：
- 后端镜像内含 `dist/admin/` + `dist/scripts/admin-create.js` ✅
- admin 镜像内含静态产物 + 正确 nginx 配置 ✅
- admin-web 产物标题 = 记账应用 · 管理后台；`_app.config.js` 中 API = `/api` ✅

**NAS 操作要点**（传给 luchao，未代执行）：
- 传 `out/{backend,frontend,admin}.tar` + `docker-compose.nas.yml` 到 NAS
- `docker load -i backend.tar && docker load -i frontend.tar && docker load -i admin.tar`
- `docker compose -f docker-compose.nas.yml up -d`
- 中台管理员初始化（中台**无注册入口**，必须脚本建）：
  `docker compose exec backend node dist/scripts/admin-create.js ops_admin <密码>`
- 访问：App `http://NAS_IP:8080`，中台 `http://NAS_IP:8081`

> ⚠️ `docker-compose.nas.yml` 里密码是明文 `ChangeMe_123456` 且已入库，
> 若 NAS 实际在用，建议改密码或迁到 .env 方式。

## 七、本会话验证记录

| 项 | 结果 |
|---|---|
| 后端 `tsc --noEmit` | 0 |
| admin-web `vue-tsc --noEmit` | 0（多次） |
| dev server 编译校验 | `curl :5666/src/views/user/list.vue` 确认 $setup["Button"] |
| 后端 7001 | 在线（未带 token 返回 401，正常） |

## 七点五、⚠️ 生产时区偏移一天（5bf4299）

**现象**（luchao 在 NAS 实测反馈）：查 9月20日的流水，出来是 19日；
填 19日保存后变 18日 —— **整体偏移一天**。本机开发完全正常。

**根因**（又一个"开发全绿、生产才暴露"）：
- 后端 DB 连接串配了 `timezone: '+08:00'`（config.default.ts）
- 读 DATE 列时，mysql2 驱动把它解析成 UTC 瞬间值
  （DB 里 `2026-09-20` → JS `2026-09-19T16:00:00.000Z`）
- TypeORM 再用**进程本地时区**取年月日
- 本机 `TZ=Asia/Shanghai(+08)` 恰好抵消 → 正确
- **NAS 容器（node:20-slim）默认 `TZ=UTC`** → 取成 09-19 → 偏移一天

**定位过程**（三步缩小范围）：
1. `DATE_FORMAT(record_date,'%Y-%m-%d')` 直查 DB → 存储值**正确**（`2026-09-20`），
   说明 bug 在读不在写
2. 本机 TypeORM 读写测试 → 正确；**TZ=UTC + TypeORM 读回 → `"2026-09-19"`** ✅ 复现
3. 生产容器 `docker run ... sh -c 'node -p "new Date().toString()"'` → `GMT+0000` 坐实

**修复**：
- `deploy/Dockerfile.backend` 加 `ENV TZ=Asia/Shanghai`
- 两个 compose 的 backend 服务也显式加 `TZ: Asia/Shanghai`（双保险）
- 已用生产容器端到端验证：POST/GET 的 recordDate、recordTime 均正确

**关键认知**：**数据本身存储无误**（DB 里就是 2026-09-20），只是读取时算错 →
**改完容器时区后旧数据的"错乱"显示会自动恢复**，不需修数据。

> 与 09-18 §9.1 第 6 条（实体扫描失效）同类：**dev/test 全绿，只有生产镜像暴露**。

## 八、仍遗留

1. **模板残留未清完**（本次按 luchao 意愿只做最小清理）：
   - `router/routes/core.ts` 的 `/auth` children 仍挂 code-login/qrcode-login/
     forget-password/register 及对应 `views/_core/authentication/*`
   - `page.json` 的 `auth` 段冗余项、`demos.json`（引用方已删）
   - `views/_core/about/`、`fallback/coming-soon.vue`
2. **NAS 已部署，但需重传 backend.tar**（时区修复 5bf4299）：
   已重新导出 `deploy/out/backend.tar`（20:32）+ `docker-compose.nas.yml`（加了 TZ），
   NAS 上 `docker load -i backend.tar && docker compose -f docker-compose.nas.yml up -d backend`
   → 之后 App 的日期显示即恢复正常（旧数据自动恢复，无需修数据）
3. **NAS 中台管理员需手动创建**（中台无注册入口）：
   `docker compose exec backend node dist/scripts/admin-create.js <用户名> <密码>`
4. **本机未做 compose 全栈端到端验证**（仅单容器验证了时区；
   如需可在本机起 8080/8081 全套）
5. 长期：深色模式 / 顶部安全区 / 键盘 Tab 导航 / CI
