# 2026-09-12

> 09-11 深夜工作的延续（跨午夜），完整上下文见 `memo/2026-09-11.md` 第二十五节及 `.workbuddy/memory/2026-09-11.md`。

## 记一笔分类选择器：改为左右联动（已完成）

用户要求交互与参考图一致：

- **右侧显示全部二级分类**（按一级分组滚动展示，最近使用置顶）
- **滚动右侧反推左侧高亮**（scroll spy）
- **点击左侧一级，右侧定位到对应分组**
- 仍然只能选二级分类

## 实现要点

- 右侧 `scroll-view` 加 `@scroll` 监听 + `scroll-into-view` 定位；
  每个分组设锚点 id（`group-anchor-<rootId>`），左侧 `side-anchor-<rootId>`。
- **锚点测量**：`uni.createSelectorQuery().in(getCurrentInstance())`，
  取各分组与滚动容器的 `boundingClientRect`，相减得到相对内容顶部的偏移
  ——该值不随滚动变化，**只需在弹窗打开后测一次**。
- **滚动反推**：`scrollTop >= anchorTops[i] - 8`（8px 容差防边界跳动），
  取满足条件的最大 i 作为当前组。
- **双向定位都要"先清空再赋值"**：`scroll-into-view` 值不变不会重新触发滚动，
  必须 `mainIntoView=''; nextTick(() => mainIntoView='group-anchor-x')`。
- 左栏跟随高亮项滚动（同样用 scroll-into-view）。
- 搜索模式切到独立结果列表（不做联动），退出搜索后重新测量。

## 踩坑

- **watch(groups) 会频繁触发**：groups 是 computed，每次求值都是新数组引用。
  改为 watch 一个字符串签名（`rootId:childrenLength` 拼接），只有结构变化才重新测量。
- 弹窗打开后测量要等渲染完成：`await nextTick()` + `setTimeout(120ms)` 兜底。

## 验证

`build:h5` 通过；H5 与 record 页面模块均 200。后端未改动（lint/tsc/94 单测此前已全绿）。

## 补充：滚动条——左侧隐藏、右侧自绘贴边

用户反馈：右侧滚动条被 `.main` 的 `padding: 12px` 挤进了内容区，不在最右侧。
**根因**：原生滚动条永远贴"滚动容器"的边缘，而 padding 在容器内侧，
所以只要容器有右 padding，滚动条就不可能贴到组件最右侧。

**解法：放弃原生滚动条，自绘**：

- `onMainScroll` 里本来就有 `scrollTop`，顺手记下 `scrollHeight`；
  `measureAnchors` 时顺便量出可视高度——三者齐了就能算滑块位置：
  `barHeight = viewport/scrollHeight * viewport`（最小 40px），
  `barTop = scrollTop / scrollable * (viewport - barHeight)`。
- 滑块是 `.content` 下的绝对定位元素（`right: 2px`），不受 `.main` 的 padding 影响。
- `pointer-events: none` —— 纯位置指示器，不拦截滚动手势。
- 原生滚动条沿用 App.vue 的全局隐藏规则（正好避免出现两根）。
- 加了 `transition: top 0.08s linear`，滑块移动更顺滑。

教训：**和框架内部 DOM 结构斗争不如换思路**。第一版试图用 CSS 选择器
精确命中 uni-scroll-view 的内层容器来"恢复"原生滚动条，位置不可控；
改为自绘后位置完全由自己的布局决定，反而更简单。

## 收入分类改为两级（已完成）

用户给了 2 张收入分类截图，要求与支出一样做两级。整理出 **2 个一级 + 19 个二级**：

- **职业收入**（8）：工资收入 / 利息收入 / 加班收入 / 奖金收入 / 投资收入 / 兼职收入 / 公积金提款 / 顺风车
- **其他收入**（11）：礼金收入 / 中奖收入 / 意外来钱 / 经营所得 / 信用卡还款 / AA还款 / 物品回收 / 房租收入 / 共享带宽收入 / 保险报销 / **其他**

「其他收入 → 其他」同时是收入侧的回退分类（旧收入分类清理时交易改挂它），
复用旧「其他」的名字正好接住历史归属（upsert 保留原 id）。

### 踩坑：seed 的二级循环只写了支出

收入二级**第一次没建出来**（每用户 69 而非 88）——`rebuildCategories` 的二级循环
只遍历了 `EXPENSE_CATEGORY_PRESET`。这个假设在收入扁平时代成立，收入也做两级后失效。

**修法**：二级循环同时遍历两个 preset，`childPlan` 加 `type` 字段（二级 type 必须与父一致）。

修后重跑：34 用户 × **88 个**分类（支出 67 + 收入 21），全部用户一致；幂等复验通过。

名字冲突检查（全局唯一键）：「房租收入」≠「房租」、「利息收入」≠「利息支出」、
「兼职收入」≠ 旧「兼职」——新收入侧与支出体系无撞名。

### 验证

ESLint 0 / 单测 **94/94** / 回归 **47/47**；前端无需改动（两级逻辑对收支通用）。

---

## 日期时间选择器 + 账单支持时刻（已完成）

参考图要求：日期从底部弹窗选（日历），并有「时刻」开关支持记录时间。

### 后端

- 迁移 `TransactionRecordTime1789144765914`：`transactions` 加 `record_time` TIME **可空**列。
  **刻意不动 `record_date`**——日期仍是 DATE，统计区间过滤与索引完全不受影响；
  `record_time` 只影响展示与同日内排序（MySQL DESC 排序中 NULL 靠后 = 没填时间的排后面）。
- DTO：`recordTime` 可选，正则 `^([01]\d|2[0-3]):[0-5]\d$`；传空串 = 清除（后端存 NULL）。
- 排序：`record_date DESC, record_time DESC, id DESC`。

### 前端 `DateTimePicker.vue`

- 日历：年月切换（‹ ›）、**周日起始**的星期行、今天显示「今」橙色圆底、选中实心橙
- 「时刻」开关：关 → 只选日期；开 → picker-view 时/分两列滚轮（打开时默认落在当前时间）
- 右上角「完成」一次性带回 `{date, time}`；`time=null` 表示不记录时刻

### 验证

- 带时刻创建 → 存 `12:30:00` 返回一致；不带 → null；`25:99` → 422 ✅
- 排序：同日有时刻的排在无时刻前面 ✅
- 单测 94 → **98**（新增 4 个 recordTime 用例）；冒烟测试数据已清理

---

## 文档一致性复核 + 首次 git 初始化（已完成）

修补上一轮挑出的 4 个问题，**无业务代码改动**。详细记录见 `.workbuddy/memory/2026-09-12.md`。

- **实测复核**：单测 **98/98**（6 suite）、回归 **47/47**、Swagger **23 操作 / 14 路径 / 5 分组 / 49 模型**、
  每用户 **88** 个分类（支出 13+54、收入 2+19）、ESLint 0 / tsc 0 / prettier 通过。
  > 「14 个接口」历史上指**路径数**，现在仍是 14 条路径但操作数已 23，写文档要区分。
- **订正**：规划文档 16 处（含 §4.2 `categories` DDL 补 `parent_id` + 外键、§4.3 补 2 个迁移、
  §8.2 收入 7 一级 → 2 一级 + 19 二级、§12 追加第十五次）；两份 docs 的过期数字；
  `category-preset.ts` 头注释。
- **git**：首次初始化 `main`，**107 文件**入库（无 node_modules/dist 泄漏）；
  `.gitignore` 补 `.env.*`、`unpackage/`、`.workbuddy/`。
- **环境坑**：bash 工具里 `nohup ... &` 起的服务会被杀，必须用 `run_in_background`。
- **未动**：CORS（同域反代部署不需要）。
- **补记（第二轮复查）**：又逮到 3 处漏网——§0 TL;DR 与 §2 技术选型表仍写 `ucharts`（实际是 `RingChart` SVG 自绘，
  已核 `frontend/package.json` 确认无任何图表依赖）；变更记录里第十四次还挂着「（本次）」而第十五次已存在；
  §0 未反映 git 已初始化。均已订正，并把「首次 git 初始化」并入第十五次条目。
  **教训：改文档要按「同一概念的所有出现位置」搜一遍，只改一处 = 制造新的不一致。**

---

## 移动端配色与字体规范（已完成，仅规范不含组件改造）

用户要求「仅针对移动端优化界面设计，聚焦配色与字体」。产出：

- `docs/移动端配色与字体方案.md`（规范正文 + WCAG 核对 + 迁移映射表）
- `frontend/src/styles/tokens.scss`（token 落地文件，**尚未被任何组件 import**）

### 体检结论（都是实测，不是目测）

| 发现 | 数据 |
|---|---|
| 裸色值 | 全 `src/` 40+ 个，「同义不同值的灰」5 个 |
| `uni.scss` 变量引用次数 | **0** —— 有 token 文件，但没人用 |
| 对比度不达标 | **11 处**（§1.2 表 ❌ 行数；当时误写为 9，后订正）；最严重是白字压主色按钮 `#FF6B35`（2.84:1） |
| 字体文件 | 无（唯一的 `.ttf` 是 uni-ui 图标字体）——现状正确，别加 |

### 方案要点

- **品牌橙拆三档**：`#FF6B35` 只做装饰/Logo/图表（白字 2.84 不达标）→ 按钮底 `#CF4A12`（4.52 ✅）
  → 橙色文字/Tab/焦点环 `#C7430F`（4.95 ✅）。品牌识别靠色相，换明度几乎看不出。
- **11 处修复**（原文档写"9 处"，实为 §1.2 表 ❌ 行数 11，已订正）：收入 `#52C41A→#0B8038`、支出 `#FF4D4F→#D92D20`、辅助 `#999→#6E7787`、
  链接 `#4A90D9→#1D63B8`、Tab 两色、按钮底。
- **图表 8 色换 7 色**：旧调色板的真凶是**暖色三兄弟互相打架**——`#FF6B35`↔`#F5A623` **17.1**、
  `#F5A623`↔`#FF4081` **23.0**、`#FF6B35`↔`#FF4081` **28.1**、`#BD10E0`↔`#50E3C2` **28.6**（阈值 40）；
  另有 3 对灰度不可辨（ΔL 0.001 / 0.007 / 0.043）。
  > ⚠️ 本轮初版曾写"原 `#7ED321`(青柠) 与 `#50E3C2`(薄荷) 色盲下几乎同色"——**已复核推翻**，实测距离 103.9，是区分度较高的一对。
  新序列用 Okabe-Ito 色相做底、贪心优化 + 人工选色相家族，**相邻段色盲可区分度最低 75.0**（阈值 70）。
  配套要求：扇区 2px 白色描边 + 图例含名称/金额/占比 + 最多 7 段（Top6 + 其他）。
- **字体**：系统字体栈（不引中文 Web 字体）；八档 12/14/15/16/17/20/28/36 px，**11px 全部抬到 12px**；
  字重只用 400/500/600（禁 700，CJK 伪粗体会糊）；行高正文 1.6。
- **等宽数字**：金额加 `font-variant-numeric: tabular-nums` + 右对齐 —— 记账 App 的关键细节。

### 踩坑 / 发现

- **优化器不会审美**：纯贪心最大化色盲距离，结果产出「两个洋红 + 两个天蓝」，功能合格观感灾难。
  最终做法是**人工挑色相家族、脚本只做验收**。
- **第一版图表配色自我否决**：品牌橙 `#C2410C` 与琥珀 `#B45309` 色盲距离仅 11.7（同一个颜色）。
- **文档里写数字前必须实测**：我一度在文档里写「`#0B8038` 压浅绿底只有 4.1:1」，实跑是 **4.52:1**；
  反倒是 `#D92D20` 压浅红底 4.22、`#C7430F` 压浅橙底 4.49（都差一点不达标）。
  **"背景一变，前景必须重算"** —— 这直接催生了独立的徽标色 token。
- **SCSS 块注释会进产物**：用 `/* */` 写 token 注释，编译后 3.6KB 注释原样打进 CSS；
  改 `//` 后产物 1 字节。校验方式：`npx sass tokens.scss out.css && wc -c out.css`。
- **验证 token 文件的方式有点特别**：它只有变量和 mixin，正确产物就是**空文件**，所以"能编译"不够，
  还得写探针 `.scss` 实际消费一遍变量与 mixin 才能证明可用。

### 未做（明确边界）

规范只定义「应该长什么样」，**没有改任何组件**。9 处对比度问题在代码里依然存在，
按文档 §7 分 9 步落地，其中阶段 1–4 是「只改色值、不动布局」的低风险批次。
> ↑ 这是**当时**的状态。同日后续已执行阶段 1–4（见本文件末尾「移动端配色落地：阶段 1–4」小节），
> 对比度问题已全部消除（实为 **11 处**，"9 处"这个数从初版起就是错的，已订正），阶段 5–9 仍未开始。

---

## 启动项目：前端 dev server 改绑 IPv4（已完成）

用户要求「把项目启动起来」。现状：MariaDB 容器在跑，7001 / 5173 均无监听。

### 启动方式（务必记住）

两个长驻进程都用**工具的 `run_in_background`**，不用 `nohup ... &`（会被杀）：

| 服务 | 命令 | 端口 | 说明 |
|---|---|---|---|
| 后端 | `npm run dev`（= `mw dev --ts`） | 7001 | 需先 `export PATH=/usr/local/bin:$PATH` |
| 前端 | `cd frontend && npm run dev:h5` | 5173 | 走 vite 代理 `/api` → 7001 |

### 改了一处配置：`frontend/vite.config.ts`

**问题**：Vite 默认 `host: 'localhost'`，在本机 Node 下**只监听 `[::1]`（IPv6 回环）**，
`curl http://127.0.0.1:5173` 直接 `http=000`。浏览器会自动回落所以肉眼看不出，
但**只探 IPv4 的工具/预览面板会连不上**（实测经 `127.0.0.1:5173/api` 打代理就是 000）。

**改法**：`server.host = '127.0.0.1'`（显式绑 IPv4），并加 `strictPort: true`
避免端口被占用时静默换到 5174、让代理配置与实际端口对不上。

> 经验：**dev server 起来了 ≠ 你能连上**。判断服务是否可用要分别探 `127.0.0.1` 与 `localhost`，
> 只探一个很容易被 IPv4/IPv6 绑定的差异骗过去。

### 端到端验证（不是只看端口）

- 后端：`/swagger-ui/index.json` 200；未授权访问 `/api/accounts` 正确返回 **401**
- 登录 `demo` / `123456` → 拿到 169 字符 JWT
- 账本列表 `code:0`，**2 个**账本；分类接口返回 **88 条**；`monthly?month=2026-09` 返回
  `income 16000.00 / expense 147.20 / balance 15852.80`
- 经**前端 5173 代理**再走一遍登录 → 拿到同样的 token（代理链路通）

---

## 配色校验器入库 + 文档自纠（已完成）

用户提出约定：**生成的 md 文档都放在项目目录中**。核查：项目内 6 个 md（根 1 + `docs/` 3 + `memo/` 2），
`/tmp` 下**没有任何 md**——约定本来就成立。但顺着这条线查出真问题：方案文档附录 A 声明
「校验脚本可按上述公式复现」，而那套脚本正躺在 `/tmp/palette/*.py` 里等重启被清空。

### 新增 `scripts/check-contrast.mjs`

- 零依赖 Node ESM，把文档发布的**每一个**对比度 / 色盲距离 / ΔL 都写成断言（当时 **61 项**，后于阶段 1–4 扩至 **98 项**）。
- 用法：`node scripts/check-contrast.mjs` 或 `npm run check:contrast`（已加入 `package.json`）；
  有 MISMATCH 时退出码 1，可当 CI 门禁用。
- 用 Node 重写而不搬运 Python：项目本就是 Node 栈、零新增依赖，且跨实现重算一遍本身就是交叉验证。

### 它首跑就抓出文档自身 10 处错误

1. **§5 深色模式整表数值全错**：表头写 `bg-card #24272E`，但 `13.39 / 8.43 / 6.19` 是对 `#1E2126` 算的，
   `3.26 / 1.26` 又是对 `#22252B` 算的——**一张表混了三个底色**。而这张表的正下方就写着
   「背景变了，前景必须重算」。已统一按 `#24272E` 重算（13.39→12.40、8.43→7.81、6.19→5.73、
   6.95→6.43、8.61→7.97、5.82→5.39、2.63→2.43、3.26→3.17、1.26→1.23）；色值本身不用动
   （层级 1.21、控件边界 3.17 仍达标）。
2. **§2.7 的论断是错的**：初版称「`#7ED321` 青柠与 `#50E3C2` 薄荷色盲下几乎同色」，实测 **103.9**
   ——属旧调色板里区分度**较高**的一对。真凶是暖色三兄弟（橙↔琥珀 17.1、琥珀↔品红 23.0、橙↔品红 28.1）。
   替换结论不变，依据订正。

### 教训（已固化）

- **"写进文档的实测值"若没有可执行脚本，就只是修辞。** 上轮把脚本留在 `/tmp` = 给文档配了个会自动消失的证据。
- **同一张表的每个数字必须共用同一基准底色**——我在"背景变了前景必须重算"这句话正下方犯了它。
- 校验器要断言**文档当前发布的值**，而不是断言"我以为对的值"；否则它永远不会打文档的脸。

---

## 移动端配色落地：阶段 1–4（已完成）

上一轮只出规范、没动组件。本轮按规范 §7 执行**阶段 1–4**——「只改颜色值、不动任何布局」的纯收益批次。
详细工程记录见 `.workbuddy/memory/2026-09-12.md` 同名小节。

### 做了什么

- **token 层真接上了**：`uni.scss` 顶部 `@use '@/styles/tokens.scss' as *;`。
  uni 插件的 `createCss()` 会把 `uni.scss` 全文当 `additionalData` 前置进每个 SCSS 块，
  所以**组件一个 import 都不用写**。代价：16 个 `<style>` 块要从纯 CSS 改成 `lang="scss"`。
- **219 处替换 / 15 个 `.vue`**：品牌橙按场景分流（装饰留 `#FF6B35`、实心底 `#CF4A12`、橙字 `#C7430F`）、
  5 个"差不多的灰"归一为 4 个、收入/支出/链接/Tab 全部换新色。
- **11 处对比度问题全部消除**（白字压主色按钮 2.84:1 → 4.52:1）。
  > 顺带订正：「9 处不达标」这个数从方案初版起就是错的，§1.2 表里实际有 11 行 ❌；
  > 校验器只断言对比度**值**、不管这个**计数**，所以一直没被抓到。现已把该计数也纳入校验器（第 12 节）。
- 图表色抽到 `frontend/src/constants/chart.ts`（JS 里取不到 SCSS 变量，只能双写 + 脚本比对一致性）。
- `pages.json` 的 tabBar 选中/未选中色同步。

### 三个只有动手才会发现的坑

1. **改完 `uni.scss` 必须重启 dev server**。`additionalData` 只在配置解析时读一次，
   热更新不会重读。症状特别唬人：`npm run build:h5` 一切正常，`npm run dev:h5` 报 `Undefined variable`。
2. **`opacity` 是隐形降级**。banner 和「我的」页头卡的说明文字用了 `opacity: 0.9 / 0.85`，
   白字 4.52:1 被压到 3.95 / 3.69:1。**颜色值达标 ≠ 观感达标**，已全部去掉透明度。
3. **`text-tertiary` 只能压白卡**。白卡 4.51:1 ✅，换到页面底只剩 4.17:1 ❌、浅底 3.98:1 ❌
   —— 上一轮文档里把 4.17 标成 ✅ 是错的，本轮订正。**背景一变，前景必须重算。**

另：日历「今天」`#ffb98a` + 白字是全项目最差的一处（**1.67:1**），已改 `#FFE3D6` + `#B33A0C`。
uni-ui 组件会用自己的 `$uni-*` 覆盖项目值（不带 `!default`），唯一生效的是登录页输入框边框
（`#dcdfe6`≈1.3:1 → `#8a94a6` 3.06:1 ✅）——这是库的限制，不是配置错误。

### 验证（全绿）

配色校验 **98/98**（61 → 98，新增 token 自洽 / SCSS↔TS 一致 / 旧色残留 / §1.2 计数 四项防护）、
ESLint 0、prettier 通过、`build:h5` 成功、单测 **98/98**、回归 **47/47**、dev 16/16 样式模块编译。

### 顺带修掉一个「从初版就错」的数字

方案文档 §1.2 表里实际有 **11 行 ❌**，但全项目一直写「**9 处**不达标」。
校验器只断言对比度**值**、从不检查这个**计数**，所以 61 项断言时代一次都没抓到过。现已全部改为 11，
并**给校验器加了第 12 节**自动比对「文中声明值 == 表内 ❌ 行数」——断言数 97 → 98。
（做了负向验证：把文档改回 9，脚本立刻 MISMATCH、退出码 1。）

> **教训：校验器要覆盖"计数"，不只是"数值"。** 一个从没报过错的校验器，可能只是因为它不知道该检查什么。

### 两条工具层面的教训

- **同一文件的多处编辑不能并行发**：一次消息里发两个针对同一文件的编辑，后一个会基于旧快照写入，
  **静默覆盖**前一个（两个都报成功，但回读是旧值）。**同文件修改必须串行 + 回读确认。**
- **Bash 里的 `grep` 对中文不可靠**（实测假阴性），裸搜 `frontend/` 还会超时被杀。
  中文检索用 Grep 工具（但它跳过 `.workbuddy/`），搜 `.workbuddy/` 用 Python。

### 还没做（阶段 5–9）

字号层级（11→12px）、金额 `tabular-nums` + 右对齐、环形图 Top6 + 「其他」聚合 + 白描边、
`:focus-visible` 焦点环与 ≥44px 触控目标、真机复验（320px / iOS / Android / 系统字号 200%）。
这批**会动布局与交互**，风险高于本轮，需要单独确认后再做。


---

## 移动端视觉落地：阶段 5–9（已完成）

上一轮做「只改颜色值」的阶段 1–4，本轮做**会动布局与交互**的阶段 5–9。
**详细工程记录见 `.workbuddy/memory/2026-09-12.md` 同名小节。**

- **阶段 5 字号归一**：114 处 `font-size` 全部 token 化，其中 **31 处渲染值真的变了**
  （11→12 ×6、13→14 ×16、17→16 ×1、18→20 ×4、22→24 ×3、26→28 ×1）。新增 `$icon-*` 图标阶梯 8 档。
- **阶段 6**：金额 `tabular-nums` + 右对齐（`@include amount` + `min-width` 撑定宽）。
- **阶段 7**：环形图 Top6 + 「其他」聚合；扇区分隔改用"缩短弧长露出底环"
  —— 文档原来的 `stroke="#FFFFFF" stroke-width="2"` **实测无效**（同心同宽被盖住）。
- **阶段 8**：焦点环改 `outline` 版（`box-shadow` 会被 `overflow:hidden` 裁掉）；12 处触控目标补足。
- **阶段 9 复验**：320px / 375px × 正常 / 200% 字号，7 页 **21 屏 0 屏不合规**。

### 修掉 4 类真问题

flex 项默认 `min-width:auto`、写死 `width`、`.banner-expense` 被 overflow 静默裁掉半截金额、
以及**统计页三栏金额互相压字**——最后这条是**阶段 5 的 18→20px 归一自己引入的**，被阶段 9 抓出来。

### 三个"漂亮的全绿"陷阱

1. `body` 有全局 `overflow-x: hidden`，**溢出不再表现为滚动条**，而是静默丢字 / 压字
   —— 这两种 `scrollWidth` 都测不到。判据必须分三桶。
2. 模拟「字号 200%」时 `getComputedStyle` **逐层复利**（给 html 写 32 → body 读到 32 写 64…），
   量出的是 2^深度 倍。**读和写必须分两趟。**
3. 登录按文字"登录"定位会**静默点空**（导航标题同名），七页全成登录页却报"零溢出"。

### 订正两个错数字（第三、四次了）

§1.3 的「共 8 档 / 11px 出现 3 处」→ 实测 **17 档 / 114 处 / 11px 6 处**；
规划文档 §0 残留的 `ucharts`（第十五次声称已改，实际漏了）。

> 同一个病：**校验器只断言"数值"、没人检查"计数"**。所以本轮把计数也变成了断言
> （`check-contrast` §13④：114 / 91 / 23 / 0），并做了负向验证。

**验证**：`check:contrast` **115/115**、ESLint 0、单测 **98/98**、回归 47/47、21 屏 0 不合规。
1 处已知接受：环形图中心注解（Ø132px 内孔 vs 198px 文本，几何无解，属重复标注）。
