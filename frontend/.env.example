# 复制为 .env.production 后按需修改

# 接口地址。留空则默认 /api，即前后端同域部署（Nginx 把 /api 反代到后端 7001）。
# 若前后端不同域，填完整地址，例如：
# VITE_API_BASE_URL=https://api.your-domain.com/api
VITE_API_BASE_URL=

# 开发代理的后端地址（见 vite.config.ts）
VITE_BACKEND=http://127.0.0.1:7001
