# 2026-09-17

## MyAccountBook：删除 workbuddy 子分支
- 用户要删除 `workbuddy/develop-3278bfc3`。该分支被 git worktree 检出在 `/Users/luchao/WorkBuddy/Worktrees/MyAccountBook/develop-3278bfc3`（git branch -a 中带 `+` 前缀即为被其他 worktree 检出）。
- 该分支无上游（未推送远程），工作树干净。
- 操作：`git worktree remove /Users/luchao/WorkBuddy/Worktrees/MyAccountBook/develop-3278bfc3` 移除工作树，再 `git branch -d workbuddy/develop-3278bfc3` 删除分支，最后 `git worktree prune` 清理元数据。
- 经验：workbuddy 相关开发分支使用 git worktree 管理，路径在 `/Users/luchao/WorkBuddy/Worktrees/MyAccountBook/` 下；删除此类分支需先移除 worktree 再删分支。
