# 个人记账应用 · 全栈技术规划与实施文档

> 后端：**Midway.js v4** · 数据库：**MariaDB 10.11** · 鉴权：**JWT** · 前端：**uni-app（Vue3，跨端）**
> 体验参考：**随手记（Suishouji）**
> **本文档已根据实际实施情况修订**，技术选型与依赖版本均以实测为准。

---

## 0. TL;DR

个人记账应用全栈方案。后端 Midway.js 4.2.3（IoC 模块化）+ TypeORM 0.3 + MariaDB 10.11（Docker）+ JWT 鉴权，覆盖流水 CRUD / 时间·分类筛选 / 月度汇总 / 分类占比 / 分类管理 / 注册登录。前端 uni-app（Vue3）一次开发多端发行，底部 Tab 四页（记账/明细/统计/我的），统计页用 ucharts 环形图。

**当前进度：后端功能 100%，回归 47/47、单测 98/98 全绿；Swagger、工程化三件套、联调文档均已完成（§6.5、§8.4、§8.5）；前端 uni-app（H5 + uni-ui）完整可用（§7.6、§8.2）。**

**多账本功能已完成**：可创建 / 删除 / 合并账本，当前账本可切换，存量数据已平滑迁入默认账本且老接口行为不变。设计详见 **`docs/多账本设计文档.md`**。

---

## 1. 产品定位与核心流程

### 1.1 产品目标
轻量、贴合随手记体验的跨端个人记账应用。

- 完整 CRUD 记账（收/支，金额/分类/日期/备注）
- 按时间范围 + 分类多维查询筛选
- 月度汇总（收入/支出/结余）+ 分类占比
- JWT 鉴权（多端/公网就绪）
- 前后端模块化、易扩展

### 1.2 核心流程（贴合随手记）
- **记一笔**：登录 → 记一笔 → 切收入/支出 → 选分类（图标网格）→ 输金额 → 选日期（默认今天）→ 填备注（可选）→ 保存 → 落库。
- **月报 / 占比**：登录 → 选月份 → 看收入/支出/结余 → 切分类占比 → 环形图展示 → 可按分类筛选。
- **登录取 Token**：提交账号密码 → 校验 → 签发 JWT → 请求带 `Authorization: Bearer <token>` → 守卫校验放行。

### 1.3 需求池（优先级）

| 诉求 | 模块 | 优先级 | 状态 |
|---|---|---|---|
| 流水 CRUD | transactions | P0 | ✅ 已完成 |
| 时间范围 + 分类查询筛选 | transactions | P0 | ✅ 已完成 |
| 月度汇总（收入/支出/结余） | statistics | P0 | ✅ 已完成 |
| 分类占比统计 | statistics | P0 | ✅ 已完成 |
| JWT 登录 + Token 中间件 | auth | P0 | ✅ 已完成 |
| 分类 CRUD | categories | P0 | ✅ 已完成 |
| 用户注册 | auth | P0 | ✅ 已完成（文档补充项） |
| 种子数据（测试用户 + 默认分类） | seed | P0 | ✅ 已完成（文档补充项） |
| 后端回归测试 | test | P0 | ✅ 已完成（47 用例全绿） |
| 前端跨端 App | uni-app | P0 | ✅ 已完成（H5 端，8 页面 + 7 组件） |
| 多账本（创建/删除/合并/切换） | accounts | P1 | ✅ 已完成 |
| 分类两级体系（支出 + 收入） | categories | P1 | ✅ 已完成 |
| 账单时刻（record_time） | transactions | P1 | ✅ 已完成 |
| 统计趋势线 | statistics | P1 | 未开始 |
| 多账户/资产、生物登录、离线记账 | accounts/auth | P2 | 未开始 |

---

## 2. 技术选型（实测版本）

| 维度 | 后端 | 前端 |
|---|---|---|
| 语言/运行时 | TypeScript 5.4 / Node.js ≥20（实测 v24.14.1） | TypeScript / Vue3 |
| 框架 | Midway.js **4.2.3** | uni-app（Vue3 + Vite） |
| 数据层 | TypeORM **0.3.20** + mysql2 3.9 | — |
| 数据库 | **MariaDB 10.11.19**（Docker） | — |
| 鉴权 | @midwayjs/jwt 4.2.3 | uni.setStorageSync 持久化 Token |
| 校验 | @midwayjs/validate 4.2.3（joi 17.13.4） | 组件内轻校验 |
| 状态 | 依赖注入容器 | Pinia |
| 图表 | — | 无第三方库，`RingChart.vue` SVG 自绘（原因见 §7.6） |
| 通信 | REST JSON | luch-request（拦截器） |
| CLI | @midwayjs/cli **2.1.1** | HBuilderX / CLI 多端发行 |

### 2.1 后端依赖清单（package.json 实测）

```json
{
  "dependencies": {
    "@midwayjs/bootstrap": "4.2.3",
    "@midwayjs/core": "4.2.3",
    "@midwayjs/jwt": "4.2.3",
    "@midwayjs/koa": "4.2.3",
    "@midwayjs/logger": "4.0.1",
    "@midwayjs/swagger": "4.2.3",
    "@midwayjs/typeorm": "4.2.3",
    "@midwayjs/validate": "4.2.3",
    "bcryptjs": "^2.4.3",
    "mysql2": "^3.9.7",
    "typeorm": "^0.3.20"
  },
  "devDependencies": {
    "@eslint/js": "^10.10.0",
    "@midwayjs/cli": "2.1.1",
    "@midwayjs/mock": "4.2.3",
    "@types/bcryptjs": "^2.4.6",
    "@types/jest": "^30.0.0",
    "@types/node": "^20.11.0",
    "eslint": "^10.10.0",
    "eslint-config-prettier": "^10.1.8",
    "jest": "^29.7.0",
    "patch-package": "^8.0.1",
    "prettier": "^3.9.6",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5",
    "typescript-eslint": "^8.70.0"
  }
}
```

> 版本兼容性：`typescript-eslint@8.70` 的 peer 为 `eslint: ^8.57 || ^9 || ^10`，
> 与 ESLint 10 兼容；`typescript` 要求 `>=4.8.4 <6.1.0`，项目 5.4.x 满足。

### 2.2 与初版规划的依赖差异（重要）

| 规划写法 | 实际情况 | 处理 |
|---|---|---|
| `@midwayjs/cli: ^4.0.0` | **不存在**，最高 2.1.1 | 改用 2.1.1 |
| `@midwayjs/validate: ^4.0.0` | 存在，但 4.x 是 joi 兼容层 | 直接使用，DTO 用 `@Rule(RuleType.xxx)` |
| `type: 'mysql'` | TypeORM 有专用 `'mariadb'` 驱动 | 改用 `'mariadb'`（仍复用 mysql2） |
| — | `@midwayjs/logger` 需手动安装 | mock 的打包缺陷，见 §9.2 |
| — | Node 24 + CLI 2.1.1 + mock 4.x 冲突 | patch-package 修复，见 §9.2 |

---

## 3. 环境搭建（可复现步骤）

### 3.1 MariaDB 10.11（Docker）

```bash
docker run -d \
  --name account-book-mariadb \
  -e MARIADB_ROOT_PASSWORD=root123456 \
  -e MARIADB_DATABASE=account_book \
  -e MARIADB_CHARACTER_SET=utf8mb4 \
  -e MARIADB_COLLATE=utf8mb4_general_ci \
  -p 3306:3306 \
  -v account_book_data:/var/lib/mysql \
  mariadb:10.11 \
  --character-set-server=utf8mb4 \
  --collation-server=utf8mb4_general_ci
```

### 3.2 国内网络下的 Docker 代理（必须）

Docker Hub 直连被墙，需配置代理。Docker Desktop 的代理**不能只改 `~/.docker/config.json`**（那只管 CLI），必须改：

`~/Library/Group Containers/group.com.docker/settings.json`

```json
{
  "overrideProxyHttp": "http://127.0.0.1:7897",
  "overrideProxyHttps": "http://127.0.0.1:7897",
  "overrideProxyExclude": "localhost,127.0.0.1",
  "proxyHttpMode": "manual"
}
```

**关键坑**：`proxyHttpMode` 必须从 `system` 改为 `manual`，否则 Docker 会忽略手动代理设置、走系统 PAC 导致拉取失败。

### 3.3 数据库初始化

```bash
npm run migration:run   # 建表（含 CHECK 约束）
npm run seed            # 插入测试用户 + 默认分类（幂等）
```

---

## 4. 数据库方案（MariaDB 10.11）

### 4.1 E-R 关系

```
user 1—N account        （多账本：每个用户可有多个账本，其中恰有一个 is_default）
account 1—N transaction （account_id NOT NULL，ON DELETE CASCADE）
user 1—N category       （分类仍为用户级共享，不挂账本）
category 1—N category   （自引用 parent_id，最多两级：二级挂钩它的一级，ON DELETE CASCADE）
user 1—N transaction
category 1—N transaction（category_id 可 NULL，支持未分类记账）
```

### 4.2 实际表结构（由 TypeORM 迁移生成）

```sql
CREATE TABLE `users` (
  `id` bigint UNSIGNED NOT NULL AUTO_INCREMENT,
  `username` varchar(64) NOT NULL,
  `password_hash` varchar(255) NOT NULL,
  `created_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  UNIQUE INDEX `IDX_fe0bb3f6520ee0469504521e71` (`username`),
  PRIMARY KEY (`id`)
) ENGINE=InnoDB;

CREATE TABLE `categories` (
  `id` bigint UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` bigint UNSIGNED NOT NULL,
  `name` varchar(64) NOT NULL,
  `type` enum('income','expense') NOT NULL,
  `icon` varchar(64) NOT NULL DEFAULT '',
  `sort` int NOT NULL DEFAULT 0,
  `parent_id` bigint UNSIGNED NULL,                 -- 两级分类新增（NULL = 一级）
  UNIQUE INDEX `uk_user_name` (`user_id`, `name`),
  INDEX `idx_user_parent` (`user_id`, `parent_id`),  -- 两级分类新增
  PRIMARY KEY (`id`),
  CONSTRAINT `FK_categories_user` FOREIGN KEY (`user_id`)
    REFERENCES `users`(`id`) ON DELETE CASCADE,
  CONSTRAINT `FK_categories_parent` FOREIGN KEY (`parent_id`)  -- 两级分类新增
    REFERENCES `categories`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB;

CREATE TABLE `transactions` (
  `id` bigint UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` bigint UNSIGNED NOT NULL,
  `account_id` bigint UNSIGNED NOT NULL,          -- 多账本新增
  `type` enum('income','expense') NOT NULL,
  `amount` decimal(12,2) NOT NULL,
  `category_id` bigint UNSIGNED NULL,
  `record_date` date NOT NULL,
  `record_time` time NULL,                        -- 时刻（新增，可空）
  `note` varchar(255) NOT NULL DEFAULT '',
  `created_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  PRIMARY KEY (`id`),
  INDEX `idx_user_date` (`user_id`, `record_date`),
  INDEX `idx_user_cat` (`user_id`, `category_id`),
  INDEX `idx_user_type` (`user_id`, `type`, `record_date`),
  INDEX `idx_user_account_date` (`user_id`, `account_id`, `record_date`),  -- 多账本新增
  CONSTRAINT `transactions_amount_check` CHECK (`amount` > 0),
  CONSTRAINT `FK_transactions_user` FOREIGN KEY (`user_id`)
    REFERENCES `users`(`id`) ON DELETE CASCADE,
  CONSTRAINT `FK_transactions_category` FOREIGN KEY (`category_id`)
    REFERENCES `categories`(`id`) ON DELETE SET NULL,
  CONSTRAINT `FK_transactions_account` FOREIGN KEY (`account_id`)          -- 多账本新增
    REFERENCES `accounts`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB;

-- 多账本新增
CREATE TABLE `accounts` (
  `id` bigint UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` bigint UNSIGNED NOT NULL,
  `name` varchar(64) NOT NULL,
  `icon` varchar(64) NOT NULL DEFAULT '',
  `sort` int NOT NULL DEFAULT 0,
  `is_default` tinyint NOT NULL DEFAULT 0,
  `created_at` datetime(6) NOT NULL DEFAULT CURRENT_TIMESTAMP(6),
  UNIQUE INDEX `uk_user_account_name` (`user_id`, `name`),
  INDEX `idx_user_default` (`user_id`, `is_default`),
  PRIMARY KEY (`id`),
  CONSTRAINT `FK_accounts_user` FOREIGN KEY (`user_id`)
    REFERENCES `users`(`id`) ON DELETE CASCADE
) ENGINE=InnoDB;
```

### 4.3 迁移文件

| 文件 | 作用 |
|---|---|
| `1789116172271-InitSchema.ts` | 建三表 + 索引 + 外键 |
| `1789117000000-AddAmountCheck.ts` | 手工补 `CHECK (amount > 0)` |
| `1789137009697-MultiAccount.ts` | **多账本**：建 accounts 表、transactions 加 `account_id`、为既有用户建默认账本并回填存量交易、校验无遗漏后收紧为 NOT NULL |
| `1789142218062-CategoryParent.ts` | **两级分类**：categories 加 `parent_id` 自引用外键（`ON DELETE CASCADE`）+ `idx_user_parent`。只改结构，数据重建交给 seed |
| `1789144765914-TransactionRecordTime.ts` | **账单时刻**：transactions 加 `record_time` TIME 可空。**刻意不动 `record_date`**（DATE 继续承担统计与索引），record_time 只管展示与同日排序 |

### 4.4 索引设计说明

- `(user_id, record_date)` — 月度/范围查询
- `(user_id, category_id)` — 分类筛选
- `(user_id, type, record_date)` — 收支分面统计

所有查询强制 `user_id` 前缀实现数据隔离。

### 4.5 已知技术取舍

1. **主键 `BIGINT UNSIGNED`**：mysql2 驱动把 bigint 读成 **string**，故实体中 `id`/`userId`/`categoryId` 的 TS 类型是 `string`（如 `"1"`）。前端拿到的 id 是字符串。
2. **`amount` 保持字符串**：文档要求"金额以字符串传输防精度丢失"，实体 `amount` 类型为 `string`，统计时按需 `Number()`。
3. **`CHECK (amount > 0)` 需手工迁移**：TypeORM 0.3 的 `@Check` 装饰器在 MySQL/MariaDB 方言下**不参与 schema 生成**（已知限制），故实体上的 `@Check` 无效，需手写 ALTER。
4. **`created_at` 精度为 `datetime(6)`**：TypeORM 默认加微秒精度，与规划的 `DATETIME` 略有差异，功能无影响。

---

## 5. 后端目录结构（实际）

```
MyAccountBook/
├── src/
│   ├── config/
│   │   └── config.default.ts        # DB/JWT/校验/全局中间件配置
│   ├── entity/
│   │   ├── user.entity.ts
│   │   ├── account.entity.ts        # 多账本
│   │   ├── category.entity.ts
│   │   └── transaction.entity.ts
│   ├── migration/
│   │   ├── 1789116172271-InitSchema.ts
│   │   ├── 1789117000000-AddAmountCheck.ts
│   │   ├── 1789137009697-MultiAccount.ts
│   │   ├── 1789142218062-CategoryParent.ts        # 分类两级
│   │   └── 1789144765914-TransactionRecordTime.ts # 账单时刻
│   ├── common/                       # 跨模块共享（规划外新增）
│   │   ├── error-code.ts            # 业务错误码枚举
│   │   ├── business.error.ts        # 业务异常（httpStatus 自动推导）
│   │   ├── api.response.ts          # 统一响应结构
│   │   └── swagger/
│   │       └── response.vo.ts       # Swagger 响应模型（包装后的 code/data/message）
│   ├── filter/
│   │   └── default.filter.ts        # 全局兜底异常过滤器
│   ├── middleware/
│   │   ├── jwt.guard.ts             # JWT 守卫（全局 + 白名单）
│   │   └── response.middleware.ts   # 统一响应包装 {code,data,message}
│   ├── auth/
│   │   ├── auth.controller.ts
│   │   ├── auth.service.ts
│   │   └── dto/
│   │       ├── login.dto.ts
│   │       └── register.dto.ts
│   ├── category/
│   │   ├── category.controller.ts
│   │   ├── category.service.ts
│   │   ├── category-preset.ts       # 预置两级分类体系（支出 13+54、收入 2+19）
│   │   └── dto/category.dto.ts
│   ├── account/                      # 多账本
│   │   ├── account.controller.ts
│   │   ├── account.service.ts        # CRUD + 合并 + 删除校验
│   │   └── dto/
│   │       ├── account.dto.ts
│   │       └── account.vo.ts
│   ├── transaction/
│   │   ├── transaction.controller.ts
│   │   ├── transaction.service.ts
│   │   └── dto/transaction.dto.ts
│   ├── statistics/
│   │   ├── statistics.controller.ts
│   │   ├── statistics.service.ts
│   │   └── dto/statistics.dto.ts
│   ├── seed/
│   │   └── index.ts                  # 种子数据
│   ├── configuration.ts
│   ├── data-source.ts                # TypeORM CLI 专用
│   └── interface.ts                  # JwtPayload / PageResult / Context 扩展
├── patches/
│   └── @midwayjs+mock+4.2.3.patch   # 修复 CLI 检测（见 §9.2）
├── test/                            # 单元测试（见 §8.4，98 用例）
│   ├── setup.ts                     # 端口/密钥/数据库等全局前置配置
│   ├── helper.ts                    # 数据源、随机用户名、清理、业务错误断言
│   ├── smoke.test.ts                # 基础设施冒烟
│   ├── auth.service.test.ts
│   ├── category.service.test.ts
│   ├── transaction.service.test.ts
│   ├── statistics.service.test.ts
│   └── account.service.test.ts      # 多账本
├── bootstrap.js
├── docs/
│   └── 前端接口联调文档.md        # 前端可直接照抄的样例（真实调用采集）
├── frontend/                        # uni-app 前端（H5，见 §7.6、§7.7）
│   ├── src/                        # pages / components / store / api / utils
│   ├── vite.config.ts              # /api 代理 + @ 别名
│   └── .npmrc
├── jest.config.js
├── eslint.config.mjs            # ESLint 10 flat config（见 §8.5）
├── .prettierrc
├── .prettierignore
├── package.json
└── tsconfig.json
```

**与规划的差异**：新增 `common/` 目录存放跨模块共享的错误码/异常/响应封装（这些不属于任何单一业务模块）。

---

## 6. 接口契约（实际）

**统一前缀 `/api`**（对规划的有意偏离，便于 Nginx 反代与前端同域部署）。

| 方法 | 路径 | 鉴权 | 请求关键字段 | 响应要点 | 状态 |
|---|---|---|---|---|---|
| POST | `/api/auth/register` | 否 | `{username,password}` | `{token,expiresIn,user}` | ✅ |
| POST | `/api/auth/login` | 否 | `{username,password}` | `{token,expiresIn,user}` | ✅ |
| GET | `/api/categories` | 是 | 可选 `type`、`parentId` | `Category[]`（含 `parentId`） | ✅ |
| GET | `/api/categories/:id` | 是 | — | `Category` | ✅ |
| POST | `/api/categories` | 是 | `{name,type,icon?,sort?,parentId?}` | `Category` | ✅ |
| PUT | `/api/categories/:id` | 是 | 同上（部分，`parentId:''` 可提升为一级） | `Category` | ✅ |
| DELETE | `/api/categories/:id` | 是 | — | `{success,deletedChildren}` | ✅ |
| GET | `/api/transactions` | 是 | `start/end/categoryId/type/page/size` | `{list,total,page,size}` | ✅ |
| GET | `/api/transactions/:id` | 是 | — | `Transaction`（含 category） | ✅ |
| POST | `/api/transactions` | 是 | `{type,amount,categoryId?,recordDate,recordTime?,note?}` | `Transaction` | ✅ |
| PUT | `/api/transactions/:id` | 是 | 同上（部分） | `Transaction` | ✅ |
| DELETE | `/api/transactions/:id` | 是 | — | `{success:true}` | ✅ |
| GET | `/api/statistics/monthly?month=YYYY-MM` | 是 | 可选 `accountId` | `{month,income,expense,balance}` | ✅ |
| GET | `/api/statistics/overview` | 是 | 可选 `accountId` | `{total,ranges[5]}` 首页总览 | ✅ |
| GET | `/api/statistics/category?month=YYYY-MM&type=` | 是 | 可选 `type`、`accountId` | `[{categoryId,name,icon,type,sum,ratio}]` | ✅ |
| GET | `/api/accounts` | 是 | — | `Account[]` | ✅ |
| GET | `/api/accounts/:id` | 是 | — | `Account` | ✅ |
| POST | `/api/accounts` | 是 | `{name,icon?,sort?}` | `Account`（首个自动 isDefault） | ✅ |
| PUT | `/api/accounts/:id` | 是 | `{name?,icon?,sort?,isDefault?}` | `Account` | ✅ |
| GET | `/api/accounts/:id/delete-preview` | 是 | — | `{transactionCount}` | ✅ |
| DELETE | `/api/accounts/:id?confirmName=xxx` | 是 | 确认名必须与账本名一致 | `{success,deletedTransactions}` | ✅ |
| POST | `/api/accounts/merge-preview` | 是 | `{targetId,sourceId}` | `{sourceTotal,willMove,willSkip}` | ✅ |
| POST | `/api/accounts/merge` | 是 | `{targetId,sourceId}` | 合并报告 | ✅ |

**多账本对老接口的影响**（均为新增可选参数，不传则行为与改造前一致）：

- `POST /api/transactions`、`PUT /api/transactions/:id`：新增 `accountId`；不传自动落到默认账本
- `GET /api/transactions`：新增 `accountId` 筛选；不传返回全部账本
- `GET /api/statistics/monthly`、`/category`：新增 `accountId`；不传统计全部账本

**新增错误码**：`40403` 账本不存在、`40903` 账本名已存在、`40001` 删除确认名不一致、`40002` 不能删最后一个账本、`40003` 不能合并到自己。

> 多账本的完整设计（合并去重口径、删除策略、迁移方案、验收要点）见 **`docs/多账本设计文档.md`**。

### 6.1 统一响应结构

```json
{ "code": 0, "data": {}, "message": "success" }
```

### 6.2 HTTP 状态码策略（对规划的有意补充）

响应体结构统一为 `{code, data, message}`，同时 **HTTP 状态码与业务码分离**：

| 场景 | HTTP | body.code |
|---|---|---|
| 成功 | 200 | 0 |
| 参数校验失败 | 422 | 40000 |
| 未登录/Token 失效 | 401 | 40100 |
| 无权限 | 403 | 40300 |
| 资源不存在 | 404 | 404xx |
| 冲突（用户名/分类重名） | 409 | 409xx |
| 服务端错误 | 500 | 50000 |

业务错误码规则：`code / 100` 向下取整 × 100 即 HTTP 状态码（如 40401 → 404）。

### 6.3 金额格式约定

- **请求与响应均为字符串**（如 `"12.34"`）
- DTO 用正则严格校验：`/^\d+(\.\d{1,2})?$/`（正数 + 最多两位小数）
- DB 层 `CHECK (amount > 0)` 兜底

### 6.4 鉴权约定

- 请求头：`Authorization: Bearer <token>`
- 全局 JWT 守卫，默认所有 `/api` 路由需登录
- 放行规则：① **非 `/api` 路径一律放行**（Swagger 文档等框架自带路由）；② `/api` 下白名单为 `/api/auth/login`、`/api/auth/register`
- 校验通过后 `ctx.user = { userId, username }` 供 controller 取用
- JWT 有效期 7 天，secret 走环境变量 `JWT_SECRET`

### 6.5 Swagger 接口文档（已完成）

| 项 | 值 |
|---|---|
| 包 | `@midwayjs/swagger@4.2.3`（已在 `configuration.ts` 的 imports 中注册） |
| UI 地址 | `http://127.0.0.1:7001/swagger-ui/index.html` |
| JSON 地址 | `http://127.0.0.1:7001/swagger-ui/index.json` |
| 覆盖范围 | 23 个操作 / 14 条路径 / 5 个分组（认证、分类、账单、统计、账本）/ 49 个模型 |
| 鉴权 | 全局 Bearer；除登录注册外均需点击右上角 Authorize 填入 token |

要点：

- **响应模型展示的是包装后的结构**。所有 `/api` 响应都会被 `ResponseMiddleware` 包成 `{code, data, message}`，因此 `src/common/swagger/response.vo.ts` 里的 VO 全部带这层外壳，前端照抄时记得是 `res.data.data`（axios）而不是 `res.data`。
- **每个 VO 都显式声明三个字段，不用继承**——Midway Swagger 对继承属性的解析不稳定。
- 业务错误码写在 `ErrorResponseVO.code` 的 description 里（40101 / 40401 / 40402 / 40901 / 40902 等）。

---

## 7. 前端实现（H5 端已完成，联调文档见 `docs/前端接口联调文档.md`）

> **开写前先看 `docs/前端接口联调文档.md`**：23 个接口的请求/响应样例（均为真实调用结果）、
> 完整错误码表、luch-request 拦截器封装、各端 baseURL 差异与联调自检清单。

### 7.1 信息架构
底部 Tab 4 个：**记账** / **明细** / **统计** / **我的**

### 7.2 页面清单
登录页、首页/今日概览、记一笔、明细、统计、分类管理、我的。

### 7.3 关键交互
- **记一笔**：切收/支 → 选分类（图标网格）→ 大键盘输金额 → 选日期（默认今天）→ 备注 → 保存 → 回明细
- **明细**：时间线展示，支持月份/分类/收支类型筛选，下拉分页
- **统计**：按月切换，收支汇总 + 分类占比**环形图**（`RingChart`，SVG 自绘）+（P1）趋势线

### 7.4 前端目录结构
```
src/
├── pages/          login / home / record / detail / statistics / category / mine
├── components/     AmountKeyboard / CategoryGrid / RingChart
├── store/          user / category / transaction / statistics（Pinia）
├── api/            auth / category / transaction / statistics 封装
├── utils/          request 拦截器 / storage 工具
└── static/         分类图标资源
```

### 7.5 请求拦截器约定
- 请求前注入 `Authorization: Bearer <token>`
- 响应捕获 `HTTP 401` 或 `body.code === 40100` 时清 Token 并 `uni.reLaunch` 跳登录页
- 其他业务错误统一 `uni.showToast`
- **响应剥掉 `{code,data,message}` 外壳**：拦截器直接 return `body.data`，
  业务代码不写 `res.data.data`（详见 `docs/前端接口联调文档.md` §3）

### 7.6 前端依赖（实测版本，与初版规划的偏差）

| 包 | 规划写法 | 实际 | 说明 |
|---|---|---|---|
| uni-app | — | `3.0.0-4080420251103001` | 官方 `vite-ts` 模板 |
| vue | — | 3.4.21 | 模板固定版本，未升级 |
| `@dcloudio/uni-ui` | — | `^1.5.12` | **独立版本线**，不是 3.0.0-alpha，按 alpha 版本号装会 404 |
| pinia | 最新 | `2.1.7` | pinia 4 / 2.3 要求 vue ≥3.5.11，与模板的 3.4.21 冲突，故降级 |
| luch-request | 最新 | `3.1.1` | **4.0 是重写版，API 全变**（改为 `createLuchRequest`，无 default 导出），3.1.1 稳定且资料多 |
| 图表 | `qiun-data-charts` | **SVG 自绘** | `qiun-data-charts` 不在 npm（只在 DCloud 插件市场）；目标端是 H5，环形图用 SVG 零依赖实现 |
| sass | — | `^1.x` | uni-ui 组件用了 scss，不装会构建失败 |

> 已在 `frontend/.npmrc` 开启 `legacy-peer-deps=true`：uni-app 依赖树与部分库的 peer 要求冲突。

### 7.7 目录结构（实际）

```
frontend/
├── src/
│   ├── pages/       home / detail / statistics / mine / login / record / category / account
│   ├── components/  RingChart（SVG 环形图）/ CategoryGrid / CategoryPicker（底部二级选择器）/
│   │                AmountKeyboard / DateTimePicker（日历 + 时刻滚轮）/ RecordFab（底栏中部记一笔）/ EmptyState
│   ├── store/       user / category / account（Pinia）
│   ├── api/         auth / category / transaction / statistics / account
│   ├── utils/       request（luch-request 封装）/ icon（emoji 映射）/ format（千分位）
│   ├── pages.json   四 Tab + easycom 自动引入 uni-ui
│   └── main.ts      注册 Pinia
├── vite.config.ts   /api 代理（绕 CORS）+ @ 别名
├── .env.example     VITE_API_BASE_URL / VITE_BACKEND 说明
└── .npmrc           legacy-peer-deps=true
```

**跨域方案**：后端未配 CORS，H5 开发用 Vite 代理 `/api → 127.0.0.1:7001`，
前端 baseURL 直接写 `/api`。生产环境需换成真实域名（见 `src/utils/request.ts`）。

---

## 8. 关键实现步骤

### 8.1 后端（进度）

| 步骤 | 状态 |
|---|---|
| 1. 初始化项目（Midway v4 Koa 模板） | ✅ |
| 2. 安装依赖（实测版本） | ✅ |
| 3. 写 config.default.ts（DB/JWT/校验/detector） | ✅ |
| 4. 写 entity（三实体 + 关系 + 索引） | ✅ |
| 5. 写 migration（建表 + 手工补 CHECK） | ✅ |
| 6. auth 模块（register/login + bcrypt + JWT 签发） | ✅ |
| 7. category 模块（CRUD + userId 隔离） | ✅ |
| 8. transaction 模块（分页/范围/分类查询 + 类型一致性） | ✅ |
| 9. statistics 模块（月度汇总 + 分类占比） | ✅ |
| 10. seed 脚本（测试用户 + 两级分类体系，每用户 88 个分类） | ✅ |
| 11. 回归测试（现 47 用例全绿） | ✅ |
| 12. Swagger 接口文档（23 个操作注解 + 响应模型） | ✅ |
| 13. 单元测试（@midwayjs/mock，service 层，现 98 用例） | ✅ |
| 14. ESLint + Prettier | ✅ |

**后端功能完成度 100%（步骤 1-11），工程化 100%（步骤 12-14）。** 关键实现细节见 §9 踩坑记录，回归测试脚本为 \`scripts/regression-test.sh\`。

### 8.2 前端（骨架已完成，H5 端）

| 步骤 | 状态 |
|---|---|
| 1. 拉取 uni-app Vue3 + Vite + TS 模板到 `frontend/` | ✅ |
| 2. 装依赖（版本实测，见 §7.6） | ✅ |
| 3. pages.json 配 tabBar（记账/明细/统计/我的）与 7 个页面 | ✅ |
| 4. 封装 `@/utils/request`（luch-request，剥外壳 + 401 跳登录） | ✅ |
| 5. api 四模块 + Pinia（user / category） | ✅ |
| 6. 页面：登录 / 首页 / 记一笔 / 明细 / 统计 / 分类管理 / 我的 | ✅ |
| 7. 组件：RingChart / CategoryGrid / AmountKeyboard | ✅ |
| 8. 联调验证（build:h5 通过 + 代理转发正常） | ✅ |
| 9. 细节打磨：账单编辑 / 空状态 / 加载态 / 移动端适配 / 生产 baseURL | ✅ |
| 10. **多账本**：创建 / 删除 / 合并 / 切换 / 迁移 | ✅ |
| 11. 首页改版：历年总览 banner + 五区间统计 + 分类排行 + 底栏中部记一笔 | ✅ |
| 12. **分类支持两级**：支出 13 个一级 + 54 个二级 | ✅ |
| 13. 收入分类也改为两级：2 个一级（职业收入 / 其他收入）+ 19 个二级 | ✅ |
| 14. 记一笔改**底部弹出二级选择器**（`CategoryPicker`，左右联动 + 最近使用 + 搜索） | ✅ |
| 15. 日期改**日历弹窗** + 账单支持**时刻**（`DateTimePicker` + `record_time`） | ✅ |
| 16. 骨架屏、图表交互、PWA 等增强 | ⬜（P2） |

**目标端：H5**（用户已确认）。UI 框架：**uni-ui**。

### 分类体系（两级）

| 项 | 说明 |
|---|---|
| 层级 | **最多两级**：一级分类下可挂二级；二级下不允许再挂（创建/更新时校验） |
| 支出 | **13 个一级 + 54 个二级**（居家物业 / 行车交通 / 交流通讯 / 休闲娱乐 / 金融保险 / 其他杂项 / 电子产品 / 养殖 / 医疗保健 / 人情往来 / 衣服饰品 / 食品酒水 / 学习进修） |
| 收入 | **2 个一级 + 19 个二级**：职业收入（工资收入 / 利息收入 / 加班收入 / 奖金收入 / 投资收入 / 兼职收入 / 公积金提款 / 顺风车）、其他收入（礼金收入 / 中奖收入 / 意外来钱 / 经营所得 / 信用卡还款 / AA还款 / 物品回收 / 房租收入 / 共享带宽收入 / 保险报销 / 其他） |
| 合计 | **每用户 88 个**（一级 15 + 二级 73） |
| 记账交互 | **底部弹出选择器**（`CategoryPicker`）：左侧一级分类、右侧按一级分组的二级列表（滚动联动）；**只能选到二级**，一级仅用于切换分组。支持最近使用、搜索、在当前一级下新建二级 |
| 统计口径 | **聚合到一级**：二级分类的金额归入其父；`COALESCE(p.id, c.id)` 实现 |
| 图标 | `icon` 字段直接存 emoji（前端 `iconOf()` 兼容老的标识符写法） |
| 唯一键 | `(user_id, name)` **全局唯一**（不分层级）——MySQL 唯一索引不约束 NULL，把 parent_id 纳入唯一键反而拦不住一级重名 |
| 数据重建 | `npm run seed`（仅 demo）或 `npm run seed:all`（**所有用户**）会 upsert 新体系并清理不在名单里的旧分类，其交易按类型改挂到「其他支出」/「其他」 |

**每用户分类构成**

| 层级 | 支出 | 收入 | 小计 |
|---|---|---|---|
| 一级（分组） | 13 | 2 | **15** |
| 二级（可记账） | 54 | 19 | **73** |
| 合计 | 67 | 21 | **88** |

**全库实测**（2026-09-12 复核，替换原有的「32 用户 × 74」旧口径）：

| 校验项 | 结果 |
|---|---|
| 已初始化用户 | **34 个用户各有 88 个分类**（34 × 88 = 2992，全部一致）✅ |
| 每用户一级 / 二级 | 15 / 73，与预设一致 ✅ |
| 分类数异常的用户 | 4 个（2 个 0 分类、2 个 2 分类），均为**回归/单测脚本创建的临时账号**，非 seed 产物 |
| 交易总数 | 53（含测试数据），分类体系重建未造成丢失 |

> 命名注意：唯一键全局唯一，所以收入侧用「**礼金收入**」而不是「红包」——支出的「人情往来」下已有二级分类「红包」，
> 同名会被 upsert 覆盖（历史上真踩过：收入分类凭空少了一个）。
>
> 少数测试残留数据（回归脚本创建的临时分类）在清理后变为「未分类」，属预期。

### 首页布局（改版后）

参考随手记式布局，自上而下：

1. **banner**：当前账本的**历年累计**（总支出大字 + 总收入 / 结余），左上角可切换账本
2. **区间卡片**：今天 / 本周 / 本月 / 本年 / 去年，每行显示该区间的总收入与总支出
3. **分类排行卡片**：本月各分类支出排行（排名、占比、金额、进度条），右上角显示记账笔数与总支出
4. **底栏中部**：凸起「记一笔」按钮（`RecordFab` 组件，4 个 Tab 页均引入）

数据来源：`GET /api/statistics/overview`（一次请求拿全部区间，避免首屏 6 次往返）
+ `GET /api/statistics/category`（排行，新增 `count` 字段）。

**本周口径为周一至周日**（由后端统一计算，避免各端算法不一致）。

### 多账本完成情况

详见 `docs/多账本设计文档.md`。要点：

- 新增 `accounts` 表，`transactions.account_id` NOT NULL + CASCADE 外键
- 迁移把 16 个用户的历史数据全部归入各自的「默认账本」，**0 条孤儿交易**
- 老接口不传 `accountId` 时自动落到默认账本，**既有 31 个回归用例全部通过**
- 合并采用严格全字段去重（金额+日期+类型+分类+备注），重复保留目标方
- 删除需输入账本名确认，且不允许删掉最后一个账本
- 单测新增 26 个用例（75/75 全绿），回归新增 14 个（45/45 全绿）

补充：

- **账单编辑**复用记一笔页：`/pages/record/index?id=xxx` 进入编辑态，
  `onLoad` 拉详情回填、标题改「编辑账单」、保存走 `PUT`、底部多一个删除入口。
- **明细页**支持三种编辑入口：点击整行、左滑「编辑」、左滑「删除」（两个按钮）。
- **空状态**统一用 `components/EmptyState.vue`（图标 + 文案 + 可选按钮），
  首页、明细、统计、分类管理均已接入。
- **加载态**：列表底部用 uni-ui 的 `uni-load-more`（more / loading / noMore 三态）。
- **生产 baseURL**：读 `VITE_API_BASE_URL`，未配置则默认 `/api`（前后端同域部署，
  Nginx 反代）。配置文件示例见 `frontend/.env.example`。

### 8.3 统计 SQL 思路

```sql
-- 月度汇总
SELECT
  SUM(CASE WHEN type='income'  THEN amount ELSE 0 END) AS income,
  SUM(CASE WHEN type='expense' THEN amount ELSE 0 END) AS expense
FROM transactions
WHERE user_id = :userId AND record_date BETWEEN :monthStart AND :monthEnd;

-- 分类占比
SELECT category_id, type, SUM(amount) AS sum
FROM transactions
WHERE user_id = :userId AND record_date BETWEEN :monthStart AND :monthEnd
  [AND type = :type]
GROUP BY category_id, type ORDER BY sum DESC;
```

服务层按 `sum / 该 type 总 sum` 算 `ratio`，JOIN categories 取 `name/icon`；未分类（`category_id IS NULL`）单独归组。

### 8.4 测试策略（已完成）

两层测试，职责不重叠：

| 层 | 工具 | 位置 | 覆盖 | 用例数 |
|---|---|---|---|---|
| 单元测试 | `@midwayjs/mock` + Jest | `test/*.test.ts` | **service 层业务逻辑**：异常分支、数据隔离、排序、统计口径 | 98 |
| 回归测试 | curl + bash | `scripts/regression-test.sh` | **HTTP 接口层**：状态码、鉴权、DTO 校验 | 47 |

**单元测试选型说明**：service 层强依赖 TypeORM（实体关系、QueryBuilder、唯一键、外键 CASCADE），
手写 mock 成本高且测不到真实约束，因此**直接连真实 MariaDB 容器**，
每个用例用随机用户名建独立用户，`afterAll` 按 userId 清理。

关键约定：

- `test/setup.ts` 固定端口（7002，避开开发服务的 7001）与 JWT 密钥，保证可复现。
- 每个测试文件 `afterAll` 必须 `close(app)` **并** `closeTestDataSource()`，
  否则数据库连接句柄不释放，Jest 进程不会退出（表现为测试"卡住"）。
- 断言业务错误码要用 `Number(err.code)`——`BusinessError` 继承 `MidwayError`，
  构造时把业务码转成了字符串（见 §9.7）。
- 造统计数据时**每个断言场景用独立月份/独立用户**，否则用例之间会互相污染
  （曾出现"未分类金额 100 变 103"的假失败）。

覆盖率分布（6 个 suite）：基础设施 2、auth 6、category 23、transaction 21、statistics 20、account 26（合计 98）。

### 8.5 代码规范（已完成）

| 工具 | 版本 | 配置文件 |
|---|---|---|
| ESLint | 10.10.0 | `eslint.config.mjs`（flat config，ESLint 9 起不再用 `.eslintrc`） |
| typescript-eslint | 8.70.0 | 同上（`tseslint.config()` 组合） |
| Prettier | 3.9.6 | `.prettierrc` / `.prettierignore` |
| eslint-config-prettier | 10.1.8 | 关掉与 Prettier 冲突的格式化规则 |

**规则取舍原则**：个人项目配 lint 是为了挡住真会出事的问题（未使用变量、未 await 的 Promise），
不是把代码管成教科书。因此显式放开：

- `no-explicit-any` — 对接 Koa `ctx`、TypeORM raw 结果时很难避免
- `no-console` — 服务日志与 seed 脚本需要
- `no-non-null-assertion` — TypeORM 实体字段大量使用
- `test/**` 下额外放宽 `no-unused-vars`（测试里未使用参数很常见）

**Prettier 风格**：单引号、分号、`printWidth: 100`、2 空格、尾逗号 `all`、LF。

首次接入时格式化了 21 个文件（纯格式，无语义变更），已用单测 49/49 + 回归 31/31 + `tsc --noEmit` 三重验证。

> 注意：`eslint.config.mjs` 用 `.mjs` 后缀——项目是 CommonJS（`package.json` 无 `type: module`），
> 用 ESM 配置文件最稳，避免 flat config 的模块格式歧义。

---

## 9. 已知问题与踩坑记录

### 9.1 【已解决】路由全 404 — 三个根因叠加

**现象**：服务能启动（`Start on LAN http://...:7001`），但所有路由返回 404（Koa 默认 HTML）。

**根因 A：入口文件加载失败被静默吞掉**
- `findProjectEntryFile` 用 `loadModule(..., { safeLoad: true })` 加载入口
- 排查过程中加入的临时调试代码有 TS 编译错误
- `safeLoad: true` 吞掉错误返回 undefined → configuration 未被识别 → `Found Framework length = 0`
- **教训：Midway 入口文件加载失败不会报错**，排查路由问题时应优先确认入口是否真被加载

**根因 B：文件扫描器必须显式声明（v4 重大变更）**
- Midway v4 的 `@Configuration` **必须配置 `detector`**
- 否则 controller / middleware / filter 一个都不会被自动加载
- 参考：`@midwayjs/mock` 的 `createLegacyApp` 内部就是这么做的
- 正确写法：

```ts
@Configuration({
  imports: [koa, typeorm, validate, jwt],
  importConfigs: [join(__dirname, './config')],
  detector: new CommonJSFileDetector({ conflictCheck: true }),
})
```

**根因 C：全局中间件 / 过滤器必须显式注册（v4 不再读 config）**
- `config.middleware` 在 v4 **完全无效**（baseFramework 中无任何相关代码）
- filter 也不会自动扫描
- 必须在 `onReady` 中注册：

```ts
@App() app: koa.Application;
async onReady() {
  this.app.useMiddleware([JwtGuardMiddleware, ResponseMiddleware]);
  this.app.useFilter([DefaultErrorFilter]);
}
```

### 9.2 【已解决】CLI + mock + Node 24 三方不兼容

**根因**：CLI 2.1.1 用 `require.resolve('@midwayjs/mock/package.json')` 检测依赖，而 `@midwayjs/mock` 4.x 的 `package.json` 有 `exports` 字段且未暴露 `./package.json`。Node 22+ 严格遵循 `exports`，直接抛 `ERR_PACKAGE_PATH_NOT_EXPORTED`。

**解决**：patch-package 给 mock 的 `exports` 补一行 `"./package.json": "./package.json"`。

- 补丁文件：`patches/@midwayjs+mock+4.2.3.patch`
- `package.json` 加 `"postinstall": "patch-package"`

### 9.3 【已解决】mock 缺 @midwayjs/logger

**根因**：`@midwayjs/mock@4.2.3` 的 `dist/creator.js` 中 `require('@midwayjs/logger')`，但 logger 只被列在 mock 的 `devDependencies` 而非 `dependencies`，安装 mock 时不会自动带出。

**解决**：手动安装 `@midwayjs/logger@4.0.1`（logger 独立版本线，最新 4.0.1，与 core 4.2.x 不同步）。

### 9.4 【已解决】Docker 代理不生效

见 §3.2，关键是 `proxyHttpMode` 必须为 `manual`。

### 9.5 【已解决】TypeORM @Check 不生成 DDL

TypeORM 0.3 的 `@Check` 装饰器在 MySQL/MariaDB 方言下不参与 schema 生成，需手工迁移补 CHECK 约束。

### 9.6 【已解决】TypeORM entities 路径规则（易踩）

`@midwayjs/typeorm` 内部用 `@midwayjs/glob` 扫描实体，规则特殊：

- glob 的 `cwd` = `baseDir` = `<项目根>/src`
- 匹配逻辑：文件绝对路径 `replace(baseDir, '')` 后比对 pattern
- `formatGlobString` 会给**不以 `*` 开头**的 pattern 自动补前导 `/`

**正确写法**（相对 src、不带前导斜杠）：

```ts
const entityPattern =
  relative(join(process.cwd(), 'src'), join(__dirname, '../entity')) +
  '/*.entity{.ts,.js}';
// 结果：'entity/*.entity{.ts,.js}'
```

**错误写法**（均匹配不到任何文件）：
- `join(__dirname, '../entity/*.entity{.ts,.js}')` —— 绝对路径
- `'/entity/*.entity{.ts,.js}'` —— 前导斜杠导致 `formatGlobString` 重复补 `/`

### 9.7 【已解决】BusinessError 状态码映射错误

原实现 `Math.floor(code / 100) * 100` 是错的（40901 → 40900，超出 400-599 范围被兜底成 500）。

正确：`Math.floor(code / 100)`（40901 → 409，40100 → 401）。

### 9.8 【已解决】服务启动即退出 — seed 脚本被文件扫描器触发

现象：`node -r ts-node/register bootstrap.js` 日志先打印 `current app started`，紧接着报
`MidwayDefinitionNotFoundError: Definition for "AddAmountCheck1789117000000" not found`，然后 `exit with code:1`。

两个根因叠加：

1. **seed 无条件自执行**：`src/seed/index.ts` 底部直接调用 `seed()`。Midway 的文件扫描器会加载 `src` 下所有 `.ts`，于是每次启动服务都会跑一遍种子脚本；失败时的 `process.exit(1)` 直接带走服务进程。
2. **seed 加载了迁移**：seed 复用了 `src/data-source.ts`（含 `migrations` 配置），`DataSource.initialize()` 会解析迁移类，而这些类没有注册进 Midway 容器，必然解析失败。

解决：

- 自执行加守卫 `if (require.main === module)`，仅 `npm run seed` 直接运行时触发。
- seed 内部自建不含 `migrations` 的 DataSource（只需要实体）。

> 教训：**任何放在 `src` 下的脚本都不要写无条件自执行**，扫描器会加载它。

### 9.9 【已解决】Swagger 打不开 — 被自家 JWT 守卫 401

现象：`/swagger-ui/index.json` 返回 401，`/swagger-ui` 返回 404。

根因：`JwtGuardMiddleware.match()` 原写法是 `!publicPaths.includes(ctx.path)`，即**除登录注册外的所有路径**都要校验 token，Swagger 这类非业务路由也被拦了。

解决：守卫只管业务 API——`if (!ctx.path.startsWith('/api')) return false;`。

附带知识：Midway Swagger 的文档 JSON 路径是 **`/swagger-ui/index.json`**（不是 `/swagger-ui.json`），UI 页面是 `/swagger-ui/index.html`。

### 9.10 【已解决】Jest 跑不起来 — 类型定义与 watchman

三个坑，按顺序踩：

1. **`Cannot find name 'describe'`**：`mw test` 会先跑一次 TS 类型检查，缺 `@types/jest` 直接失败。装它即可。
2. **`unable to talk to your watchman`**：本机没跑 watchman 服务，但依赖里有 `fb-watchman`，Jest 启动时连接失败会导致进程崩溃。在 `jest.config.js` 里设 `watchman: false`。
3. **测试"卡住"不退出**：测试里自建的 TypeORM 数据源没关闭，连接句柄不释放。`afterAll` 里必须 `closeTestDataSource()`。

补充：`mw test` 实际读的是 `@midwayjs/cli-plugin-test` 的配置，
但它**会合并项目根的 `jest.config.js`**（`{...默认, ...用户配置}`），所以自定义配置是生效的。

### 9.11 【已解决】清空分类无效 — TypeORM 关系对象回填外键

**现象**：`PUT /api/transactions/:id` 传 `categoryId: ""` 想把账单改成未分类，接口返回 200，但 `categoryId` 仍是旧值。

**根因**：`update()` 里只改了 `entity.categoryId`，而 `findById()` 已经把 `category` 关系对象加载到了实体上。
TypeORM 保存时依据关系对象判断"关系未变"，于是把外键**回填成旧值**。

**修复**：清空时同时断开关系对象。

```ts
if (dto.categoryId !== undefined) {
  const nextCategoryId = dto.categoryId || null;
  patch.categoryId = nextCategoryId;
  if (!nextCategoryId) {
    entity.category = null;   // 关键：同时断开关系，否则外键被回填
  }
}
```

> 教训：**改外键字段时，若实体上已加载了对应的关系对象，两者要一起改**，否则关系会覆盖字段。

### 9.12 【已解决】单测假失败 — 测试数据互相污染

**现象**：统计单测里"未分类金额"期望 `100.00`，实际 `103.00`。

**根因**：`monthly` 的"月份首尾两天"用例往同一个月（2026-08）里又插了两条**未分类**账单（1.00 + 2.00），
而 `categoryBreakdown` 的用例在其后执行，读到的是被污染后的数据。

**修复**：该用例改用独立月份（2026-10）造数据。

> 教训：**每个断言场景用独立月份或独立用户**，共享数据Fixture 的用例之间会互相影响，
> 而且这种失败极具迷惑性——看起来像业务 bug，实际是测试数据问题。

### 9.13 【已解决】前端依赖四个坑（uni-ui / sass / luch-request / pinia）

**坑 1：`@dcloudio/uni-ui` 版本号与其他 @dcloudio 包不同步**
其他包是 `3.0.0-4080420251103001`，uni-ui 是**独立的 `1.5.x` 版本线**。
按 alpha 版本号装会直接 `ETARGET 404`。正确写法：`npm i @dcloudio/uni-ui@^1.5.12`。

**坑 2：缺 sass 导致构建失败**
uni-ui 组件内部用 scss，报错 `Preprocessor dependency "sass" not found`。
装 `npm i -D sass` 即可。会有 Dart Sass 的 deprecation 警告，不影响构建。

**坑 3：luch-request 4.0 是重写版，API 全变**
4.0 改成 `type: module`，导出的是 `createLuchRequest()`，**没有 default 导出**，
按老写法 `import Request from 'luch-request'` 会报 `"default" is not exported`。
解决：降到 `3.1.1`（经典 `new Request()` API，资料多）。

**坑 4：pinia 与模板的 vue 3.4.21 冲突**
pinia 4.x / 2.3 要求 `vue ^3.5.11`，模板锁 3.4.21；再降到 2.1.7 又会牵出
`@vue/composition-api`（vue2 时代的可选 peer）冲突。
最终用 `pinia@2.1.7` + `frontend/.npmrc` 里 `legacy-peer-deps=true` 解决。

### 9.14 【已解决】`onLoad is not exported by vue`

**现象**：构建报 `"onLoad" is not exported by "vue.runtime.esm.js"`。

**根因**：把 uni-app 的页面生命周期 `onLoad` 当成了 Vue 的生命周期，写成
`import { ref, computed, onLoad } from 'vue'`。

**修复**：`onLoad`、`onShow`、`onPullDownRefresh`、`onReachBottom` 都属于 uni-app，
必须从 `@dcloudio/uni-app` 导入；`ref/computed/reactive` 才是从 `vue` 导入。

### 9.15 【已解决】H5 跨域 — 后端未配 CORS

后端完全没有 CORS 配置，H5 直接请求 `127.0.0.1:7001` 会被浏览器拦。
**解法**：Vite devServer 代理 `/api → http://127.0.0.1:7001`，前端 baseURL 写 `/api`。
生产环境需换真实域名，或在后端加 `@midwayjs/cors`。

---

## 10. 待明确事项 / 扩展点

- **默认分类种子数据**：✅ 已实现（seed 脚本，两级体系，每用户 88 个：支出 13+54、收入 2+19）
- **多用户隔离**：✅ 已按 `user_id` 强隔离，回归测试已验证跨用户不可见
- **账单分类类型一致性**：✅ 已强制校验（支出账单只能挂支出分类）
- **期初余额**：默认「结余 = 本期收入 − 支出」，不含期初；`accounts` 表已落地（见多账本），但余额型账户（资产/负债）仍为 P2
- **首发包目标端**：✅ H5 已完成（`frontend/`，Vue3 + uni-ui）；微信小程序 / 原生 App 为 P2
- **生物登录 / 离线记账 / 图标集**：均 P2
- **多账本**：✅ 已完成（`accounts` 表 + 账本 CRUD / 删除预检 / 合并 / 当前账本切换，设计见 `docs/多账本设计文档.md`）；
  已知限制：合并不可逆、删除无回收站、当前账本存前端（多端不同步）
- **CORS**：⚠️ **后端当前仍未配置 CORS**（`src/` 与 `package.json` 均无相关配置）。
  现状是 H5 靠 Vite devServer 代理 `/api` → `127.0.0.1:7001` 绕开跨域；
  若前端要独立域名部署，需二选一：后端加 `@midwayjs/cors`，或继续用同域反代（**推荐**，见决策 D11 的 `/api` 前缀）。
  详见 `docs/前端接口联调文档.md` §7.2

---

## 11. 常用命令

```bash
# 环境
export PATH=/usr/local/bin:$PATH

# 启动 MariaDB 容器
docker start account-book-mariadb

# 开发
npm run dev                  # 启动开发服务（含热重载，需先修复 patch-package 问题）

# 数据库
npm run migration:generate -- src/migration/XxxName
npm run migration:run
npm run migration:revert
npm run seed                 # 种子数据（幂等）：demo 账号 + 账本 + 两级分类体系
npm run seed:all             # 为**库里所有用户**初始化默认账本与分类体系（已有数据的环境用这个）

# 构建与生产
npm run build
npm run start

# 测试
npm run test                 # 单元测试（Jest + @midwayjs/mock，98 用例，test/ 目录）
npm run test -- --cov        # 带覆盖率
bash scripts/regression-test.sh   # 端到端接口回归（47 用例，需先启动服务）

# 代码规范
npm run lint                 # ESLint 检查
npm run lint:fix             # 自动修复
npm run format               # Prettier 格式化 src/ 与 test/
npm run format:check         # 只检查不写入（适合放在 CI）
npx tsc --noEmit             # 类型检查

# 前端（frontend/ 目录下执行）
npm run dev:h5            # H5 开发服务器 http://localhost:5173
npm run build:h5          # 生产构建
npm run type-check        # vue-tsc 类型检查
cp .env.example .env.production   # 按需配置 VITE_API_BASE_URL（同域部署可留空）

# 接口文档
# 服务启动后访问 http://127.0.0.1:7001/swagger-ui/index.html
# JSON：http://127.0.0.1:7001/swagger-ui/index.json

# 联调：先启动后端（7001），再启动前端（5173），前端 /api 由 Vite 代理转发
# 测试账号：demo / 123456

# 本地调试须知：curl 需绕过代理，否则 127.0.0.1 会被本地代理（7897）拦成 502
curl --noproxy '*' http://127.0.0.1:7001/swagger-ui/index.json

# 直接启动（开发期）
node -r ts-node/register bootstrap.js
```

---

## 12. 变更记录

| 日期 | 变更 |
|---|---|
| 初版 | 全栈规划文档（不含代码） |
| 第二次 | 按实际实施修订：更新实测版本、记录环境搭建、补充 `/api` 前缀与注册接口、记录 5 个踩坑、标注路由 404 未解决问题 |
| 第三次 | 后端完成：修复路由 404（三根因）、entities 路径、状态码映射；补齐 transaction / statistics / seed / 回归测试；新增 2 个踩坑记录（9.6、9.7）；进度更新为「后端 100%，前端未开始」 |
| 第四次 | 后端工程化第一步：Swagger 落地（14 接口注解 + 29 个响应模型 + DTO 字段说明），新增 §6.5；修复 seed 自执行导致服务启动退出（§9.8）与 Swagger 被 JWT 守卫拦截（§9.9）；后端进度表新增工程化步骤 12-14 |
| 第五次 | 后端工程化第二步：单元测试落地（jest.config.js + test/ 共 49 用例，覆盖 service 层），新增 §8.4；修复「清空分类」真 bug（TypeORM 关系回填外键，§9.11）；记录 Jest 环境三个坑（§9.10）与测试数据污染（§9.12）；回归测试补 1 个用例（30 → 31） |
| 第六次 | 后端工程化第三步：ESLint 10（flat config）+ Prettier 3 落地，新增 §8.5；修掉 2 个未使用变量告警；格式化 21 个文件并以「单测 49/49 + 回归 31/31 + tsc --noEmit」三重验证；**后端工程化 100% 完成** |
| 第七次 | 前端联调文档：新增 `docs/前端接口联调文档.md`——14 接口的真实请求/响应样例、完整错误码表、luch-request 拦截器封装、数据格式约定（金额字符串/UTC 时间/ID 字符串）、各端 baseURL 差异与 CORS 现状、联调自检清单 |
| 第八次 | 前端骨架落地（目标端 H5 + uni-ui）：新建 `frontend/`（uni-app Vue3 + Vite + TS），7 页面 + 3 组件 + Pinia + 请求层；新增 §7.6 前端依赖实测与 §7.7 目录结构；记录前端依赖四个坑（§9.13）、onLoad 导入错误（§9.14）、H5 跨域方案（§9.15）；`build:h5` 通过 |
| 第九次 | 前端细节打磨：账单编辑（复用记一笔页 + 明细页左滑/点行三种入口）、EmptyState 组件接入四页、uni-load-more 三态加载、移动端安全区与全局样式、生产 baseURL 改读 `VITE_API_BASE_URL`（默认 `/api` 同域部署）；`.env.example`；编辑链路 create→update→delete 冒烟通过 |
| 第十次 | **多账本改造**：新增 `accounts` 表与 `transactions.account_id`；五步迁移把存量数据平滑迁入默认账本（0 孤儿）；新增 8 个账本接口（CRUD + 删除预检 + 合并预检 + 合并）；账单与统计支持账本维度且不传参数时行为不变；注册自动建默认账本；前端账本管理页 + 首页切换器 + 各页按当前账本联动。设计文档 `docs/多账本设计文档.md`。单测 49→**75**，回归 31→**45** |
| 第十一次 | **首页改版**（参考随手记布局）：新增 `GET /statistics/overview`（历年累计 + 今天/本周/本月/本年/去年，一条 SQL 条件聚合）；`categoryBreakdown` 增加 `count` 笔数；首页重做为 banner + 五区间卡片 + 分类支出排行；新增 `RecordFab` 底栏中部凸起「记一笔」（4 个 Tab 页均引入）；新增 `utils/format.ts` 千分位格式化。单测 75→**83**，回归 45→**47** |
| 第十二次 | **分类支持两级**：`categories` 加 `parent_id` 自引用外键（迁移）；新增 `category-preset.ts`（13 支出大类 + 54 二级 + 7 收入）；seed 改为 upsert 新体系并清理旧分类、其交易按类型改挂回退分类；`CategoryService` 增加层级校验（最多两级、子类类型与父一致、有子不可再挂父）；**统计改为按一级聚合**（`COALESCE(p.id, c.id)`）；分类管理页改两级树形展开、记一笔页改一级+二级两步选择；`iconOf()` 兼容 emoji。单测 83→**94** |
| 第十三次 | **记一笔改为底部弹出分类选择器**：新增 `CategoryPicker` 组件（左一级列表 + 右二级网格 + 最近使用 + 搜索 + 新建二级），**只能选到二级分类**；记一笔页分类改为单行入口（显示「一级 / 二级」）；切换收支类型自动清空分类；保存前强制校验已选分类 |
| 第十四次 | **日期选择改日历弹窗 + 支持时刻**：迁移加 `transactions.record_time`（TIME 可空，不动 record_date 保证统计平滑）；DTO 加 `recordTime`（HH:mm 正则校验，空串=null 清除）；排序改为 `record_date DESC, record_time DESC, id DESC`；新增 `DateTimePicker` 组件（月历翻页、今天高亮"今"、时刻开关 + 时/分滚轮）；分类选择器改左右联动滚动。单测 94→**98** |
| 第十五次 | **文档一致性复核（无代码改动）**：全量实测后订正过期数字——测试用例数（单测 75→98、回归 45→47）、Swagger 覆盖（14→23 操作 / 5 分组 / 49 模型）、分类体系（收入 7 一级 → 2 一级 + 19 二级，每用户 88 个），修正 §4.2 `categories` DDL 缺失的 `parent_id`/外键、§4.3 迁移表缺失 2 个迁移、§5/§7.7 目录树缺失的文件；同步 `category-preset.ts` 头注释、`docs/多账本设计文档.md` 与 `docs/前端接口联调文档.md` 中的过期结论；修正 §0/§2 残留的 `ucharts` 表述（实为 `RingChart` SVG 自绘）。**同轮完成首次 git 初始化**：`main` 分支，107 个文件入库（`node_modules/`、`dist/` 零泄漏），`.gitignore` 补 `.env.*` + `!.env.example`、`unpackage/`、`.workbuddy/`（项目记忆以 `memo/` 为准） |
